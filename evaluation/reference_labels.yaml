# evaluation/reference_labels.yaml — the blind reference groundedness labels (spec §13.7).
#
# Eight items, selected with `SEED = 1729` by `evaluation.schema.reference_subset()` — the only
# place in the project where sampling exists (§13.6), and asserted stable under two different
# `PYTHONHASHSEED` values by `tests/unit/test_reference_subset_deterministic.py`.
#
# `evaluation/deterministic.py::judge_agreement()` reports `judge_agreement_rate` — the fraction of
# these on which the reference label and the judge agree, after binarising the judge's per-item
# groundedness at §13.8's 0.85 threshold — plus `judge_agreement_n` and the per-item disagreements.
# **There is no Cohen's κ and no inter-annotator machinery** (§22): at n = 8 a κ's confidence
# interval is wide enough to be meaningless, while an agreement rate with its n stated is honest.
# The methodology is named accurately everywhere it appears — "reference labels authored by an
# independent model, blind to the judge's verdicts; agreement rate on 8 items" — and **never**
# "human-vs-judge".
#
# An item whose judge verdict is `null` (a twice-failed judge call) or whose turn produced no answer
# leaves the denominator rather than scoring 0, which is why `judge_agreement_n` is published beside
# the rate and is usually smaller than 8.

protocol:
  labeller: >-
    blind-opus-labeller — a separate Claude Opus 5 session dispatched by the controller: the same
    vendor as the agent (Anthropic claude-haiku-4-5), a different model, in an independent session
    that read only the packet — and a different vendor and family from the judge (Google
    gemini-3.5-flash-lite)
  labelled_on: "2026-09-22"
  blinding: >-
    Authored on 2026-09-22 against the final published run `r_1790074972_baseline` (deployed commit
    8a89310). That is the only run in this protocol, and the packet carried that run's own served
    answers: for each item, the question, the answer `r_1790074972_baseline` served, and — verbatim
    — every evidence item the synthesis prompt carried for that turn, each labelled with its class.
    The session was fresh and read only that packet. The packet is judge-free by construction of its
    builder: `scripts/gen_label_packet.py` reads the dataset, the run file's `item_id` → `turn_id`
    mapping and served answer, and the trace store's spans, and it never reads `scores` or
    `verdicts` — so no verdict, no per-claim verdict, no rationale and no groundedness score was in
    front of the labeller. The judged run file was not written until 04:37:44Z on 2026-09-22, after
    this packet was built at 04:25:16Z. The session read no run file, no REPORT.md, no CHANGELOG and
    no phase report.
  selection: >-
    `evaluation.schema.reference_subset()` over the items whose gold behaviour is `answer` — a
    refusal, a clarification and an escalation carry no policy claims to ground.
  seed: 1729
  subset: seed_1729_8
  # The sample is a seeded draw over the gold-`answer` population and consumes no judge output at
  # all, so no judge output — not even the ordering of one — informed which items are in it. The
  # hard-case subset of `reference_labels_hard.yaml` is the opposite and says so.
  selection_disclosed: false
  method: >-
    Binary, answer-level: `grounded` iff every statement of company policy in the answer is
    supported by the evidence shown, and `not_grounded` otherwise. The evidence set is §13.3's:
    everything the synthesis prompt actually carried, in four classes — the whole text of every
    non-quarantined retrieved chunk (`retrieval`), sections fetched in full (`section`), the
    deterministic compliance engine's requirement evidence (`compliance`) and the employee-record
    results the agent read (`structured_data`) — and a claim is supported if any item of any class
    supports it.

# Each label also carries the `turn_id` of the served answer it was authored against, read from
# `r_1790074972_baseline`'s own `items[]`. A verdict is a judgement about one answer, and an answer
# belongs to one turn of one run, so `evaluation.runner.recompute_agreement()` refuses to fold this
# file into a run whose turn for an item differs — the blinding paragraph's "that run's own served
# answers" is then checkable rather than merely asserted (§13.7).
labels:
  - item_id: benefits-001
    turn_id: 4bb6f0bcd9e1b8f15ceb7b7e09127b54
    verdict: grounded
    rationale: >-
      The 90-day waiting period, coverage starting the day it ends, the 401(k) having no waiting
      period and life/AD&D beginning with the medical waiting period all appear in c_7566e6e377678fcd
      and c_88f9cf68022eeaea; the next steps (enrol in week one, digital cards in MosaicOne Benefits)
      come from c_5892cd18e5f9cae8 and c_76cc7adc6c535185.
  - item_id: benefits-002
    turn_id: bb4f35250c9c1137d3971ea57d23fdc1
    verdict: grounded
    rationale: >-
      The 13 November 2026 end date is taken directly from lookup_benefits_status#1
      ("waiting_period_ends": "2026-11-13"), and the 30-day election window plus default
      employee-only enrolment come from c_7566e6e377678fcd, making the 13 December deadline correct
      arithmetic on shown operands; benefits@mosaicrobotics.example appears in c_0df5d11cd585014f.
  - item_id: conduct-001
    turn_id: bfa6e2c8ebbed3da9e810c388fba2534
    verdict: grounded
    rationale: >-
      The 2-business-day acknowledgement and 30-calendar-day investigation target are verbatim in
      c_faa7e3e074e0f281, the 4-business-hour Severity 1 response in c_8befbf9c1eefd50e, the
      definition of "response" in c_8e5b6fa6417ebbd2, and the Employee Relations routing in
      c_b1b9f056ae7f2f27.
  - item_id: expenses-001
    turn_id: 9fc4a9f225ba53adfced6f428c12fe91
    verdict: grounded
    rationale: >-
      The USD 750 annual home-office allowance, the itemised examples, the 1 January reset with no
      carry-over, and the exclusion of laptops/monitors/docks are all verbatim in c_c0b4b1d7c6436137;
      MosaicOne Expenses is named in c_9ce35f1b68d5c0f8 and the itemised-receipt requirement in
      c_98e436172ce912d3.
  - item_id: pto-001
    turn_id: cffd52f958bc110e9fd8c9c4d9610db3
    verdict: grounded
    rationale: >-
      The claim that full-time employees with three or more years of service accrue 1.50 days per
      month is stated verbatim in c_e178629918c7cd96.
  - item_id: remote-001
    turn_id: 3ab532a812363378a42d791e1911f2b6
    verdict: grounded
    rationale: >-
      The 3-days-per-week hub requirement, the team (not individual) setting the days, and the
      Tuesday/Wednesday/Thursday anchor are stated verbatim in c_cf43ea100f30ec65.
  - item_id: remote-002
    turn_id: cd9f733985c4cac6090a69577aedd9a4
    verdict: grounded
    rationale: >-
      The Tax & Legal review for stays over 30 days, the 21-calendar-day filing with destination,
      dates, workspace address and work description, and the three separate approval records are
      verbatim in c_c502ccd2ba5028f5; the right-to-work/letter-of-assignment/no-visa-sponsorship
      claim is in c_cceb27b487508cbd, the device and 5-business-day IT Security notice in
      c_57b2015388bbf7d4, and the rolling 90-day limit in c_864fb910f46475a7.
  - item_id: remote-003
    turn_id: e780294e07af32f4acd29cbe640edf50
    verdict: grounded
    rationale: >-
      The 20-day duration for 1-20 October 2026 follows the inclusive counting rule in
      c_4142a3cd62ef49dd and is confirmed by check_policy_compliance#1 ("duration_days": 20);
      Ireland's approved status is in c_b189d36f26fb7c60, the direct-manager decision with 21
      calendar days' notice in c_0d2c84e4df38b6a0 and c_64fbd63786de81d4 (which also names the
      MosaicOne "Work from another country" request), and manager Dana appears in
      lookup_employee_profile#1.
