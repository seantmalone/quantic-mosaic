# evaluation/reference_labels.yaml — the blind reference groundedness labels (spec §13.7).
#
# Eight items, selected with `SEED = 1729` by `evaluation.schema.reference_subset()` — the only
# place in the project where sampling exists (§13.6), and asserted stable under two different
# `PYTHONHASHSEED` values by `tests/unit/test_reference_subset_deterministic.py`.
#
# `evaluation/deterministic.py::judge_agreement()` reports `judge_agreement_rate` — the fraction of
# these on which the reference label and the judge agree, after binarising the judge's per-item
# groundedness at §13.8's 0.85 threshold — plus `judge_agreement_n` and the per-item disagreements.
# **There is no Cohen's κ and no inter-annotator machinery** (§22): at n = 8 a κ's confidence
# interval is wide enough to be meaningless, while an agreement rate with its n stated is honest.
# The methodology is named accurately everywhere it appears — "reference labels authored by an
# independent model, blind to the judge's verdicts; agreement rate on 8 items" — and **never**
# "human-vs-judge".
#
# An item whose judge verdict is `null` (a twice-failed judge call) or whose turn produced no answer
# leaves the denominator rather than scoring 0, which is why `judge_agreement_n` is published beside
# the rate and is usually smaller than 8.

protocol:
  labeller: >-
    blind-opus-labeller — a separate Claude Opus 5 session dispatched by the controller: the same
    vendor as the agent (Anthropic claude-haiku-4-5), a different model, in an independent session
    that read only the packet — and a different vendor and family from the judge (Google
    gemini-3.5-flash-lite)
  labelled_on: "2026-09-22"
  blinding: >-
    Authored on 2026-09-22 against the published run `r_1790110325_baseline` (deployed commit
    80a5a71, 30 items). That is the only run in this protocol, and the packet carried that run's own
    served answers: for each item, the question, the answer `r_1790110325_baseline` served, and —
    verbatim — every evidence item the synthesis prompt carried for that turn, each labelled with
    its class. The session was fresh and read only that packet. The packet is judge-free by
    construction of its builder: `scripts/gen_label_packet.py` reads the dataset, the run file's
    `item_id` → `turn_id` mapping and served answer, and the trace store's spans, and it never reads
    `scores` or `verdicts` — so no verdict, no per-claim verdict, no rationale and no groundedness
    score was in front of the labeller. What the files themselves evidence about the order is this
    and only this: the packet was written at 21:16:08Z on 2026-09-22 and the judged run file at
    21:27:40Z, so the packet predates the judged run file — but the judge pass was already in flight
    when the packet was built (its first call is logged at 21:00:23Z), so this paragraph does not
    claim that no judge verdict existed anywhere upstream. The session read no run file, no
    REPORT.md, no CHANGELOG and no phase report.
  selection: >-
    `evaluation.schema.reference_subset()` over the items whose gold behaviour is `answer` — a
    refusal, a clarification and an escalation carry no policy claims to ground.
  seed: 1729
  subset: seed_1729_8
  # The sample is a seeded draw over the gold-`answer` population and consumes no judge output at
  # all, so no judge output — not even the ordering of one — informed which items are in it. The
  # hard-case subset of `reference_labels_hard.yaml` is the opposite and says so.
  selection_disclosed: false
  method: >-
    Binary, answer-level: `grounded` iff every statement of company policy in the answer is
    supported by the evidence shown, and `not_grounded` otherwise. The evidence set is §13.3's:
    everything the synthesis prompt actually carried, in four classes — the whole text of every
    non-quarantined retrieved chunk (`retrieval`), sections fetched in full (`section`), the
    deterministic compliance engine's requirement evidence (`compliance`) and the employee-record
    results the agent read (`structured_data`) — and a claim is supported if any item of any class
    supports it.

# Each label also carries the `turn_id` of the served answer it was authored against, read from
# `r_1790110325_baseline`'s own `items[]`. A verdict is a judgement about one answer, and an answer
# belongs to one turn of one run, so `evaluation.runner.recompute_agreement()` refuses to fold this
# file into a run whose turn for an item differs — the blinding paragraph's "that run's own served
# answers" is then checkable rather than merely asserted (§13.7).
labels:
  - item_id: benefits-001
    turn_id: 742c0469df2858c23a15a2ba1dcea12d
    verdict: grounded
    rationale: >-
      The 90-day waiting period, coverage starting the day the period ends, the 401(k) having
      no waiting period, and life/AD&D beginning with the medical waiting period all appear
      verbatim in c_7566e6e377678fcd and c_88f9cf68022eeaea; the two next steps (week-one
      enrolment, digital cards in MosaicOne Benefits) are in c_5892cd18e5f9cae8 and
      c_76cc7adc6c535185.
  - item_id: benefits-002
    turn_id: 17a3ef2de2219f9b8d97b05d89d4a668
    verdict: grounded
    rationale: >-
      The employee-specific date "waiting period ends 13 November 2026" comes from the
      employee's own record (lookup_benefits_status#1, waiting_period_ends 2026-11-13), and
      the 90-day rule, 30-day election window and default enrolment are verbatim in
      c_7566e6e377678fcd; the "by 13 December" next step is 13 Nov + the 30 days shown in that
      same passage.
  - item_id: conduct-001
    turn_id: de87e7eaec39306c85f0ec51459aad15
    verdict: grounded
    rationale: >-
      Acknowledgement within 2 business days and investigation within 30 calendar days are in
      c_faa7e3e074e0f281, the Severity 1 response of 4 business hours is in
      c_8befbf9c1eefd50e, and "as fast as the facts allow" for Severity 1 resolution is in
      c_8e5b6fa6417ebbd2; Employee Relations as a contact is named in c_b1b9f056ae7f2f27.
  - item_id: expenses-001
    turn_id: dfb11c4c8464c290de6d029bef7f8295
    verdict: grounded
    rationale: >-
      The USD 750 per plan year cap, the example item list, the 1 January reset with no
      carry-over and the exclusion of laptops/monitors/peripherals are all verbatim in
      c_c0b4b1d7c6436137, and the next steps name only MosaicOne Expenses and the
      20th-of-month payroll cut-off, both shown in c_9ce35f1b68d5c0f8.
  - item_id: pto-001
    turn_id: 765c42d2379fab4e108a3a53ca1da864
    verdict: grounded
    rationale: >-
      The single claim — 1.50 days of PTO per month for full-time employees with three or more
      years of service — is stated word for word in c_e178629918c7cd96.
  - item_id: remote-001
    turn_id: f74ebb05bad537522915e674e4ac27d7
    verdict: grounded
    rationale: >-
      The claim that hybrid employees assigned to a hub office work on site at least 3 days
      each week is stated verbatim in c_cf43ea100f30ec65.
  - item_id: remote-002
    turn_id: 0eb0eec0c5eaa84efa305f90888938e8
    verdict: grounded
    rationale: >-
      Every requirement listed — three separate approval records, the 21-calendar-day Tax &
      Legal filing with destination/dates/workspace address/work description, the three review
      outcomes, the 12-month tenure and good-standing/ineligible-role rules, the managed
      encrypted laptop with always-on VPN and 5-business-day IT Security notice, and the
      company-data restriction — maps to c_c502ccd2ba5028f5, c_6577bf6f392890bf,
      c_57b2015388bbf7d4, c_cceb27b487508cbd and c_fda4f4779ad55565.
  - item_id: remote-003
    turn_id: 1553f812c9a019955fb8e8fb104caa05
    verdict: grounded
    rationale: >-
      The 20-consecutive-day duration and "no Tax & Legal review under 30 days" come from
      check_policy_compliance#1 and c_4142a3cd62ef49dd, Ireland's approved status from
      c_b189d36f26fb7c60, the 21-day written manager approval raised in MosaicOne under "Work
      from another country" from c_64fbd63786de81d4, the device/5-business-day IT Security
      notice from c_57b2015388bbf7d4, the unverified-device caveat from the compliance
      record's not_stated requirement, and "Dana, your direct manager" from
      lookup_employee_profile#1; the 10 September and 24 September next-step dates are derived
      from the 1 October departure plus the 21-calendar-day and 5-business-day counts shown in
      the evidence.
