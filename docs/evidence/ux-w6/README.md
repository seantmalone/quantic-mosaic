# UX W6 — before and after, for each Critical

Six screen ids, twelve files, all at **1440×900**, full-page.

* **before** — the independent re-audit's own capture set, the screens it judged on 2026-09-15
  (`docs/evidence/ux-reaudit-2026-09-15.md` §6: `make ux-capture`, LOCAL, `LLM_PROVIDER=stub`).
* **after** — `make ux-capture` on this commit, same harness, same four scripted servers, same
  stub scripts. No live model call was made and `.env` was never read.

| Critical | before → after | what changed |
|---|---|---|
| **npo2-01 = dr-new-1** — every chart ~300px in a ~1380px panel; one of six guardrail rule names drawn, rotated, clipped and overlapping the y-axis title | `dashboard-safety-before.png` → `dashboard-safety-after.png` | the plot fills the panel and all six rules are named horizontally on two lines |
| the same defect on the landing page | `dashboard-overview-before.png` → `dashboard-overview-after.png` | the turns-per-hour line fills its panel; the Traffic tile reads **Questions asked** over the figure it counts, with the answered share beneath it |
| **JX-R1 = cpux-re-1** — a completed HR ticket under *"What I suggest you do"*, disclaimed *"Suggestions are guidance, not company policy"*, contradicted two bullets below | `chat-after-confirm-before.png` → `chat-after-confirm-after.png` | *"Done — your request is with the HR Time Off team. Reference MOCK-HR-000001."* leads the turn, above the facts, outside the suggestions group and outside its footnote; the contradicting pointer is gone and the queue has its human name |
| **npo2-02** — *"File … by 13 September 2026 (21 days before 3 November)"*, a month early, plus `E1007` and seven suggestion bullets | `chat-answer-before.png` → `chat-answer-after.png` | `next_steps` renders only on an outcome that groups nothing, so the answered turn carries neither the model-computed deadline nor the employee id; two suggestion bullets, not seven |
| **nav-reaudit-1** — the reader offered no way back, and Back landed on an un-rehydrated `/` with 0 turns | `policy-reader-before.png` → `policy-reader-after.png` | *"← Back to your conversation"* above the document, pointing at `/?session=<id>`; and the passage is rendered markdown with the `·` heading join and a human effective date |
| the conversation that link returns to | `chat-reloaded-before.png` → `chat-reloaded-after.png` | the transcript, replayed — which is what chat's own URL now carries after the first turn |

The full set of 69 screen ids × 3 viewports is regenerated by `make ux-capture` into the
git-ignored `.ux-capture/`; only the twelve files above are committed.

**Fix round 1 (the review of this wave).** `policy-reader-after.png` was re-captured on the fix
commit. The first after-shot showed the defect the review found in it: `policy_markup()` joined a
paragraph's source lines with `<br>`, so the corpus's ~150-character hard wrap was reprinted as a
forced break mid-sentence (*"…anywhere there is a laptop and a reliable / connection."*). The
paragraphs in the current file wrap to the column they are read in, which is what the previous
`<p>{{ chunk.text }}</p>` did before this wave and what the fix restores.

**Refreshed after W6b.** `dashboard-safety-after.png` was first captured before the wave's own
self-review fix to `row_name()` and still showed the disclosure named by an epoch
(*"What was written in full — 1789487770345981, row 1"*). The file now in this directory is the
`make ux-capture` screen from the W6b commit, where the disclosure is named by the row's first
worded column (*"hr_ticket, row 1"*). Nothing else in the set changed.
