# P18 report — publish step (live values into the graded documents)

Base `213ec095b34c67dc844fc4be1733b8ffdd8f81fd` on `main`. One commit, nothing pushed:

| sha | subject |
|---|---|
| `a6b1f02ae7198a9df330a7b792a8879cd3a05c1d` | `P18(docs): the live values are in the graded documents` |

`HEAD = a6b1f02ae7198a9df330a7b792a8879cd3a05c1d`; `git status` clean. Documentation only: no file under `src/`,
`tests/`, `evaluation/`, `scripts/`, `mcp/` or `corpus/` was touched, and no new test was written
(the brief names none; the docs contract tests are the gate and they pass). `.env` was never read,
printed or staged — the one command that needs it (`scripts/check_render_hours.py`) was run as the
brief specifies, in a single shell, with no variable echoed. No request was made to the deploy URL.

---

## 1 · Every value published, and the file it came from

| Value | Published as | Source |
|---|---|---|
| Tokenized grader link | `README.md` `Deployed:` — **and nowhere else** | `data/runtime/post_gate_results.json` → `render.tokenized_url` |
| Deploy URL / service id / plan / region | `deployed.md` §*Deployed URLs*, §*What is live* | `post_gate_results.json` → `render.{deploy_url, service_id, plan_observed, region, num_instances, disk, pull_request_previews, auto_deploy, auto_deploy_trigger}` |
| Deploy trigger (API, hook optional) + `dep-dahcukqfngtc7390n740` `trigger: api` | `deployed.md` §*How a commit reaches the service*, `NEEDS-FROM-USER.md` | `publish_step_wording.deployed.md_deploy_trigger`, `CHANGELOG.md_P11_entry`, ledger 2026-09-10 |
| GitHub secrets actually set (`DEPLOY_URL`, `RENDER_API_KEY`, `RENDER_SERVICE_ID`) | `deployed.md`, `docs/architecture.html` f6-ghsec | `post_gate_results.json` → `github_secrets_set` |
| Turso db / group / region / plan / overages / URL | `deployed.md` §*Cost*, §*Environment variables*, `NEEDS-FROM-USER.md` gate 3 | `post_gate_results.json` → `turso.*` |
| Turso FK parity answer (enforced) | `deployed.md` (the "one thing Turso has never been asked" paragraph, now answered) | `post_gate_results.json` → `turso.parity_smoke` |
| Turso usage 379k read / 10.5k written | `deployed.md` §*Cost* | ledger, P11c section |
| Render hours: build ~11.5 of 500; instance hours UNAVAILABLE | `deployed.md` §*Cost*, §*Live provider facts*, `NEEDS-FROM-USER.md` | `scripts/check_render_hours.py`, run 2026-09-11 (output in §3) |
| Live memory `rss_mb` **293.6** | `deployed.md` §*Memory*, §*Live provider facts*, `docs/demo-script.md` | `post_gate_results.json` → `smoke.health.rss_mb` |
| Cold start 44.8 / 2.8 / 23.3 / **71.0** s; warm **22.5 s**; n=1 | `deployed.md` §*Cold start*, `README.md`, `docs/demo-script.md`, `docs/architecture.html`, traceability RUBRIC5.6 | ledger 2026-09-10 18:55Z; warm turn cross-checked against `P14-report.md` §Item 5 |
| Deployed sha per run: `5419ec5` / `b24ad32` / `da0dca2` | `deployed.md` §*Which commit served which evaluation run* | ledger (P11b, P13, final-sweep entries); run files record `git_sha: dev` (harness tree) |
| Judge billing: paid standard since 2026-09-10, ≈ $0.16–0.18 a pass (249–296 calls) | `deployed.md` §*Cost*, `NEEDS-FROM-USER.md` gate 1a, `CHANGELOG.md` | ledger; `r_*.json` `judge_calls` (249 final, 296 after-P13, 264 before) |
| `LLM_RPM=60` / `LLM_BURST=30` + reason | `deployed.md` §*Environment variables* | ledger 2026-09-10 19:05Z; optimization log (3.9 s/turn bucket wait, p90 12.2 s; 10,000 RPM account limit) |
| Model spend $6.84 over 12 runs + $0.09 demos | `deployed.md` §*Cost* | sum of `metrics.est_cost_usd` over `evaluation/results/r_*.json`; `post_gate_results.json` → `eval.spend_usd.demos` |
| Published run metrics (0.982 / 0.925 / 1.000 / 0.974 / 0.992 / 0.846 / 0.808, p50 16.7 s, p95 32.4 s) | `README.md`, `design-and-evaluation.md` (script-written block + the three-column table), `CHANGELOG.md` | `evaluation/results/latest.json` = `r_1789086979_baseline.json` |
| Before column (0.692 …) and after-P13 column (0.808 …) | `README.md`, `design-and-evaluation.md`, `CHANGELOG.md` | `r_1789055103_baseline.json`, `r_1789069158_baseline.json`, `docs/optimization-log.md` |
| Agreement 1.00 (n=8, seed) / 0.875 (n=8, hard, `pto-003`) | `design-and-evaluation.md` §*Judge methodology* | final run `metrics.judge_agreement_*`, `notes`; ledger 02:10Z |
| Ablation −0.231 (bar 0.25); flip list | `design-and-evaluation.md` §*Ablation* | `evaluation/results/comparison.json` |
| Five strict-pass failures and their clauses | `design-and-evaluation.md` | `evaluation/REPORT.md` (generated by `deterministic.strict_pass_causes()`) |
| 1,815 tests | `ai-tooling.md`, `design-and-evaluation.md`, `docs/demo-script.md` | `pytest -q` at this commit (§3) |

Nothing was published as "pending" — every value the brief names had a recorded source. The only
`pending:` marker left in the repository is `README.md`'s `Demo video:` line (gate 6), as required.

## 2 · What changed, file by file

**`README.md`** — `Deployed:` now carries the tokenized link; `Demo video:` untouched; the cold-start
paragraph replaced with the measured segments; a two-column before/published results table added
under `## Evaluation` with one line pointing at `docs/optimization-log.md`.

**`deployed.md`** — the "not provisioned yet" banner and the whole *What is blocked* section replaced
with *What is live, and what produced it*; live URLs; the deployed-sha-per-run table; the deploy
trigger corrected to the Render API with the hook demoted to optional; the tokenized link referenced
in its `<token>` form only; the measured cold-start table with its `n=1` label and the readiness
defect that the measurement exposed; env-var table showing what is actually set plus the limiter
override and its reason; `## Cost` rewritten with the Render/Turso/judge/agent figures; live RSS
added to `## Memory`; the P11 live-facts table's `pending` rows replaced with observations; the
Turso FK question answered.

**`NEEDS-FROM-USER.md`** — gates 2, 3 and 4 moved to *Discharged* with what each produced; new
discharged gates **2a** (Render payment method) and **1a** (paid billing on the judge project);
gates 6 and 7 stay open, and gate 6 is now marked unblocked; the deploy-hook paragraph rewritten
(no manual browser step remains); *The exact steps* re-framed as the record of what was run; the
blocked-by-gate table replaced with a produced-what/still-open table naming the two open items plus
the grader-invite confirmation.

**`CHANGELOG.md`** — six new dated entries in the file's existing voice: P13 measured, P14, P15,
P16, the published run, and the publish step. (P13's own change entry and the P11c defect entry
were already in the file, written by those phases; I did not duplicate them, and the new "P13
measured" entry is the measured-effect line the brief asks for.)

**`docs/requirements-traceability.md`** — R7.1, SUB.2 and RUBRIC5.6 flipped to `verified` with the
evidence named; RUBRIC5.1 flipped to verified on two of three thresholds with the third published
short (see §5); two project-added rows added, **R6.6** (streaming + narration) and **DOCS.10** (the
optimization log); header prose, the counts line and the coverage summary updated to match.

**`design-and-evaluation.md`** — results block regenerated by the script; a new *Reading the three
columns* section (the human paragraph the brief asks for, see §5); the strict-pass cause table, the
ablation table, the flip list and the judge-agreement figures refreshed from the published run; the
*Known limitations* list re-authored against the current state; the honesty note and the
document-header sentence updated.

**`ai-tooling.md`** — two stale figures corrected (0.654 → 0.808 with the before column named;
1,652 → 1,815 tests).

**`docs/demo-script.md`** — a new `## Optimization story` section: eight beats drawn from the
optimization log's talking points, each assigned to an existing segment so the 9:15 total is
unchanged; the close, the deployment segment and the CI segment updated to the current numbers; the
cold-start wake-up note now quotes the measurement.

**`docs/architecture.html`** — numbers only: the cold-start tile and its detail payload; the GitHub
secrets tile and its payload (it listed a secret that does not exist); the CI tile's deploy trigger.
No styling, layout, `data-id` or diagram geometry was touched.

## 3 · Definition of done — real output

```
$ .venv/bin/ruff check .
All checks passed!
```

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q
........................................................................ [ 99%]
...............                                                          [100%]
1815 passed in 157.55s (0:02:37)
```

Pristine: 1,815 passed, zero warnings, zero skips, zero xfails. (1,815 is the count at the base
commit too — this phase adds no test and touches no code.)

```
$ .venv/bin/python scripts/check_facts.py | tail -2
14 documents · 57 facts · 7 rule scenarios · 33 requirements
OK — every quote is verbatim, every heading path is real, every fact_key resolves.
```

```
$ .venv/bin/python scripts/pii_check.py | tail -1
pii_check: clean — 13 files, no SSN shapes, no non-.example addresses, no non-555 numbers
```

The tokenized link does **not** trip `pii_check`: the script scans `mock_data/` only, and the link
lives in `README.md`. Nothing was allowlisted.

```
$ .venv/bin/python scripts/paste_eval_numbers.py
wrote design-and-evaluation.md's results table from evaluation/results/latest.json (target: deployed)

$ .venv/bin/python scripts/paste_eval_numbers.py --check
OK — the results table matches evaluation/results/latest.json
```

```
$ set -a; . ./.env; set +a; .venv/bin/python scripts/check_render_hours.py
  since 2026-09-01T00:00:00Z
  instance hours UNAVAILABLE of 750 — /v1/metrics/instance-count answered 200 with no samples for this service; the Render dashboard's own usage page is the figure to read
  build minutes  ~11.5 of 500 (488.5 left) — derived from deploy wall-clock, an upper bound

Both figures are approximations of the dashboard's own; the dashboard is authoritative.
```

(No value from `.env` was printed; the script prints only the two budget lines above.)

The docs contract tests specifically, run on their own after every edit:

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q tests/contract/test_docs_completeness.py tests/contract/test_readme_headings.py tests/contract/test_published_run_commands.py
...................................................                      [100%]
51 passed in 1.15s
```

No test needed weakening. `test_deployed_link_is_tokenized_or_names_an_open_gate` now takes its
`https://` branch and asserts the `?access=` form; `test_demo_video_link_is_recorded_or_names_an_open_gate`
still takes its pending branch, which is why gate 6 must — and does — stay unticked in
`NEEDS-FROM-USER.md`.

## 4 · TDD evidence

None to give, honestly: the brief names no test to write, and this phase writes prose and figures
into documents whose assertions already exist (`tests/contract/test_docs_completeness.py`,
`test_readme_headings.py`, `test_published_run_commands.py`, `tests/unit/test_paste_eval_numbers.py`).
Those tests were run before the edits (green), after each document (green) and at the end over the
whole suite. The one behavioural gate that *changed* branch is the `Deployed:` link assertion above,
and it was watched flipping from the `pending:` branch to the `?access=` branch.

## 5 · Ambiguities resolved, and how

1. **"the results block (`paste_eval_numbers.py` regenerates it — run it)" under the README item.**
   The script writes only into `design-and-evaluation.md`, between its two markers; it has no
   README path. Simplest reading that satisfies both halves: run the script (it rewrote the design
   document's block from `latest.json`, i.e. the final run), and hand-author a short before/published
   table in README's `## Evaluation`. README is not script-owned and no marker was invented for it.

2. **"add the human paragraph the runner leaves room for (or `design-and-evaluation.md` §results)".**
   The runner leaves no room: `write_report()` re-renders `evaluation/REPORT.md` whole, preserving
   only the `ABLATION` block, and the file's own header says nothing in it is hand-edited. A
   paragraph added there would be destroyed by the next `--report` and would make that header false.
   I took the brief's alternative and put *Reading the three columns* in `design-and-evaluation.md`,
   directly under the results block. `evaluation/REPORT.md` is untouched.

3. **RUBRIC5.1 cannot honestly read `verified`.** Its verification clause demands groundedness
   ≥ 0.90 (0.982 ✅), `cit_resolve_mean` ≥ 0.95 (1.000 ✅) *and* `strict_pass_rate` ≥ 0.85
   (**0.808 ❌**). Marking it `verified` would be the weakening the brief forbids; marking it
   `planned` would be false, since the run exists and is judged. The row now states two passes and
   one shortfall explicitly, with the five failing items pointed at. RUBRIC5.3 and RUBRIC5.9 stay
   `planned — verified at publish`: both need the recording.

4. **Where the judge-billing gate goes.** The brief asks for it to be ticked, and §19.1 has no item
   for it. I followed the convention the P11b handoff itself sanctioned for the Render card (2a) and
   added **1a** beside the model-keys gate, with a sentence in the header explaining that a
   letter-suffixed item is one the build discovered.

5. **Two rows the traceability matrix had no id for.** R6.6 and DOCS.10 are marked
   *(project-added, not one of the 87 checklist items)* in their own Requirement cell, counted
   separately in the coverage summary (`87 + 2`), so the 87-item arithmetic the document has always
   published stays true.

6. **Scope beyond the brief's eight items, all of it truth-restoring.** Running the script and
   publishing the final column left three neighbouring passages contradicting the numbers directly
   above them, so I refreshed them rather than publish a document that argues with itself:
   `design-and-evaluation.md`'s strict-pass cause table, ablation table, flip list, agreement
   figures and *Known limitations*; `ai-tooling.md`'s two stale figures; and `docs/architecture.html`'s
   GitHub-secrets tile, which named a secret (`RENDER_DEPLOY_HOOK_URL`) that was never created. I
   did **not** restyle anything, and I did not touch `docs/optimization-log.md` (main-session owned)
   or `evaluation/REPORT.md` (generated).

7. **The tokenized link appears exactly once.** The brief permits it on README's `Deployed:` line
   and that line only. `deployed.md` previously instructed pasting it into `## Access` too; both
   places there now carry the `?access=<token>` form and say the real one is in README.

## 6 · Self-review findings (and what I did about them)

- **Caught before commit: the token had leaked into `deployed.md` twice** — the URL table and the
  Access bullet — because `NEEDS-FROM-USER.md`'s own instructions said to paste it there. Replaced
  both with the `<token>` form. Verified with a repo-wide grep: one occurrence, `README.md:10`.
- **Caught: `deployed.md` claimed the service sets "exactly the `render.yaml` list and nothing
  else"**, which stopped being true when `LLM_RPM`/`LLM_BURST` were PUT onto the service on
  2026-09-10. The sentence now says "the ten keys read back from the API, plus the two limiter
  variables added the same day".
- **Caught: the CHANGELOG entries landed in the wrong order** (after the last existing entry rather
  than before my new block). Reordered so the file still reads oldest-to-newest at the tail, and the
  trailing blank line was trimmed.
- **Caught: an invented statistic.** A first draft of the judge-overlap sentence repeated the
  ledger's "the judge scored 25 of 26 answered items 1.0"; the run file says 15 of the 18 judged
  items are exactly 1.0 (the rest are 0.929, 0.90, 0.85). Recomputed from the run file and corrected.
- **Caught: a test-count drift.** `docs/demo-script.md` said 1,814 (the P16 count) while the suite at
  this commit is 1,815. Aligned every literal count to 1,815 with its date.
- **Checked and left alone:** `evaluation/REPORT.md` (generated), `docs/optimization-log.md`
  (main-session owned), `render.yaml` (see concerns), every `src/`/`tests/` file.

## 7 · Concerns for the final review

1. **`gitleaks` may flag README's `Deployed:` line.** The line is `access=<43-char urlsafe token>`,
   which matches the default `generic-api-key` rule's keyword-plus-entropy shape. `gitleaks` is not
   installed here so I could not confirm it, and CI's `lint` job scans **full history**, so once this
   commit is pushed the finding would persist. This commit alone will not trigger CI on a push to
   `main` (`paths-ignore` covers repo-root `*.md` and `docs/**`), but the next code push or any pull
   request will run the scan. The brief forbids me allowlisting to get a check past, and I have not
   touched `.gitleaks.toml`. The decision — a narrowly scoped `[[allowlists]]` entry
   (`targetRules = ["generic-api-key"]`, `paths = ['''^README\.md$''']`, a regex anchored to this
   one link shape) versus accepting a red `lint` — belongs to the main session, which owns the push.
   **This is the single thing most likely to bite.**
2. **`render.yaml`'s header comment still says the deploy hook is "the ONLY path from a commit to
   the running service".** That is now wrong (the job POSTs to the Render API), but `render.yaml` is
   a manifest with contract tests and is outside the brief's file list, so I left it. One comment
   edit would settle it.
3. **`docs/architecture.html` still labels a diagram node "Render deploy hook"** in the figure-6 SVG
   and several edge payloads. I corrected the tiles that stated a wrong *fact* (the secrets list, the
   CI trigger) but did not rename diagram nodes, because that is structural editing of a figure the
   brief told me not to restyle.
4. **The cold-start figure is n=1 everywhere.** Probes 2 and 3 were running while this phase ran.
   When they land, five places carry the number and all of them say `n=1`: `deployed.md` (table plus
   two prose mentions), `README.md`, `docs/demo-script.md` (two mentions), `docs/architecture.html`
   (tile plus detail payload) and the traceability RUBRIC5.6 row.
5. **`data/runtime/post_gate_results.json` is a P11b snapshot**, so its `eval.*` section still names
   the *first* deployed sweep and its `cold_start.status` still reads BLOCKED. I took only the
   provisioning, health and wording facts from it and took every evaluation and cold-start figure
   from the run files, the ledger and the optimization log, as the brief directs.
6. **Attribution trailer.** This session's harness instruction names
   `Claude Opus 5 (1M context)` while `constraints.md` §commit-pattern names `Claude Fable 5.1`. I
   followed the session instruction (it states that it replaces earlier attribution guidance), which
   matches the ruling already recorded in the ledger for Opus-authored phase commits.
7. **Two documents now assert the demo scripts ran live against the deployed URL** (the
   NEEDS-FROM-USER table row, from the P11b handoff's `demo_tasks` block). That is a P11b
   observation, not something I re-ran — the brief forbids calling the live URL.

---

# Fix round 1 — the CI/CD deploy row in `design-and-evaluation.md`

One finding, one file, prose only. No code, no test, no generated block changed.

## The finding

> [Important] `design-and-evaluation.md:717` — the CI/CD job table still says the `deploy` job "curls
> `RENDER_DEPLOY_HOOK_URL`". That secret does not exist (`post_gate_results.json`
> `github_secrets_not_set`), and `.github/workflows/ci.yml` POSTs `/v1/services/$RENDER_SERVICE_ID/deploys`
> when it is absent.

Confirmed, and the diagnosis is right: the publish commit corrected this claim in `deployed.md`
(§*How a commit reaches the service*), `NEEDS-FROM-USER.md` (lines 65–66, 115–116) and
`docs/architecture.html` (`f1-ci`, `f6-ghsec`) but left the primary design document — the one
RUBRIC5.7 and R8.4 send a grader to — asserting the superseded mechanism. The ledger assigned this
correction to the publish step at `progress.md:193` ("Ruling: CI deploy triggers via Render API
(`RENDER_SERVICE_ID` + `RENDER_API_KEY`) with the Deploy Hook as an optional alternative … spec
§14.5/§15.2 + docs updated at publish"). Missing it was an oversight, not a judgement call.

## What changed

**`design-and-evaluation.md:717`** — the `deploy` row now reads exactly as the finding specifies:

```
| `deploy` | `needs: [test, docker]`, main pushes (or an explicit dispatch) only; POSTs `/v1/services/{id}/deploys` with `RENDER_API_KEY` + `RENDER_SERVICE_ID`, or curls `RENDER_DEPLOY_HOOK_URL` when that optional secret is set |
```

**`design-and-evaluation.md:729–730`** — the same superseded claim appears a second time, eleven
lines below the table, inside the R8.4 argument: "so the only path from a commit to the running
service is **the hook that job curls**". Fixing only the row would have left the document
contradicting itself on the same page, so that clause now reads "… is the deploy that job triggers
— `POST /v1/services/{id}/deploys` today, or the Deploy Hook if that optional secret is ever set."
This matches `deployed.md`'s already-corrected sentence ("It triggers it through
`POST /v1/services/{id}/deploys` with `RENDER_API_KEY` and `RENDER_SERVICE_ID`; a Deploy Hook is an
equivalent alternative and wins when `RENDER_DEPLOY_HOOK_URL` is set"). The R8.4 argument itself is
untouched and still holds: `autoDeploy: false` plus `needs: [test, docker]`, evidenced by the
recorded red run.

Both claims are checkable against the workflow at `.github/workflows/ci.yml:139–146`, which branches
`if [ -n "$RENDER_DEPLOY_HOOK_URL" ]` → `curl -fsS -X POST "$RENDER_DEPLOY_HOOK_URL"`, else
`POST https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys` with a bearer header; and
against the live proof already published in `deployed.md`, deploy `dep-dahcukqfngtc7390n740`,
`trigger: api`.

Nothing else was touched. One known stale echo is left deliberately out of scope and flagged
instead: `render.yaml:17`'s inline comment still says "deploys arrive ONLY via the CI deploy hook".
It is a source-file comment, not a graded document, and this phase is documentation-only — worth a
one-word fix in a code phase, not worth a `render.yaml` edit in the publish commit.

## Covering tests and definition-of-done commands — real output

The covering tests for the amended file are the docs contract suites (`test_docs_completeness.py`
asserts `design-and-evaluation.md`'s required headings and claims; `check_facts.py` asserts every
quoted fact in it is verbatim and every heading path real).

```
$ .venv/bin/ruff check .
All checks passed!
```

```
$ .venv/bin/python scripts/check_facts.py | tail -2
14 documents · 57 facts · 7 rule scenarios · 33 requirements
OK — every quote is verbatim, every heading path is real, every fact_key resolves.
```

```
$ .venv/bin/python scripts/pii_check.py | tail -1
pii_check: clean — 13 files, no SSN shapes, no non-.example addresses, no non-555 numbers
```

```
$ .venv/bin/python scripts/paste_eval_numbers.py --check
OK — the results table matches evaluation/results/latest.json
```

(The script-owned results block sits above the edited section and is unchanged, so `--check` is run
rather than the regenerating form.)

Covering tests, on their own:

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q tests/contract/test_docs_completeness.py tests/contract/test_readme_headings.py tests/contract/test_published_run_commands.py
...................................................                      [100%]
51 passed in 0.88s
```

Whole suite:

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q
........................................................................ [ 91%]
........................................................................ [ 95%]
........................................................................ [ 99%]
...............                                                          [100%]
1815 passed in 157.02s (0:02:37)
```

Pristine: 1,815 passed, zero warnings, zero skips, zero xfails — the same count as the publish
commit, as expected for a prose fix. `.env` was not read, printed or staged;
`scripts/check_render_hours.py` was not re-run (no figure in it changed). No request was made to the
deploy URL. Only `design-and-evaluation.md` and this report were staged.

---

# Fix round 2 — the three-probe cold start reaches the graded documents

One finding, [Important]: `docs/optimization-log.md`'s new *2026-09-11 — Cold start, measured three
times without keep-alive* section (commit `a018700`, main-session authored) published an n=3 table
and a keep-alive decision, while every other graded document still said `n=1, … two further probes
queued` and `design-and-evaluation.md:699` denied the decision outright ("A keep-alive cron would
consume ~744 of the 750 monthly workspace hours to disguise a number the rubric asks us to explain,
so there is none"). Two incompatible stories about both `n` and the keep-alive; no artefact behind
probes 2–3; no keep-alive workflow in `.github/workflows/`.

## Which branch of the fix, and why

Branch **(a)** — propagate the three-probe result — because the probes are real and the ledger is
their record. `.superpowers/sdd/2026-09-08-implementation-roadmap/progress.md` carries all three:

```
- 2026-09-10 18:55Z: … Cold start measured (n=1, after 1000 s idle): spin-up→/health 44.8 s; /health→/ready
  2.8 s; first POST /chat 23.3 s; first request total 71.0 s; warm turn 22.5 s.
- Cold probe 2 (2026-09-11 02:19Z, on da0dca2): wake→/health 43.5 s; /health→/ready 0.1 s; first POST /chat 23.9 s;
  first request total 67.5 s; warm turn 22.5 s. (Probe 1 on bf85ffd: 44.8 / 2.8 / 23.3 / 71.0 / 22.5.) Probe 3 running.
- Cold probe 3 (02:37Z): wake→/health 52.4 s; →/ready 0.1 s; first POST /chat 25.2 s; total 77.6 s; warm 23.9 s.
  n=3 summary … The publish step (P18) writes n=1; the final fix wave replaces it with the n=3 table.
```

That last sentence is this fix round's charter. The same ledger carries the keep-alive ruling at
`progress.md:307-312` — Sean, **2026-09-10 20:40Z**: *"measure that cold-start as planned, and then
implement the GitHub actions keepalive"*, with the pinger queued as its own step **after** the
publish wave. So the decision genuinely predates the probes it decides on (the "one day before"
detail in the finding is the ledger's own ordering, not an error), and the workflow is **pending by
design**, not missing. Both facts are now stated where they were previously implied or denied.

The keep-alive workflow itself is **not landed here**: it is a separate queued step with its own
brief, and this phase is documentation-only. It is marked pending in every document that mentions
it, and `.github/workflows/` is untouched (`ci.yml` only).

## What changed

New artefact — **`docs/evidence/cold-start-probes.json`** (the finding's "land a run artefact"):
per-probe timestamps, git shas, the five segments, the medians/ranges, the ledger entry each probe
came from, and the honest note that `measure_cold_start.py` prints a markdown table and writes no
file, so the segments are transcribed from each run's ledger entry. It also records why the rounded
segments do not always sum to the measured total (probe 3 sums to 77.7 and measured 77.6).

| File | Change |
|---|---|
| `docs/optimization-log.md` §*Cold start, measured three times* | probe shas and UTC timestamps added (`bf85ffd` 18:55Z; `da0dca2` 02:19Z / 02:37Z); the medians-do-not-sum note; a pointer to the new artefact; the decision line now carries its ledger timestamp and says it was taken **before** the probes; a new **Status** paragraph stating the workflow is not in the repository yet and that every published figure is no-keep-alive behaviour |
| `docs/optimization-log.md` §*Wave 3 table* | one sentence separating Wave 3's *paid* keep-alive pinger ("not recommended", beside Render Starter) from the free GitHub Actions pinger decided later that day — the last remaining internal contradiction in the log |
| `deployed.md:20` | produced-by row → `2026-09-10 and 2026-09-11 (n=3)` |
| `deployed.md` §*Cold start* | the single-column table replaced by the per-probe table with a median column; the "one sample is one sample" paragraph replaced by the spread (67.5–77.6 s cold, 22.5–23.9 s warm) and the reason probe 1's 2.8 s `/ready` differs; a new **On the keep-alive** paragraph stating the ruling, the 744/750 arithmetic and that the workflow is queued; the defect paragraph now says probe 1 was the first probe after P11c and 2–3 show the fix holding; links to the new artefact |
| `deployed.md:167` | "2.2 s on the laptop against **44.8 s**" → median 44.8 s (43.5–52.4 over three probes) |
| `deployed.md` live-facts table | the cold-start row → median 71.0 s (67.5–77.6), segment medians, `n=3`, both dates, artefact named as a source |
| `README.md:79-86` | n=3, both dates, segment medians, the range, and a clause saying the keep-alive is a queued decision rather than something already hiding the numbers |
| `NEEDS-FROM-USER.md:218` | `done 2026-09-10 (n=1; two further probes queued)` → `done 2026-09-10 and 2026-09-11 (n=3; median 71.0 s cold, 22.5 s warm)` |
| `design-and-evaluation.md:698-712` | *"Cold start is documented, not hidden … so there is none"* rewritten as *"Cold start is measured and published before it is mitigated"*: the n=3 figures, the ordering as the actual design decision, the ruling and its cost, the queued status, and the unchanged mitigations (`/ready`, the banner, the README preflight, cold/warm reported separately) |
| `design-and-evaluation.md` limitation 7 | "a single sample … two further probes are queued" → three samples, the three totals, both dates, and why three is enough for a spread and not for a distribution |
| `docs/requirements-traceability.md` RUBRIC5.6 | evidence cell → segment medians, `n=3`, both dates, the artefact path |
| `docs/architecture.html:1424` + `D["f6-cold"]` | tile line → `first request 71.0 s median (n=3, 67.5–77.6)`; the detail payload's seven rows re-stated with medians and ranges, the `/ready` split between builds, `n = 3` with the artefact path, and the prose now saying no keep-alive runs today and the pinger is queued. Numbers and payload strings only — no styling, geometry or `data-id` touched |
| `docs/demo-script.md` | the wake-the-instance note and the 6:15 segment's spoken line: "we measured it once, labelled it n=1 … rather than hiding it behind a keep-alive cron" → three probes, the published spread, and the keep-alive as the deliberate last step |
| `CHANGELOG.md` | new dated entry recording probes 2 and 3 with their figures, shas and times, the n=3 medians, the list of documents the `n=1` line is superseded in, the artefact, the ruling, and the explicit note that the workflow is not in the repository |

The earlier `2026-09-11 — Publish step` CHANGELOG entry is left as written: it is the dated record
of what the publish step knew, and the new entry names it as superseded rather than rewriting it.

## Covering tests and definition-of-done commands — real output

The covering tests for these files are the docs contract suites — `test_docs_completeness.py`
(`## Cold start` must carry a measured number **and** an ISO date, plus every required heading and
claim in `deployed.md`, `README.md`, `design-and-evaluation.md`), `test_readme_headings.py`, and
`test_published_run_commands.py` (which asserts what may live in `docs/evidence/`: its screenshot
membership rule covers `*.png` only, so the new `.json` sits beside `mcp-discovery-4-tools.json`
without weakening it).

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q tests/contract/test_docs_completeness.py tests/contract/test_readme_headings.py tests/contract/test_published_run_commands.py
...................................................                      [100%]
51 passed in 0.88s
```

```
$ .venv/bin/ruff check .
All checks passed!
```

```
$ .venv/bin/python scripts/check_facts.py | tail -2
14 documents · 57 facts · 7 rule scenarios · 33 requirements
OK — every quote is verbatim, every heading path is real, every fact_key resolves.
```

```
$ .venv/bin/python scripts/pii_check.py | tail -1
pii_check: clean — 13 files, no SSN shapes, no non-.example addresses, no non-555 numbers
```

```
$ .venv/bin/python scripts/paste_eval_numbers.py --check
OK — the results table matches evaluation/results/latest.json
```

(The script-owned results block is untouched by this round, so `--check` is the right form.)

```
$ .venv/bin/python -c "import json;d=json.load(open('docs/evidence/cold-start-probes.json'));print('probes',len(d['probes']),'n',d['summary']['n'])"
probes 3 n 3
```

Whole suite:

```
$ LLM_PROVIDER=stub .venv/bin/pytest -q
........................................................................ [ 87%]
........................................................................ [ 91%]
........................................................................ [ 95%]
........................................................................ [ 99%]
...............                                                          [100%]
1815 passed in 157.00s (0:02:37)
```

Pristine: 1,815 passed, zero warnings, zero skips, zero xfails — unchanged, as expected for a prose
and artefact round that touches no code. `scripts/check_render_hours.py` was not re-run (no figure
in it changed, and it is the one command that needs `.env`). `.env` was never read, printed or
staged. No request was made to the deploy URL. Only the ten files listed above and this report were
staged.

## Verification that the contradiction is gone

```
$ grep -rn "n=1\|further probes\|probes queued\|measured it once\|single sample" \
    README.md deployed.md design-and-evaluation.md NEEDS-FROM-USER.md CHANGELOG.md ai-tooling.md \
    docs/*.md docs/architecture.html | grep -v "^CHANGELOG.md:907"
CHANGELOG.md:909:- 2026-09-11 — Cold start, measured three times: … which supersedes the `n=1` line above …
```

The only surviving `n=1` strings are the historical publish-step CHANGELOG entry (line 907) and the
new entry that names it as superseded. Every keep-alive mention across the graded documents now says
the same three things: no pinger runs today, every published figure is the no-ping behaviour, and a
ten-minute GitHub Actions pinger is a queued decision costing ≈744 of 750 free hours.

## Concerns for the final review

1. **The keep-alive workflow is still owed.** Nine documents now say it is queued. Until that step
   lands, `.github/workflows/` holding only `ci.yml` is the truth they describe — but the promise is
   now published in `README`-adjacent documents, so the queued step should either land or be
   explicitly dropped before the final push.
2. **`tests/unit/test_check_render_hours.py:50`'s docstring** still argues "no keep-alive cron, so
   idle time is free". It is a docstring about interval arithmetic, not a graded claim, and the test
   passes either way — but it is the one place in the tree that will read stale once the pinger
   lands. Out of scope for a documentation round; worth a word in the phase that lands the workflow.
3. **The probe segments are transcribed, not machine-captured.** `measure_cold_start.py` prints and
   writes nothing, so `docs/evidence/cold-start-probes.json` is faithful to the ledger entries
   rather than to a file the script produced, and it says so in its own `produced_by` field. Making
   the script write its JSON would close that gap permanently; that is a code change and belongs to
   a code phase.
4. Every concern from the publish commit and fix round 1 still stands as written, in particular the
   `gitleaks`/README tokenized-link decision, which remains the main session's to make.
