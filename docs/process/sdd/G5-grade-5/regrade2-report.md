# Final grade — quantic-mosaic at 39dc61c

Graded read-only against `/Users/sean/Projects/quantic-mosaic` at HEAD `39dc61c` and the live service, on the rubric in `docs/project-requirements.md`. A solid demo video meeting every video-format requirement is assumed; nothing below turns on the recording.

## Verdict

**Band 4 (high confidence) — the top of band 4.**

Every numbered requirement (R1–R10, DEMO.1–7) is met, most of them well past the bullet: a deployed single-service agentic RAG system whose live `/health` matches HEAD, nine MCP tools served over three transports with schemas byte-identical to the deployed catalog, six wired guardrails, three declarative workflows with executable completion predicates, a confirmation gate enforced inside the MCP server against a `NOT NULL REFERENCES`, five green CI jobs with a genuine container build-and-start check, a 30-item evaluation set with every rubric metric published with its own `n` from a run file the report regenerates byte-for-byte, and documentation that publishes its own failures with file and line cites.

The rubric's score-5 text requires **all** requirements at an outstanding level, and the grading instruction is explicit that a materially inconsistent evaluation claim typically caps at 4. Four such claims survive independent verification:

1. The hard-subset labelling packet prints `subset judge_lowest` in its header, five lines above the sentence swearing the criterion was withheld — contradicting `evaluation/REPORT.md:146` and `design-and-evaluation.md:1495`, in the artifact committed so that "the blinding is inspectable rather than asserted", under the one agreement figure that discriminates judge validity.
2. `design-and-evaluation.md:1886-1888` states "no item in the published run scores below 1.00 on tool recall" while that run scores `remote-004` and `unsafe-001` at 0.75 and the same document tables both.
3. `design-and-evaluation.md:1611`'s "largest delta the arm has produced since 2026-09-10" is falsified by a committed same-build trio at −0.179.
4. The published `no_structured_tools` arm calls `lookup_employee_profile` on 8 of 30 items despite listing it in its own `config.tools_disabled`, and two items' workflow 1.0 on that arm depends on the unfiltered call.

This is a claim-integrity ceiling, not a capability one. The engineering across all ten sections reads at band 5.

## Section scores

| Section | Band | Note |
|---|---|---|
| 1. Environment & reproducibility | 5 | All five bullets machine-checked: pinned 3.12.14, 84 exact pins from a `lock` recipe that now exists, SEED=1729, byte-identical chunking proved three ways, SecretStr credentials, `.env` untracked across 295 commits, whole-history gitleaks. Residual: coverage published as 10,298 statements where the tree measures 10,320. |
| 2. RAG ingestion & indexing | 4 | Four parser paths, byte-verified 205-chunk manifest, local key-free ONNX embedder, sqlite-vec + FTS5 with self-test. Held off 5 because the graded description of the chunking strategy is wrong on R2.2's own bullet in three places. |
| 3. Retrieval, prompting, citations, guardrails | 5 | Hybrid RRF with genuine pre-fusion filtering, one embed per retrieval, threshold semantics with a named regression test, citations rebuilt from the real index, 5/5 out-of-scope refusals, five multi-doc items with enforced breadth floors. |
| 4. Agent orchestration | 4 | Band-5 capability (logged RAG-or-tools gate enforced twice, three workflows with predicate-gated loops, trace proved a span projection, four fault paths, server-side write gate). Capped by a false tool-recall claim and the flagship workflow twin at 0.50 (n=2). |
| 5. MCP server(s) | 4 | Three transports from one factory, live external handshake (200 + session id + 9 tools) and 401 unauthenticated, schemas 0-diff against the deployed catalog, live calls on all nine tools. Capped by the disable filter not reaching the call boundary and by `check_policy_compliance` asserting approvals for undecided requirements. |
| 6. Web application | 4 | Chat UI, `/chat` with answer + blocks + citations + snippets + concise span trace, `/health` with a live nine-tool handshake and a five-string degradation list, two demo tasks reproducible from UI and shell with self-dating prompts that fail closed. Capped by a cold-start "elapsed counter" claimed in three documents and shipped in none. |
| 7. Deployment | 4 | One free Render Docker service, index baked at build (`built_at` predates process start), no paid database, `autoDeploy: false`, deploy gated on `needs: [test, docker, ux]` with a sha-matching wait and live smoke, cold start measured n=3 with spread and per-probe shas. Capped by a retention bound the live store contradicts 2.9×. |
| 8. CI/CD | 5 | One workflow, five jobs green on the tip and shipped; real image build + container start + in-container MCP assert; MCP discovery and tool-call tests over real stdio and uvicorn; 90% gate over 3,140 tests; both push and PR events demonstrated green; recorded skipped-deploy run as negative evidence. |
| 9. Evaluation | 4 | 30 items at the top of the band across all five kinds with corpus-traced golds, every metric with its own `n`, report regenerating byte-for-byte, build-guarded ablation with a published failed prediction, a second zero-LLM sweep, two never-merged blind label subsets. Capped by the packet leak, the false superlative, and the arm that called a disabled tool. |
| 10. Design documentation | 4 | Ten justifications each naming a rejected alternative and heading-asserted; mermaid diagram labelling all seven components with a chunk figure matching the manifest and live `/health`; both demo tasks with argument-level sequences an e2e test enforces. Capped by the CI figure omitting the gate it foregrounds, a footer dated 2026-09-09 crediting the superseded spec, and a process-trail index a round behind. |
| Submission files | 5 | Every required file present and materially beyond the bullet; `ai-tooling.md`'s commit census recounts exactly out of git; the cost row re-derives to the cent; `mock_data/` passes an independent PII scan; the provenance command is empty at HEAD **and** executed by the suite. |
| Demo readiness | 4 | Nine segments summing to 9:15 inside the window, camera and ID bullets covered, both tasks live via one-click server-dated prefills, a five-element DEMO.6 sub-checklist per task pointed at the right surface. Capped by task 2's bullet contradicting its own capture, three divergent timing sets sourced to a retired spec section, and an evaluation beat pointed at figures the page lacks. |

## Gaps to full marks (ranked)

**1 — HIGH. The hard-subset labelling packet leaks its own selection criterion.** `docs/evidence/label-packet-hard-2026-09-22.md:3` prints ``subset `judge_lowest` `` and line 5 then tells the labeller the criterion is "deliberately withheld from this packet". `evaluation/REPORT.md:146`, `design-and-evaluation.md:1495`, `evaluation/reference_labels_hard.yaml` and `docs/evidence/README.md:63` all assert the opposite, and `:1501` invites the grader to check by inspection. `scripts/gen_label_packet.py`'s HEADER interpolates the CLI subset name unsanitised, while its own docstring names this exact hazard. The hard session is the only one that returned a `not_grounded`, on an item the seed session called `grounded`. **Fix:** render the header as a neutral token, regenerate and re-label, and add a contract test asserting no packet carries a score, verdict, ordering hint or criterion-bearing subset name — or retract the clause in the report template and at `:1495` and say the hard session could infer it. **Effort:** 30 min to retract; ~2 h plus a fresh blind session to regenerate.

**2 — MED. `design-and-evaluation.md` states a tool-recall figure the published run contradicts.** `:1886-1888` claims no item scores below 1.00 on tool recall; `r_1790110325_baseline` has `remote-004` and `unsafe-001` at 0.75, both tabled at `:1374-1375` and narrated at `:1752-1761`. `:1882-1884` likewise says `unsafe-001` "passes every clause it defines" with `passed=0`. Written for the 28-item round-1 run and not updated. **Fix:** rewrite both clauses to the round-2 run. **Effort:** 10 min.

**3 — MED. `tools_disabled` is enforced on the offered catalog, not at the call boundary.** `_profile_outstanding` (`orchestrator.py:2058-2104`) has no `_permitted()` clause unlike its siblings at `:2124` and `:2204`, and `_call` (`:2403`) forwards unfiltered. `r_1790111270_no_structured_tools.json` lists `lookup_employee_profile` in `config.tools_disabled`; 8 items record it in `tools_called`, and `remote-003`/`remote-004` carry `requires_tool_results: [lookup_employee_profile]`, so their workflow 1.0 on that arm is produced by the disabled call. Both knob tests read only `tools_offered`. **Fix:** guard `_call` (or `_profile_outstanding`), add the missing test, re-drive or annotate the comparison, and record the effective union at `runner.py:1050`. **Effort:** 1 h plus ~$0.75 for a re-drive.

**4 — MED. `check_policy_compliance` attaches approvals to requirements it declined to decide.** `rules.py:533` reads `unmet:<id>` as `not decided[id]` while a `not_stated` row carries `met=False` (`:636`) and the published `unmet[]` is correctly filtered on status (`:832`). Reproduced: `equipment_request {request_type: "refresh"}` → `insufficient_evidence`, `unmet: []`, plus an early-refresh manager approval; `international_remote {destination_country: "PT"}` → **`conditional`**, `unmet: []`, approvals `[Direct manager, Director, Tax & Legal]` with the 21-day Tax & Legal step — identical to the 42-day run. 18 such guards exist; the disclosure names four. The deciding field `device_age_months` appears nowhere in the published schema, whose `request_type` description asserts the opposite of the approval reason returned. Agent-layer repair fixes prose only; the MCP body is unrepaired. **Fix:** split the guard vocabulary or suppress `unmet:`-guarded advice on `not_stated` rows; name the field; at minimum widen the disclosure and add characterisation tests. **Effort:** 2 h, or 30 min for the disclosure route.

**5 — MED. False ablation superlative and an omitted same-build sweep.** `design-and-evaluation.md:1608-1611` lists six deltas and claims −0.167 is "the largest delta the arm has produced since 2026-09-10". Recomputing over every committed run, the `e85305b` trio of 2026-09-22 (`r_1790067656_baseline` 0.9643 vs `r_1790068520_no_structured_tools` 0.7857, same dataset sha) gives **−0.1786**. `docs/optimization-log.md:896` is worse — it asserts the same superlative while listing −0.231 in the same sentence. **Fix:** add the sweep and drop the superlative in both files. **Effort:** 15 min.

**6 — MED. `deployed.md`'s quota argument rests twice on a retention bound the live store contradicts.** `:580` and `:595` both close with "`TRACE_RETENTION_SESSIONS = 300` bounds the store regardless"; `core/retention.py:29-35` exempts every session with a non-null `eval_run_id`, and live `/health` reports 856 sessions / 20,735 spans. `/health` also runs three uncached `COUNT(*)`s plus two more store queries per call, and the 379k-rows-read figure the paragraph prices was read 2026-09-10 against a near-empty store. **Fix:** reword to "retention bounds conversational sessions at 300; evaluation sessions are kept deliberately", and re-read usage or cache the counts. **Effort:** 20 min, 1 h with caching.

**7 — MED. Demo task 2's tool-chain bullet contradicts its own capture.** `docs/demo-script.md:331-332` says the pinned run "never called" `search_policy_documents`; the capture named at `:273` calls it three times (lines 79/83/87, "8 tool call(s), 3 retrieval(s)"), and `tests/e2e/test_demo_tasks.py:81` lists it in `required_tools`. The bullet's chain is also four calls short. **Fix:** re-cut the bullet off the 2026-09-22 capture and move the hedge to the 2026-09-15 run. **Effort:** 20 min.

**8 — MED. Three divergent demo segment-timing sets, one sourced to a retired spec section.** `docs/demo-script.md:78-88` (0:45–1:25 / 6:25–7:10 / 7:10–7:50 / 7:50–8:50) vs `docs/pre-submission-checklist.md:104-105` (0:45–1:30 / 6:15–7:00 / 7:00–7:40 / 7:40–8:45) vs `docs/requirements-traceability.md:185,187`, which cites "§18.3" — the section the frozen spec's own banner at `:13-16` declares superseded. That row also still pins the 2026-09-11 captures as DEMO.5's live evidence. **Fix:** make `docs/demo-script.md` the single home and have the other two reference it; repoint DEMO.5's evidence. **Effort:** 30 min, 1 h with a contract test.

**9 — LOW.** Chunking described as sentence-boundary overlap in `chunk.py:6-7`, `design-and-evaluation.md:238/:2021` and `.env.example:56`; `chunk.py:155` starts continuation windows at a raw offset and 23 of 29 begin mid-word. Mitigated by an accurate disclosure at `CHANGELOG.md:662-668` and by neither demo citing one. **Fix:** correct the three claims (20 min) or snap the start to a boundary and re-ingest (3 h + re-drive).

**10 — LOW.** Cold-start "elapsed counter" claimed at `README.md:200`, `design-and-evaluation.md:979` and `docs/architecture.html:1823`; `chat.html:20` records removing it and `tests/contract/test_chat_page_renders.py:89` pins the counterless markup. **Fix:** delete four words in three files. 5 min.

**11 — LOW.** `docs/architecture.html` CI figure draws tiles for lint/test/docker/deploy only; `ux` is bare `<text>` at `:1397` with no `data-id` (unique among 170 elements), and the only `needs` edges are test→deploy and docker→deploy. **Fix:** add an `f6-ux` tile, detail entry and edge. ~20 lines.

**12 — LOW.** The evaluation beat (`docs/demo-script.md:85`) names document recall and overall workflow completion on a tile strip that carries neither (`dashboard.py:182-191`, `eval_detail.html:66,186-192`). **Fix:** rewrite the beat by surface, or add the two tiles. 20–45 min.

**13 — LOW.** `docs/demo-script.md:80` "the run pinned on this build" — the capture is on app build `8a89310`, and `git diff --stat 8a89310..HEAD -- src mcp Dockerfile render.yaml requirements.txt` is 14 files. Disclosed at `README.md:101-107`. **Fix:** name the build, or re-capture. 10–30 min.

**14 — LOW.** Action-safety denominator keyed to `safety_at_stake` (`runner.py:561-563`), which a violating non-`unsafe_action` item cannot satisfy — though the 0.0 still drops `strict_pass_rate` over n=30. **Fix:** add `or safety == 0.0`; state the population on the report line. 20 min.

**15 — LOW.** The orchestrator's own out-of-gate `lookup_employee_profile` on a `policy_qa` turn is documented only in code (`orchestrator.py:2075-2081`); `design-and-evaluation.md:533-538` and the lifecycle diagram omit it. **Fix:** three sentences and two diagram lines. 30 min.

**16 — LOW.** `build_eval_compare` (`dashboard.py:3087-3090`) pairs the newest run per variant with no `n_items` guard, and `builds_differ` cannot fire for a run with no committed file — so one admin smoke click can replace the baseline arm under "three variants over the identical items". **Fix:** add the predicate and a notice. 45 min.

**17 — LOW.** Task 2's ≈1:20 chat frame carries two full agent turns plus two confirm resumes (≈65 s of waiting from the capture's spans) plus the 40-second safety beat, with no stated wait allowance unlike task 1. **Fix:** give the row a budget and re-chain the table. 20 min.

**18 — LOW.** Coverage published as 10,298 statements; the tip run and a static re-derivation both give 10,320. The existing guard returns `None` with no `coverage.xml` and degrades to document agreement. **Fix:** repaste and run `coverage xml` before a targeted guard run. 10–30 min.

**19 — LOW.** `docs/architecture.html:1491-1496` footer dates the page 2026-09-09 (never edited since creation) and credits the superseded spec, while the page publishes 205 chunks, 30 items and five jobs correctly. **Fix:** new footer; optionally three numeric assertions. 5–45 min.

**20 — LOW.** `docs/process/sdd/G5-grade-5/README.md` claims 9 briefs / 13 reports and `G5(…)` from `98c893f` where 12 / 28 and `G5b(…)` from `9dcd8dd` are present, with no rows for `regrade-report.md` / `regrade-gaps.json` — while `ai-tooling.md:366` sells it as where the disclosure is checked. **Fix:** a round-2 paragraph and corrected counts. 20 min.

**21–37 — LOW (housekeeping).** Retired `0.26` threshold in both golden trace fixtures; stdio still sold as a demo-video transport in the design doc and two code headers; the design doc's transport-failure retry row overstating the no-retry `tools/call` path; `est_cost_usd` mis-described as including judge spans (~$1.4–1.6 unstated); `cold-start-probes.json`, `deployed.md:244` and `optimization-log.md:309` naming `da0dca2` as the published-run build; judge token totals with no committed artifact and an apparent run mis-attribution; README's before/after pairing two non-comparable hard-subset figures; two renderers disagreeing on which metrics have targets; the rules-engine guard disclosure understating its class by 14 guards; the expense answer justifying the approver from the USD 5,000 conference rule; `check_facts.py` printing a second section count (191 vs 176) and a stale docstring; stale figures in `synthesize.j2` (204/199/995), `g5.py:6` ("one sensitive item"), and `requirements-traceability.md:136` (204 chunks); "69 screen ids" against the harness's 72; "`make setup` runs exactly those steps" plus an undocumented `requirements-ux.txt` / chromium install; `make test`'s unstated dependency on `make ingest`; `docs/**` in `paths-ignore` contradicting the comment above it; and four small stale figures on demo-shown surfaces (paths-ignore panel listing 2 of 3, the 204-chunk memory log, task 1's spoken verdict, the `file://` architecture page).

## Considered and dismissed (overturned on verification)

These were raised by assessors and refuted; they do **not** count against the band.

- **"The deployed service is at its daily LLM cap and cannot answer" (raised three times).** Real but transient and self-healing: `core/llm/limiter.py` counts `llm_call` spans since 00:00 UTC. Live `/health` now reads `calls_today: 0 / daily_call_cap: 1500`, `status: ok`. The condition is reachable essentially only by the author's own eval sweeps and is documented with its exact consequence at `docs/pre-submission-checklist.md:69-78`. The absence of a sixth `degradations[]` string is a ratified spec decision, not an oversight.
- **"`deploy` does not need `lint`, so the secret scan is non-blocking."** No rubric bullet requires it; `lint` runs a formatter, a linter and a scanner, not tests; the choice is disclosed in the architecture figure's own lint tile; and skipping a deploy does not contain a credential already in public history.
- **"G2 cannot see entailment, and the guardrail table oversells it."** The G2 row claims four triggers and one action, never entailment, and the pointer-versus-support distinction is already published three ways including `citation_accuracy 0.889` printed directly above `cit_resolve 1.000`.
- **"Task 1's dataset twin fails its own workflow predicate."** Misattribution: the 0.00 is the eval scorer's cited-breadth clause, not `remote_work.is_complete`, both required tool results were obtained, and the class is disclosed as known limitations 1, 2 and 4.
- **"`workflow_completion_by_workflow` omits a workflow the run drove."** The expenses-002 0.00 is a citation-floor miss, not `expense_claim`'s; tagging the item would publish a more misleading figure, and the keying is disclosed on the dashboard panel itself.
- **"REPORT.md publishes no warm `n` for latency."** `design-and-evaluation.md:1172` publishes "27 warm" script-generated, and the dashboard renders a Warm items row.
- **"The trace surfaces verbatim model narration as hidden CoT."** Extended thinking is disabled, so `response_text` is the ordinary visible channel; the concise trace carries no prose at all.
- Also dismissed: unpinned Docker base image and unverified gitleaks download (no rubric hook, rebuilt and asserted every push); `workflow_dispatch` "can ship any ref" (the hook sends no ref; Render is bound to `main`); the Turso hard dependency (explicitly permitted storage, self-healing, one-variable fallback documented); the 744/750 instance-hour ceiling (below budget, one service, reversible); the 202 fallback branch (a measured-away contingency); the 1280 px table exemption, part-time accrual coverage, `cit_resolve` denominator, `unsafe-002`'s gold, the design table's row ordering, the chunk-size tie, the `.env.example` coupling comment, the missing corpus-table pin, the "tenth-name" miscount, the stale `tools_disabled` test docstring, the bare-balance rule's breadth, the demo-2 holiday span, deployed.md's env table scope, `/health`'s query budget, `make demo1`'s argument shape, and the demo script's unguarded suite figures.

## Strengths

- **Claims are executable.** The published provenance command prints nothing at HEAD *and* is run by `tests/contract/test_published_run_commands.py`; `scripts/paste_eval_numbers.py --check` exits 0; `tests/contract/test_docs_completeness.py` (49 passing) holds the TOC, the ten justification headings, the seven diagram components and the commit census. The round-2 rank-1 dock is closed at its cause, not by editing a sentence.
- **The graded tip is green and shipped.** Five CI jobs succeeded on `39dc61c`; live `/health` returns that sha with `mcp.connected true`, nine tools, 14 docs / 205 chunks, `degradations: []`.
- **The build/start check is real.** Image build, sqlite-vec probe on Debian, container start, an in-container MCP assert of nine tools and the baked index, then a live template and static-asset render — with no credential. Deploy is gated three ways, with a committed red run showing `deploy` skipped.
- **Determinism is proved three times and reproduces locally.** `ingest --verify-manifest` rebuilt the 205-chunk manifest byte-identically; the same command runs in the CI `test` job, the CI `ux` job and at Docker build.
- **Irreversible-action prevention lives in the MCP server**, with a one-time token bound to tool name and canonical arguments, spent server-side, and `mock_writes.confirmation_token` declared `NOT NULL REFERENCES` so an unconfirmed write is unrepresentable. All three non-happy endings are pinned on both the persisted outcome and the rendered fragment.
- **The MCP layer is real on the wire.** Nine schemas diff clean against a live `tools/list`; an unauthenticated POST returns 401; the agent's only path is `session.call_tool`; `agent/**` importing `mcpserver/**` is grep-forbidden; the deployed record carries live calls on all nine tools including both gated writes.
- **The trace is a provable projection**, with a contract test equating trace seqs to span seqs and another forbidding any reasoning-shaped field across payloads, constrained schemas, prompts, golden traces and a live turn.
- **Evaluation artifacts regenerate rather than being typed.** `REPORT.md` reproduces byte-for-byte from the run file, `comparison.json` likewise, `assert_comparable()` raises on mixed target/dataset/build, and the pre-registered prediction failed and is published as a failure with a non-zero exit.
- **Disclosure costs the author marks and is made anyway**: 14 numbered limitations with line cites, three unfixed live-path defects named, the discarded drive and the higher-scoring uncommitted drive both named with their numbers, the voided labelling round reported, the frozen spec banner-marked point by point.
- **Synthetic-data and secrets hygiene are exemplary**: banner-marked mock files passing an independent PII scan, six `SecretStr` credentials with no defaults, `.env` untracked across all 295 commits, and a whole-history gitleaks scan whose claim is literally true.

## What the demo must show

1. **Before the take:** open `/health` and confirm `status: ok`, `mcp.connected: true`, `tool_count: 9`, and `llm.agent.calls_today` well under 1500. An exhausted cap ends every turn `daily_cap_reached` while `/health` still reads `ok`. Confirm `app.git_sha` equals `git rev-parse HEAD`; if the final commit touches only `docs/**`, force the deploy with `gh workflow run ci.yml -f deploy_only=true`.
2. **Deployed URL in the address bar**, webcam picture-in-picture for the full 7–10 minutes, government ID held still ≥3 s at about 0:15.
3. **Both agentic tasks live**, via the one-click server-dated prefills ("Working from Berlin for six weeks", "Three days of PTO, opened for me"). Do not read the frozen September wording.
4. **DEMO.6 items 1–3 from `/dashboard/sessions/{id}#turn-N`**, not the chat page — the chat page shows no tool names, arguments or results.
5. **Task 2's full gate cycle:** card → decline with its cancelled receipt → re-ask → approve → MOCK-HR ticket. Budget two full turns (~65 s of waiting) plus the 40-second safety beat; the script's 1:20 frame is short.
6. **Read breadth off the screen, not the script.** Citation counts and the tool chain on task 1 are non-deterministic; `remote-004` fails its breadth floor on the published run.
7. **Evaluation beat:** `/dashboard/evals/r_1790110325_baseline` plus the Compare tab — document recall 0.908 and overall workflow completion 0.933 are on the Compare tab, not the run-detail tiles. Confirm no dashboard smoke run sits above the three published arms.
8. **CI/CD beat:** the Actions run list, the green run on the tip, and `docs/evidence/ci-deploy-skipped.png` — not `docs/architecture.html`, whose CI figure omits `ux` and the `ux → deploy` edge.
9. If the architecture beat uses `docs/architecture.html`, **open it from the checkout as a local file** — the GitHub link renders HTML source.

## What changed since the 2026-09-22 grade

The round-2 docks are genuinely closed, and I verified each independently rather than taking the ledger's word: the published provenance command is empty at HEAD and is now *executed* by the suite; the `lock` recipe the pyproject pointed at exists (`Makefile:37-40`) with three `uv pip compile` lines matching each manifest's header; the whole-history gitleaks claim is literally true on the graded run (295 commits / 14.27 MB, matching `git rev-list --count --no-merges HEAD`); `ux` joined `deploy`'s `needs` and is contract-tested twice; the corpus table now agrees across script, committed index, design doc and live API at 14 / 63.9 / 30,938 / 176 / 205; the search schema no longer advertises a threshold the server ignored; the equipment policy is self-consistent end to end and `equipment-001` moved from groundedness 0.688 to 1.000; the discovery span carries a real `mcp_session_id` and a full 64-hex `catalog_sha`, both now asserted by shape; the cost row re-derives to $16.2534; a linked TOC exists; the frozen spec carries a dated supersession banner naming seven disagreements; and task 1's demo bullet was re-cut off the new capture. What round 3 adds is a shift in where the remaining risk lives: the two prior cards were capped mainly by stale figures, whereas the defects that cap this one are *claims about the evaluation itself* — a blinding claim falsified by the artifact published to prove it, a metric sentence contradicted by the same document, a superlative contradicted by a committed run, and an ablation arm that called the tool it disabled. Three of the four are single-sentence or single-predicate fixes; the fourth needs a guard and, ideally, a re-drive.