# Final grade — quantic-mosaic @ 2dee277

**Re-grade after the G5 fix wave.** Prior grade: band 4, 2026-09-21 (`docs/evidence/grade-card-2026-09-21.md`). A solid demo video meeting every format requirement is assumed; everything below is graded on the repository and the deployment as they stand.

## Verdict

**Band 4 (high confidence) — at the top of the band.**

> *A genuinely outstanding build — live on HEAD, green five-job CI, nine MCP tools over real transports, a re-derivable 28-item evaluation — held to a strong 4 by a handful of one-command-falsifiable claims in the graded documents and one real clarification defect sitting under the wave's flagship 1.000 metric.*

The rubric's band-5 text requires **all** requirements at an outstanding level and says a single unmet requirement or a materially inconsistent evaluation claim typically caps at 4. Three things cap it here, and I verified each myself:

1. **A false verification command in four graded documents.** README.md:261, deployed.md:83, `docs/pre-submission-checklist.md`:43 (a ticked box) and `docs/requirements-traceability.md`:222 — inside the **RUBRIC5.1** verdict, the top rubric row — all assert that `git diff 8a89310..HEAD -- src mcp Dockerfile render.yaml requirements.txt` is empty. At HEAD it returns two files (17 insertions) from commit `3aa6650`, and that commit is live. This is the sentence that underwrites "the published figures describe the shipped build", and it is the same self-contradiction class the 2026-09-21 grade opened with.
2. **R4.4 is partial in substance, and its metric claim is inconsistent.** `clarify_topic_workflow` concatenates the router's rationale with the user's message, so amb-001 ("Can I take some time off?") was served **amb-002's answer verbatim** — "where would you be working from?" — asking for a destination country the item never needed. The published `clarification_accuracy = 1.000 (n=3)` and `design-and-evaluation.md:1081`'s "all three ambiguous items name every missing detail" both rest on a judge pass over that wrong question.
3. **Two demo beats are wrong today.** The pre-take check on `/dashboard/evals` can never pass, and task 1's DEMO.6 ① names a `get_policy_section` span that the 2026-09-22 run the script itself pins does not contain (39 spans, 38.2 s, no such call — against a documented budget of 46.8 s / 35 spans).

Nothing here is a capability failure. Every rubric bullet is implemented, the deployment is fully functional at the graded commit, and the measurement work is the strongest part of the repository. The gap to 5 is roughly **two to three hours of claim-accuracy work plus one 45-minute code fix**, not new engineering.

## Section scores

| Section | Band | Note |
| --- | :-: | --- |
| 1 — Environment & reproducibility | 4 | All bullets met: venv, `.python-version` 3.12.14, exact pins across three compiled manifests (zero unpinned lines), README covering setup/run/deploy/eval, `SEED = 1729` plus byte-identical `ingest --verify-manifest` in CI, no env file ever tracked. Docked for claim accuracy inside the §1 deliverable: README.md:261's false `git diff`, README.md:166's "full-history gitleaks scan", pyproject.toml:15's nonexistent `lock` recipe. |
| 2 — Corpus & RAG ingestion | 5 | Four real parser paths with the PDF path heading-set-equality tested, heading-aware deterministic chunking proved byte-identical at Docker build and twice in CI, free local ONNX embeddings with an asymmetric-query convention and a mismatch guard, sqlite-vec cosine + FTS5 with a selftest baked into the image, citations deep-linking to live anchors that return 200. Blemish: the graded corpus table comes from a second parser. |
| 3 — Retrieval & generation | 5 | Hybrid dense+BM25 with RRF, pre-fusion filtering on both arms, a fill step making the dense threshold total, full-chunk injection with a citation-coverage index, six guardrails each with a pure rule + span + unit suite. Lower edge of the band: citation accuracy 0.875, one genuinely wrong answer, no rail catches a cited-but-contradicted claim, `min_dense_score` schema default 0.26 vs effective 0.45. |
| 4 — Agent orchestration | 4 | Separated route→act→synthesize loop, a RAG-only gate enforced twice, three declarative workflows with Python predicates over recorded tool results, an operational trace with no hidden chain-of-thought, four tested failure paths, an irreversible-action gate the agent structurally cannot bypass. Capped by the amb-001 clarification defect and the metric claim resting on it. |
| 5 — MCP integration | 5 | Nine tools from one factory over three genuine transports, production calls through a real `ClientSession.call_tool` over loopback Streamable HTTP, an architecture grep making a direct import impossible, committed schemas byte-identical to the deployed `tools/list`, discovery and calls tested over a real stdio subprocess and a real uvicorn, live-call evidence for all nine tools. Residual is record-vs-doc drift only. |
| 6 — Web application | 5 | `/chat` returns all four rubric-named fields as typed models plus usage, timings and `dashboard_url`; one endpoint serves both JSON and the htmx fragment; `/health` is a six-block degradable payload with a live MCP handshake. Two one-click self-dating demo buttons and two fail-closed curl scripts pinned by contract tests that parse the shell scripts. |
| 7 — Deployment | 5 | Live on `git_sha 2dee277` = HEAD with `degradations: []`, 9 tools, 14 docs / 204 chunks; that sha carries a green five-job run whose deploy waited for and smoke-tested the release. Single service, no paid database, full env-var table, a documented trace-store hard dependency with a one-variable recovery, an n=3 cold-start measurement published before the keep-alive that mitigates it. |
| 8 — CI/CD | 5 | One workflow, five jobs, all green on the tip: real container build-and-start check asserting `mcp.connected` + `tool_count==9` + `doc_count==14`, MCP discovery and tool-call tests over both transports, a 90% coverage gate, a pinned secret scan, and `deploy: needs: [test, docker]` that is itself contract-tested and backed by `autoDeploy: false`. Minor: `ux` outside `needs:`, and `paths-ignore` will skip the submission commit. |
| 9 — Evaluation | 4 | Substantively the strongest part of the repo — 28 items across all five required kinds with validated golds, every rubric metric with its own n, p50/p90/p95/p99 plus a cost/latency decomposition, a three-arm ablation whose pre-registered hypothesis failed and is reported as failed, REPORT.md regenerating byte-identically. Capped because the provenance sentence is false, the flagship 1.000 rests on an off-topic answer, and safety/per-workflow evidence is n=1. |
| 10 — Documentation | 4 | Ten justifications each naming a rejected alternative with measured numbers, a diagram labelling all seven required components plus a self-contained interactive walkthrough, both demo tasks as ordered MCP call sequences enforced by e2e tests, 49 contract tests holding docs to measured reality. Held by the falsifiable command, the linked "design source of truth" still drawing a deleted span rail and "four jobs", and no TOC in a 1,735-line graded document. |
| Submission files | 4 | All present and load-bearing: README, design-and-evaluation.md, ai-tooling.md (census recounted from git log and matching to the commit), deployed.md, `evaluation/`, `mock_data/` with in-band synthetic banners and a clean PII gate, `mcp/` with server + nine schemas, repo shared with `quantic-grader` (invite accepted). Docked for README.md:261 and NEEDS-FROM-USER.md:103's stale SUB.3 checkbox. |
| Demo readiness | 4 | Segment table chains and sums to 9:15, 48 UI labels verified verbatim against templates and the live deployment, both stub replays exit 0, both tasks pinned by executable acceptance records, every evaluation/ablation/coverage/CI figure re-derivable. Two beats wrong today (ranks 3 and 4 below). |

## Gaps to full marks (ranked by threat to a 5)

### 1. The one command four graded documents publish as proof of build provenance is false at HEAD — *high*
**Where:** README.md:261 · deployed.md:82-83 · docs/pre-submission-checklist.md:43 · docs/requirements-traceability.md:222 (RUBRIC5.1) · CHANGELOG.md:931

```
$ git diff --stat 8a89310..HEAD -- src mcp Dockerfile render.yaml requirements.txt
 src/hrmosaic/web/static/app.css                 | 11 +++++++++++
 src/hrmosaic/web/templates/dashboard/evals.html |  7 ++++++-
 2 files changed, 17 insertions(+), 1 deletion(-)
```

The change is commit `3aa6650` (a dashboard table CSS/column fix, no behaviour), and live `/health` reports `2dee277`, which includes it. deployed.md:82 states the strongest false form: "`8a89310` is the last commit to change **application** code". `grep -rn '8a89310' tests/ scripts/` returns nothing — no guard, even though `tests/contract/test_published_run_commands.py` exists for exactly this class and says so: *"A definition-of-done command that can never pass is worse than a missing one: it reads as evidence."* The wave's own trail foresaw it (`docs/process/sdd/G5-grade-5/task-6a-report.md`:168-173) and then landed the app change without re-reading the paragraph.

**Fix.** The narrow form is already true — `git diff --stat 8a89310..HEAD -- src/hrmosaic/agent src/hrmosaic/rag src/hrmosaic/core mcp Dockerfile render.yaml requirements.txt` is empty — so restate all five sites to name the actual delta, then add a contract test that executes the printed pathspec. **~20 min.**

### 2. amb-001's clarification asks the wrong workflow's question, and `clarification_accuracy = 1.000` rests on it — *high*
`src/hrmosaic/agent/orchestrator.py`:2921 calls `clarify_topic_workflow(decision.rationale_summary or "", turn.request.message)` — one concatenation — and :296-305 tests `remote_work` first. Verified from the published run:

- `evaluation/dataset.yaml` amb-001 = *"Can I take some time off?"*, `clarification_expects: the requested dates and the number of days`
- `r_1790074972_baseline.json` amb-001 answer = *"Happy to check — where would you be working from? I will also need to know how long you would be there, and from when."* — **byte-identical to amb-002's answer**, plus "Reply with the destination and I will pick this up."

The judge scored `named_missing_information: true` because "how long … and from when" loosely covers the expected details, so the metric is defensible while the question is wrong. The covering test (`tests/unit/test_clarification_names_every_missing_slot.py`:215-222) drives amb-001 from a fixture whose scripted rationale says "No dates were given…", so the failing branch is never exercised; and the flip is nondeterministic on one unchanged build (amb-001 got the correct question in `r_1790067656` and `r_1790075830`).

**Fix.** Two calls instead of one concatenation — `clarify_topic_workflow(turn.request.message) or clarify_topic_workflow(decision.rationale_summary)` (amb-002's own message contains "work from", so it is unaffected) — plus a unit test with a remote-work rationale on a time-off question. Then re-drive the three ambiguous items, or soften `design-and-evaluation.md:1081`. **~45 min** (+12 min / ~$0.8 to re-drive).

### 3. Task 1's DEMO.6 ① chain and timing budget were never migrated off the superseded capture — *medium*
`docs/demo-script.md`:72 says "the pinned live run took 46.8 s over 35 spans" and :215-222 tells the presenter to read `get_policy_section` off the waterfall. The run the same document pins at :186 — `docs/evidence/demo-task-1-live-2026-09-22.txt` — reads "38.2 s of turn time", "`-- trace (39 spans)`", and `grep -c policy_section` → **0**. DEMO.6 is the most heavily graded beat. `docs/evidence/README.md`:44 and `task-7a-report.md`:26 carry the same stale facts.
**Fix.** Re-cut ① from the 2026-09-22 transcript (drop or hedge `get_policy_section` the way task 2 ① hedges `search_policy_documents`) and change the budget to "~40 s: 38.2 s over 39 spans". **~15 min.**

### 4. The pre-take warm-up check on `/dashboard/evals` cannot pass — *medium*
`docs/demo-script.md`:59-61 tells the presenter to confirm the Runs table "fronts `r_1790074972_baseline`". `dashboard.py:2818` orders `created_at DESC` and `ablation.py:39-40` drives baseline **first**, so the baseline is structurally third. Live `/api/eval/runs` fronts `r_1790075830_no_structured_tools`, whose row reads "not judged" for groundedness and citation accuracy with strict pass 0.786.
**Fix.** "confirm `r_1790074972_baseline` is the newest *baseline* row and that no later drive sits above the three published arms", and open the run detail by URL rather than clicking row 1. **~10 min.**

### 5. deployed.md's cost row is contradicted by the repo's own artifacts — *medium*
deployed.md:439: "**$6.84 across the twelve committed evaluation runs** … well inside §9.8's 'under $10 all-in'". There are **22** committed run files summing to **$13.98** (the first twelve sum to 6.841240, so the row was right when dated 2026-09-11 and was never re-derived). The wave measured its own ≈$5 overage internally and never published it; "under $10 all in" is also asserted as *built* at `docs/requirements-traceability.md`:55 and :139. deployed.md:3-5's premise is that every number was measured.
**Fix.** Re-derive from `evaluation/results/r_*.json`, restate the budget honestly, and have `scripts/check_facts.py` compute the sum and compare. **~20 min.**

### 6. The graded corpus table comes from a second parser and contradicts the deployed index on screen — *medium*
`scripts/corpus_stats.py` imports `check_facts.py`'s script-local reader and prints `14 files · 64.2 pages · 31,007 words`, matching `design-and-evaluation.md`:151-168 row for row. The committed index and `hrmosaic.rag.parse.parse_corpus` both sum to **176 sections / 30,840 words / 63.7 pages** — ten of fourteen Sections figures differ (PTO and Remote published as 18 against 15). Cause: raw-bytes word counting (19 `#`-only tokens in one file, 17 underline tokens in another) plus keeping heading-only sections. `CHANGELOG.md` carries both figures. Ranked 27 previously with the fix "point corpus_stats.py at parse_corpus"; the wave recorded it "verified unchanged".
**Fix.** Repoint `corpus_stats.py` at `parse_corpus`, repaste the table, add a contract test that the design-doc table equals `corpus_stats.py --json`. Also `synthesize.j2:12`'s "median 995" → 990.5. **~30 min.**

### 7. "Full-history gitleaks scan" is claimed in README and narrated on camera — *medium*
Graded push run 35739263524: `--log-opts=--no-merges --first-parent 3aa6650^..2dee277` → **"2 commits scanned … 6.56 KB"**. The dispatch run scanned 278 commits. Claimed as full history at README.md:166, ci.yml:42, `docs/demo-script.md`:76, `design-and-evaluation.md`:761/:837, `docs/requirements-traceability.md`:67. `CHANGELOG.md:885` already states the truth, so the distinction was known. History is clean — this is claim accuracy in the graded README, spoken aloud.
**Fix.** Add an explicit whole-repo `gitleaks detect --no-git=false` step, or reword all six sites. **~15 min.**

### 8. The published tool schema advertises a threshold default the server never uses — *medium*
`mcp/tools/search_policy_documents.schema.json`:69 publishes `min_dense_score` default **0.26**; `search_policy_documents.py`:196 substitutes `settings.min_support_score` = **0.45** when the argument is omitted (and no prompt makes the model supply it). 0.26 is the pre-calibration value documented as having sat below the embedder's cosine floor. `design-and-evaluation.md`:603 reproduces 0.26 while :649 names 0.45, with nothing reconciling them. Also at `docs/architecture.html`:1535 and in two trace fixtures.
**Fix.** `float | None = None` defaulting to `settings.min_support_score` (the idiom exists at `retrieve.py:125,133`), regenerate schemas, repaste. **~20 min.**

### 9. `paths-ignore` will skip CI and the deploy on the submission commit — *medium*
`ci.yml:7` excludes `docs/**` and `*.md` from the push trigger. The final commit is by design a root-level README edit pasting the video URL (README.md:11 is the placeholder) — so no lint, no test, no docs contract tests, no deploy, and the graded tip again carries no green run with the live sha lagging HEAD. Docs commits also go straight to main in practice (3 merges total, no hooks, 20 of the last 30 commits docs-only), so the docs contract tests never gate the commits most likely to break them.
**Fix.** Add one checklist line: after the video commit, `gh workflow run ci.yml -f deploy_only=true` and re-read `/health.app.git_sha`. **~10 min.**

### 10. One genuinely wrong answer in the published run, and no rail catches a cited-but-contradicted claim — *medium*
`citation_accuracy_mean 0.8752` against `cit_resolve_mean 1.000`; equipment-001 groundedness **0.6875** (c4 contradicted, c5 unsupported) applying the *Requesting Additional Equipment* USD 500 director threshold to a 36-month refresh; expenses-002 0.7857. G2 checks resolvability and metadata/snippet fidelity, not entailment, so `blocks_dropped_by_g2 = 0` for the whole run. `corpus/rules.yml:394-400` carries the threshold with no `applies_when`, so gold, rules engine and model all conflate it — an authoring inconsistency, disclosed at `design-and-evaluation.md`:1441-1455.
**Fix.** Reconcile the corpus and the gold; add one clause to the guardrail section stating that G2 checks resolvability, not entailment. **~45 min.**

### 11-20 (low, batchable)

| # | Gap | Fix | Effort |
| :-: | --- | --- | :-: |
| 11 | `catalog_sha` persists as `[REDACTED]` (the 64-char hex digest matches `redact.py:49`'s base64 pattern) while four docs advertise it; `test_audit_completeness.py:49` passes vacuously on a truthy `"[REDACTED]"` | Short-circuit the value sweep for digest keys (PRESERVED_KEYS alone won't do it), assert `len(...) == 64`, regenerate fixtures — or delete the field from the docs | 25 min |
| 12 | Safety 1.000 at **n=1**, each per-workflow completion n=1, escalation one item; the runner already scores the four safety clauses on all 28 turns and publishes only the narrow denominator | Add 2-3 `unsafe_action` items and a second `sensitive` item, re-drive; or publish the per-turn clause pass rate as a second denominator | 60 min + one sweep |
| 13 | `r_1790067656_baseline` publishes `judge_agreement_rate 1.0` folded from labels authored against another run's answers (8/8 turn_id mismatch), rendered as an uncaveated live tile | Null the figure with a note mirroring the `r_1789069158` disclosure; print the subset (and the 0.875 hard figure) in `eval_detail.html` | 20 min |
| 14 | Stale figures in graded prose: `catalog_reopened_rate` "0.038 on the published run" (it is 0.000, and line 982 of the same file says so), `pto_request`'s published predicate missing its `lookup_employee_profile` clause, an undated "293.6 MB live" on the diagram against a live 303.8 | Correct the three; add the reopen rate to `paste_eval_numbers.py --check` | 15 min |
| 15 | The linked "design source of truth" still draws the deleted span rail, is headed "one file, four jobs", and prints a §18.3 segment table differing from the shipped script on six of eight rows; 14 in-place "Amended, UX W…" markers prove it isn't frozen | Dated supersession banner at the top of the spec; relabel `design-and-evaluation.md:5` and `README.md:19-20`; fix `design-and-evaluation.md:1555`'s corpus-browser sentence | 20 min |
| 16 | A red `ux` job (299 tests) does not block deploy — run 35723846982 is ux-red/deploy-success with an overall red conclusion; README's CI/CD paragraph omits the job entirely | Add `ux` to `needs:` or make it `continue-on-error`; name the choice in README | 10 min |
| 17 | `mcp_session_id` always null (`getattr(session, "session_id", None)` on an attribute mcp 2.2.0 doesn't have); "exactly one `mcp_discovery` span per turn" false on the confirm path (demo task 2 shows two); `mcp/README.md:136` overstates the retry | Drop or correctly source the id; reword to "per turn pass"; correct the error table | 25 min |
| 18 | Two source docstrings say "two declarative workflows" over a three-member `Literal`; `expense_claim` appears in neither the traceability R4.2 row nor `workflow_completion_by_workflow`, and no test sees its `is_complete` return True (20 calls, all False, across 2,547 tests) | Say "three"; add the row; add a three-line predicate test | 15 min |
| 19 | 1,735-line graded document with no TOC and not one anchor link; `:14` says eight `##` sections where ten exist | Linked TOC after "How to read this document" + a contract test; fix `:14` | 25 min |
| 20 | Residuals: no `lock` recipe; `chunk_size_comparison.json` on the superseded `dataset_sha` with no provenance line; blinding packets uncommitted with two mis-suffixed "Z" times; G1's two clauses coupled at the shipped defaults (334/334 spans have `supporting == candidates`); a second undeclared `agent/** → rag/` import exemption; `stop_reason` untyped with four off-spec values; `NEEDS-FROM-USER.md:103`'s stale `- [ ] SUB.3`; stdio promised in the video with no stdio beat | Batch all eight | 60 min |

## Considered and dismissed

These were raised by assessors and **overturned on verification** — they do not count against the grade.

| Claim | Why dismissed |
| --- | --- |
| Unpinned Docker base image (`python:3.12-slim`) | Not a rubric bullet; the premise "everything else is exact-pinned" is false anyway (no wheel hashes, every Action on a floating major tag); the CI `docker` job rebuilds and health-checks the image on every push, so a base-image patch cannot reach Render silently. |
| Browser commands fail on a fresh clone | `tests/ux/conftest.py:229-231` uses `pytest.importorskip` with a reason naming the two install commands, so `make ux` skips with instructions rather than erroring. |
| `/health` reports `ok` with no index built | The remediation string is already in the payload (`index.error: "… run python -m hrmosaic.rag.ingest"`); `Dockerfile:53` and `scripts/assert_health.py` make the state unreachable in a deployed image; the five-string vocabulary is spec-fixed and contract-tested. |
| Undefined `$DEPLOY_URL` in the README sweep | `runner.py:122` raises `BadTargetUrl` naming the variable and the gate; the block's three sibling lines are explicit owner-only credential placeholders, and `make eval` works from `.env.example` with no export. |
| Two README imprecisions (test counts, setup steps) | The paragraph states the split in its next sentence, and the phrasing is pinned by `test_docs_completeness.py`'s NUMBER_DOCS regexes — the proposed rewrite would turn CI red. |
| Concurrent dispatch/push deploy race | The runs were serialised by the existing concurrency group (dispatch waited 15 min); the 502 came 0.9 s after the sha flipped, inside Render's own instance swap. The ledger's own attribution was wrong. |
| Daily LLM-call cap endangers the demo | 538 calls remain against ~10-20 for a recording (~30x headroom); the cap is a documented graceful 200 with a stated notice, and `calls_today`/`daily_call_cap` are on `/health` and dashboard page 1. |
| `doc_ids` is an unvalidated hard filter | Deliberate, documented and already tested in the opposite direction (`test_a_filter_that_matches_nothing_returns_no_hits`); the SEARCH_BREADTH reminder and the G1 catalog reopen both intervene before any refusal; no recorded run ever passes the argument. |
| Ambiguous turns outside the three workflow topics name nothing | Reaching the fallback needs policy_qa + null workflow + a rationale dodging six slot-word sets and three topic-word sets; the turn still ends `clarify` with one question, a next step and two chips, and the cited test asserts the opposite of what was claimed. |
| Cancelled write filed as `refused` | The trace already distinguishes the three cases (`confirmation … declined` span vs an `error tool_failed` span vs neither), the decision is badged on `/dashboard/safety`, and no cancelled turn has ever entered the over-refusal denominator (the runner only auto-confirms). |
| `unfilled_slot()` production-dead | With a named workflow it is bit-identical to `unfilled_slots(...)[0]`, so the test asserts live behaviour; the concept is still live via `turn.clarify_slot`, and `__all__` already excludes the wrapper. |
| `deterministic.py` schema-validation over-claim | The gap record asked only that the *design doc* disclose the divergence, which it does by name; the phrase is a repo-wide idiom appearing in four unflagged files, and no published number is affected. |
| Stale MCP screenshot / hard-coded corpus size in a tool description | The screenshot's caption claims nothing untrue and is dated in the evidence index the design doc links; the "14 documents" string is accurate and a corpus change breaks six independent assertions plus the deploy gate. |
| Demo-script `--recorded` hint, free-tier instance-hour ceiling, README four-job enumeration, draft_hr_email transcript not script-driven | Each satisfied elsewhere or not a rubric bullet: the documented demo command carries the token and the 401 body names the key; the keep-alive off-switch is documented in four places with 744 ≤ 750; README.md:64-66 already describes the `ux` CI job; two demo tasks are reproducible three ways and the third transcript is bonus evidence with its curl recipe in its own header. |
| Judge-agreement tile / cold-probe harness path / design-doc question paraphrase / `architecture.html` untested | Both agreement figures are published paired in the graded artifacts and the hard one is in the page's own JSON export; the rubric asks for the cold figure (published with n, range and dates in deployed.md), not for it to come out of the runner; every one of the 28 paraphrased rows is factually correct and id-traceable; `architecture.html` *is* asserted by `test_keep_alive.py` and is currently accurate on tools, jobs, routes and item count. |
| RSS drift, "Continue this conversation" persona scoping | The demo script says "about 300 MB" and tells the presenter to read the live number; the button's conditionality is spec-mandated and audit-remediated, and the script never opens a foreign persona's session. |

## What the demo must show

Live against `https://mosaic-hr-copilot.onrender.com` only — `docs/demo-script.md`:6-7 forbids localhost and pre-recording. Wake the instance on `<DEPLOY_URL>/health` and wait for a 200 first (cold start: median 71.0 s to first answer, 44.8 s of it before `/health` answers).

- **Format:** 7:00-10:00 (the plan sums to 9:15); webcam PiP for the full duration, never cut after the intro; continuous narration; government ID held still and legible for ≥3 s at ~0:15; audio verified on a 20-second test clip.
- **Setup:** two browser tabs, one persona (`E1042`) throughout, entered via README.md:10's `?access=` link; ≥1440 wide at 100% zoom; check the PiP does not occlude the **Sources (n)** strip, the sticky composer, or the right-hand chevron that opens a span payload.
- **Both tasks end to end from the UI buttons** — *Working from Berlin for six weeks* and *Three days of PTO, opened for me* — which fill the composer with server-dated prompts; the presenter still presses **Send**.
- **DEMO.6, per task:** tool names, arguments, outputs, retrieved citations, final answer/action, read off `/dashboard/sessions/{id}#turn-N` in `seq` order. *Correct the task-1 chain first (rank 3) — `get_policy_section` is not in the pinned run.*
- **Task 2 must show the gate:** the *Confirm before anything is written* card with "Nothing is written until you choose.", the token-free `CONFIRMATION_REQUIRED` body, the `paused for confirmation` span, then `POST /chat/confirm` and the `created` result naming `MOCK-HR-<n>`.
- **The observability beat is said on the session record inside a task segment**, not in the 6:00-6:25 tour — and do **not** claim verbatim LLM messages (an `llm_call` span carries `messages_ref`; the bodies live in a table no route reads).
- **Walkthrough, four topics:** design (`docs/architecture.html` or the mermaid diagram, all seven components labelled), deployment (`render.yaml`, `/health`, `deployed.md`), CI/CD (the Actions run list — say **five jobs** and **3,116 of 3,415**), evaluation (`/dashboard/evals` for `r_1790074972_baseline` on build `8a89310`, plus the Compare tab's "Claim supported: no").
- **Dashboard pages by number:** 5 `/dashboard/llm` "Model calls", 8 `/dashboard/safety` "Guardrails", 9 `/dashboard/mcp` "Tool server", 11 `/dashboard/evals`.
- **Open human gates:** `NEEDS-FROM-USER.md`:26 (record) and :35 (submit), plus README.md:11's `Demo video:` placeholder. After pasting the URL, dispatch `ci.yml` and re-read `/health.app.git_sha` — `paths-ignore` skips CI on a root-level `*.md` commit (rank 9).
- **Post-grading hygiene** already listed at `docs/pre-submission-checklist.md`:92-98: rotate `APP_ACCESS_TOKEN`, delete the `ci-red-evidence` branch, remove `data/runtime/provision_turso.json` if provisioning left it.

## What changed since the 2026-09-21 grade

Substantively, a great deal — and almost all of it held up under checking. The prior card's rank-1 staleness gap is closed: `paste_eval_numbers.py --check` exits 0, `latest.json` names `r_1790074972_baseline`, and every headline in README and the design doc matches that run file field for field, including the awkward ones (action safety n=1, clarification n=3, hard agreement 0.875). Gap 6 (a fresh clone cannot answer) is closed by documenting `make ingest` as the first line of Local Run; gap 7 (demo task 2 narrating a rule its own request failed) is closed properly — both scripts now fetch the server's own self-dated prompt and fail closed; gap 11's profile debt now keys on recorded call arguments and moved remote-003 from tool_recall 0.667 / workflow 0.0 to 1.0 / 1.0; gap 16's MCP error semantics are corrected rather than deleted; gap 20 is fixed in the *stronger* direction (`MCP_TOOLS_DISABLED` unioned in `router.allowed_tools`, closing a privilege hole the naive fix would have opened); gap 21 gives `draft_hr_email` live calls with both endings on the deployed record; gap 22 is closed outright — the tip carries its own green push run whose deploy put `2dee277` on the live service, so `/health` now reports exactly HEAD. Five demo gaps, the span-rail leakage, and the "four jobs"/"1,800 tests" figures are all corrected in the graded documents. **What did not change is the shape of the residue:** the wave's last two commits re-opened the same provenance-claim class the prior grade opened with (the `git diff` sentence, now false in four graded documents including the RUBRIC5.1 row), its headline clarification fix left the topic-inference path serving a neighbouring workflow's question under a 1.000 metric, and its own deferred-minor list (`min_dense_score` 0.26/0.45, the corpus two-parser divergence recorded as "verified unchanged") is still open. This is a band-4 submission that is two or three hours of claim hygiene and one 45-minute code fix away from a defensible 5.