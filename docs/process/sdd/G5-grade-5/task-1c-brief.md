## Task 1c — A confirmed `draft_hr_email` write is narrated, not refused

Evidence (deployed e85305b, session `3de0ea69a1d09447ce36fa0398e19cab`, turn `87992de4dc8a7844370b5ed49b9a555d`, dashboard `/dashboard/sessions/3de0ea69a1d09447ce36fa0398e19cab#turn-1`): the agent proposed `draft_hr_email` (recipient_role manager, to_name Dana Whitfield), the confirmation card appeared with no token, `/chat/confirm` performed the write and the server minted `MOCK-EMAIL-000018` — and the turn then closed with `outcome: refused` and the **out-of-scope refusal copy** as its answer. The draft exists; the user is told the question is out of scope. Task 4's report (`.superpowers/sdd/2026-09-21-grade-5/task-4-report.md`) records the exact request that produced it, and says `scripts/demo_task_2.sh` pins the equivalent path for `create_mock_hr_ticket` while nothing pins it for `draft_hr_email`.

Required:
1. Diagnose from the trace (use the deployed `GET /api/traces/turns/{turn_id}` with the README bearer token, and the orchestrator's `_resume` path) why the resumed turn after a confirmed `draft_hr_email` reaches the out-of-scope refusal — likely the post-write synthesis/refusal guard (G1 or the topic gate) is evaluated against a turn that has no retrieval evidence (a pure "draft me an email" ask), or the resume path only has a "Done:" template for tickets.
2. Fix at the smallest correct place so a confirmed `draft_hr_email` closes with the "Done:" opener naming the `MOCK-EMAIL-*` reference and the recipient, mirroring what `create_mock_hr_ticket` does; the refusal guard must not fire on a turn whose write has just been performed.
3. Tests: an integration test in the style of `tests/integration/test_confirm_resume_lifecycle.py` that drives a manager-message ask to the confirmation card, confirms, and asserts outcome `answered`/`performed` (whatever the ticket path asserts), the `MOCK-EMAIL` reference in the answer, no out-of-scope copy, and one `draft_hr_email` write in `mock_writes`. Reuse or extend an existing LLM stub script if one covers draft_hr_email; otherwise record one from the deployed trace's completions.
4. Do not change the tools' schemas or the MCP server; do not change `route.j2`.

Run `make lint`, the touched suites, and `make test` once. Bump the suite-size figure in the four documents (README.md, ai-tooling.md, design-and-evaluation.md, docs/requirements-traceability.md; currently 3,401) if you add tests. Commit as `G5(agent): a confirmed draft_hr_email is narrated as done, not refused`.
