## Task 10 — Round-2 code and configuration (re-grade gaps 2, 8, 11, 16, 9, 7, 17, 18, 20a, 13-dashboard)

Read each gap by rank in `.superpowers/sdd/2026-09-21-grade-5/regrade-gaps.json` (evidence + fix) before touching it.

1. **Gap 2 (high) — amb-001 gets amb-002's question.** `clarify_topic_workflow` in `src/hrmosaic/agent/orchestrator.py` concatenates the router's rationale with the user's message and tests remote-work first, so an incidental "remote work" in the rationale walks the remote slot order for "Can I take some time off?". Fix: test the user's message FIRST, then fall back to the rationale (two calls, not one concatenation). Add a test replaying amb-001's published router output (read it from `evaluation/results/r_1790074972_baseline.json` → the amb-001 item's route span/rationale, or from the deployed trace API with the README bearer token, GET only) asserting the served question names the dates and the day count, and keep the amb-002/amb-003 tests green.
2. **Gap 8 — `min_dense_score` schema default 0.26 vs effective 0.45.** Make the parameter `float | None = None` in `src/hrmosaic/mcpserver/tools/search_policy_documents.py` with `settings.min_support_score` as the documented effective default (the idiom exists at `src/hrmosaic/rag/retrieve.py:125,133`); regenerate `mcp/tools/*.schema.json` with `scripts/gen_tool_schemas.py`; keep `tests/contract/test_tool_schemas_committed.py` and `test_tools_match_spec.py` green (update the spec-side expectation if the contract test reads the frozen spec — say so).
3. **Gap 11 — `catalog_sha` persisted as `[REDACTED]`.** In `src/hrmosaic/core/redact.py` short-circuit the long-base64 value sweep for digest keys (`catalog_sha`, `corpus_sha256`, `manifest_sha256`) or tighten the pattern so a 64-hex digest survives; unit test both a digest surviving and a real base64 secret still redacted.
4. **Gap 17 — `mcp_session_id` always null; "exactly one discovery span per turn" false on the confirm path; overstated retry.** Either capture the session id via the streamable-HTTP transport's `get_session_id` callback (mcp 2.2.0 `streamablehttp_client` returns it) or drop the field from `McpDiscoveryPayload`; whichever is smaller and true. Fix the code comment/docstring that claims one discovery span per turn (say "per turn pass"). Note for the docs task which documents need the same wording (mcp/README.md:136/232, design-and-evaluation.md:344, architecture.html nodes) — do not edit docs.
5. **Gap 18 — "two declarative workflows" in `src/hrmosaic/agent/router.py:44` and `src/hrmosaic/agent/workflows/__init__.py:1,11`;** say three; add a three-line unit test over `expense_claim.SPEC.is_complete` (empty / profile-only / complete).
6. **Gap 16 + Gap 9 — CI.** In `.github/workflows/ci.yml`: add `ux` to `deploy`'s `needs:` (`needs: [test, docker, ux]`), rewrite the comment that said `needs:` is deliberately absent, and remove `'*.md'` from `paths-ignore` (keep `docs/**`, `evaluation/results/**`, `evaluation/REPORT.md`) so a root README edit runs CI and deploys. Update `tests/contract/test_deploy_manifests.py` (it asserts `deploy.needs == ["test","docker"]` and `"needs" not in jobs["ux"]`) and `tests/contract/test_keepalive_workflow.py` if affected. List for the docs task every document sentence that states the old rationale (grep `needs` / `never block` in README.md, design-and-evaluation.md, docs/architecture.html, docs/demo-script.md, docs/requirements-traceability.md) — do not edit them.
7. **Gap 7 — "full-history gitleaks scan".** Make the claim true on every run: in the `lint` job add an explicit whole-history scan step using the pinned gitleaks binary/action with `--log-opts` covering all history (or `gitleaks git` / `detect` over the full checkout with `fetch-depth: 0`, which the job already has). Keep the existing step if the action's per-push scan is still wanted. Verify the YAML with `.venv/bin/python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` and the contract tests that parse ci.yml.
8. **Gap 20(a)** — `pyproject.toml:15` names a Makefile `lock` recipe that does not exist: add a `lock:` target that runs the exact `uv pip compile` commands the README describes (read README's "compiled from the authoritative pyproject.toml" sentence and `requirements*.txt` headers for the command), or change the pointer to the literal command. Prefer the target.
9. **Gap 13 (dashboard half)** — `src/hrmosaic/web/templates/dashboard/eval_detail.html`: print the agreement subset name beside `judge_agreement_rate` (and the hard figure beside its subset) so a folded-in figure is never shown without its subset. Extend an existing view-model test rather than adding a collected test if you can.

Rules: smallest correct change; `ruff`; `make lint`; run the touched test files, then `.venv/bin/pytest -q -p no:cacheprovider tests/contract tests/unit tests/integration tests/architecture` (~6 min); `python -m hrmosaic.rag.ingest --verify-manifest` must still pass. You own the suite-size bump: if the collected count changes, update README.md, ai-tooling.md, design-and-evaluation.md, docs/requirements-traceability.md (and docs/demo-script.md's "3,116 of 3,415" pair) — another agent (Task 11) is editing corpus/, evaluation/dataset.yaml, data/index/chunks.manifest.jsonl, scripts/corpus_stats.py, evaluation/results/chunk_size_comparison.json and must not be touched; it will NOT add collected tests. Commit as `G5b(code): …` with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, no `Claude-Session` trailer, explicit `git add`. Never read `.env` or `data/runtime/provision_turso.json`.
