## Task 11 — Round-2 dataset, corpus and corpus-stats (re-grade gaps 12, 10, 6, 20b)

Read each gap by rank in `.superpowers/sdd/2026-09-21-grade-5/regrade-gaps.json` (evidence + fix) before touching it. Files you own: `evaluation/dataset.yaml`, `corpus/**`, `data/index/chunks.manifest.jsonl` (regenerated only by `python -m hrmosaic.rag.ingest`), `scripts/corpus_stats.py`, `scripts/chunk_size_sweep.py` if needed, `evaluation/results/chunk_size_comparison.json` (regenerated only by the sweep script), `evaluation/schema.py` ONLY if the dataset schema must grow. Do NOT add collected tests (another agent owns the suite-size figures); extend existing tests in place if a change needs pinning. Do not edit any prose document except `corpus/README.md` if a corpus figure it states changes.

1. **Gap 12 — widen the n=1 denominators.** Add to `evaluation/dataset.yaml`, in the file's existing schema (study 5-6 existing items of each kind first — ids, category, kind, gold_behaviour, expected_tools, forbidden_tools, expected_docs, expected_end_state, gold_answer_short, clarification_expects, the workflow tag): (a) two more `unsafe_action` items — one asking the agent to draft/send an email to the manager (draft_hr_email must stop at the confirmation card), one asking to open a ticket in wording that must also stop at the card; (b) one more `sensitive` item (a workplace-conduct concern that must escalate); (c) one more `pto_request` workflow instance and one more `remote_work_eligibility` workflow instance (different persona/dates/destination from the existing ones, with gold answers derived from `corpus/facts.yml` / `corpus/rules.yml` and the mock data — cite the fact keys). Every gold must be verifiable against the corpus and mock data; use `scripts/check_facts.py` conventions. Keep the total ≤ 35 and update the counts in `evaluation/dataset.yaml`'s header comment if it states them. Run `tests/unit/test_ids_unique.py`, `tests/unit/test_mock_schemas.py`, and whatever unit tests load the dataset (grep `dataset.yaml` in tests/) — all green.
2. **Gap 10 — equipment-001 is a real corpus incoherence.** `corpus/equipment-and-asset.md` puts the USD 500 director threshold under "Requesting Additional Equipment" and makes a 36-month refresh an automatic IT ticket, while `corpus/manager-approval-matrix.md` routes an early laptop refresh to the direct manager alone; the item's gold conflates them. Resolve editorially and coherently: state in equipment-and-asset.md that a scheduled 36-month refresh is IT-ticket-only regardless of price, that an early refresh needs the direct manager per the approval matrix, and that the USD 500 director threshold applies to additional-equipment requests; align `corpus/rules.yml` and `corpus/facts.yml` (every quoted fact must stay verbatim — run `scripts/check_facts.py`), and re-author equipment-001's gold. Keep the edit small.
3. Re-ingest: `.venv/bin/python -m hrmosaic.rag.ingest` (regenerates `data/index/chunks.manifest.jsonl`), then `--verify-manifest`. Report the new chunk count (was 204) and document count — the docs task will update every "204 chunks" statement.
4. **Gap 6 — corpus stats parity.** Repoint `scripts/corpus_stats.py` at `hrmosaic.rag.parse.parse_corpus` (the production parser) so its words/sections/pages match the deployed index; run it and report the new figures (the docs task repastes the table in design-and-evaluation.md:151+ and traceability PD.1). Keep check_facts' reader for verbatim matching only.
5. **Gap 20(b)** — `evaluation/results/chunk_size_comparison.json` carries a stale `dataset_sha`; re-run the sweep (`scripts/chunk_size_sweep.py`, zero LLM cost) on the new dataset so it names the current sha. Report its result.
6. Run `make lint`, `scripts/check_facts.py`, `scripts/pii_check.py`, and `.venv/bin/pytest -q -p no:cacheprovider tests/unit tests/contract` (~5 min). Commit as `G5b(dataset-corpus): …` with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, no `Claude-Session` trailer, explicit `git add` of your files only. Never read `.env`.
