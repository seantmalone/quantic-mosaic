# Requirements Traceability Matrix — Mosaic HR Copilot

**Project:** `quantic-mosaic` · Quantic "AI Engineering Techniques and Architectures"
**Date:** 2026-09-08 · **Status of every row:** `planned` (nothing is built yet)

**Sources**
- Requirement text: [`docs/project-requirements.md`](project-requirements.md) (extracted from the authoritative PDF)
- Design that addresses it: [`docs/superpowers/specs/2026-09-08-hr-agentic-rag-design.md`](superpowers/specs/2026-09-08-hr-agentic-rag-design.md) — cited as **§n**
- Phase that builds it: [`docs/superpowers/plans/2026-09-08-implementation-roadmap.md`](superpowers/plans/2026-09-08-implementation-roadmap.md) — cited as **P0…P12**

**How to use this file.** Every row's *Verification* column names a command, a test file or a
committed artifact — never a claim. As each phase lands, flip that row's *Status* to `built` when the
verification passes in CI, and to `verified` when it has additionally passed against the **deployed**
instance. A row may never be marked complete on the strength of prose.

**Status vocabulary:** `planned` → `built` (green in CI) → `verified` (green against the deployed URL
or, for human-only items, confirmed by the user).

**Counts:** 87 checklist items — PD ×4, R1–R10 ×47, DEMO ×7, SUB ×3, DOCS ×9, RUBRIC5 ×9, USER ×8.

---

## Project Description — corpus, mock data, cost

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **PD.1** | Coherent policy corpus of ~5–20 short md/HTML/PDF/TXT files totalling 30–120 pages | §5.3 (14 files, 63 pages, 4 formats), §5.2 (fact ledger) | **P2** | `python scripts/corpus_stats.py` prints file and page counts; `tests/unit/test_corpus_stats.py` asserts `5 ≤ files ≤ 20` and `30 ≤ pages ≤ 120`; CI step 17 asserts `round(measured_total) == stated_total` | planned |
| **PD.2** | Corpus supports realistic questions on PTO, holidays, remote work, expenses, data security, benefits, onboarding, equipment, leave, workplace conduct | §5.3 (topic→document map: PTO ①, holidays ①, remote ②③, expenses ④⑤, security ⑭, benefits ⑦, onboarding ⑨, equipment ⑥, leave ⑧, conduct ⑩⑬) | **P2** | `tests/unit/test_corpus_topics.py` asserts each of the 10 topics maps to ≥ 1 document; the map is published in `corpus/README.md` (spec §5.3 places it there and only there — `scripts/corpus_stats.py` generates the corpus-SIZE sentence and the per-format table into `README.md` and `design-and-evaluation.md`, never the topic map, and §15.1's `test_docs_completeness` assertion list for `design-and-evaluation.md` contains no topic-map assertion) | planned |
| **PD.3** | Small mock structured datasets, clearly synthetic, no real private data | §5.4 (6 committed JSON files, `_synthetic` banner, **24 non-contiguous ids from `E1001`–`E1199` all matching `^E1[0-9]{3}$`, with the fixed anchors `E1002`, `E1007`, `E1042`, `E1108`**, `.example` emails, 555 phones, **no SSN field exists**) | **P3** | `tests/unit/test_mock_schemas.py` validates every file against `mock_data/schemas/*.schema.json`; `python scripts/pii_check.py` fails the build on SSN shapes, non-`.example` addresses or non-555 E.164 numbers; `tests/unit/test_mock_anchor_ids.py` asserts `{E1002, E1007, E1042, E1108} ⊆ employees`, that all 24 ids are unique and match `^E1[0-9]{3}$`, that `E1042`'s balance computes to `13.5` at the frozen clock, and that **`E1108` is `eligible: false` with `waiting_period_ends == "2026-11-13"` — a date strictly after `clock.now()`**, since a waiting period that ended before the frozen now would make the fixture arithmetically self-contradictory (spec §5.4); `mock_data/README.md` carries the SYNTHETIC DATA banner | planned |
| **PD.4** | Use free / zero-cost model and embedding options; keep the corpus and datasets small enough for modest free-tier resources | §3 rows 6, 7, 8, 15; §14.1 `## Cost`; §14.3 memory budget | **P4, P11, P12** | `deployed.md`'s `## Cost` section names each provider, model id and free tier with the date observed; `.env.example` shows no paid-only dependency; the measured index size and `rss_mb` (< 420 MB gate, §15.1 step 19) are recorded; `test_docs_completeness.py` asserts the `## Cost` heading | planned |

---

## 1. Environment and Reproducibility

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R1.1** | Create a virtual environment | §3 rows 1–2 (uv + `python3.12 -m venv`), §15.1 job `fresh-clone` | **P0** (built), **P11** (CI job) | README's `## Setup` names `python3.12 -m venv .venv` explicitly; the `fresh-clone` CI job runs the README commands **verbatim** on a clean `ubuntu-latest` runner and must pass | planned |
| **R1.2** | List dependencies in a manifest with pinned versions | §3 row 2, §4 (`pyproject.toml` authoritative, `requirements.txt` compiled and committed, `requirements-dev.txt` never in the image) | **P0** | CI step 1 installs **only** from the committed manifests (`uv pip sync requirements.txt requirements-dev.txt`); no ad-hoc installs anywhere; the Dockerfile installs from `requirements.txt` alone | planned |
| **R1.3** | README.md with setup, local run, deployment and evaluation instructions | §15.1 `test_docs_completeness` (README block) | **P0** (stub), **P12** (final) | `tests/contract/test_docs_completeness.py` asserts the headings `## Setup`, `## Local Run`, `## Deployment`, `## Evaluation`, `## Third-party components` and the three first-20-lines link lines; the `fresh-clone` job executes the Setup/Local Run commands verbatim | planned |
| **R1.4** | Fixed seeds where applicable — deterministic chunking, evaluation sampling | §6.3 (chunker is a pure function; no seed needed), §13.6 (`SEED = 1729`; the clock is part of the determinism contract) | **P4** (chunking), **P10** (sampling) | `tests/unit/test_chunking_deterministic.py` re-runs the chunker and asserts the manifest is **byte-identical** to the committed file; `tests/unit/test_eval_ordering_deterministic.py` builds the item ordering **and** the 8-item reference subset twice with varied `PYTHONHASHSEED` and asserts element-for-element identity. `SEED = 1729` is a **module constant in `core/ids.py`, not an environment variable** — it has no `Settings` field and no `.env.example` entry (§12.3), because a documented `SEED` that nothing read would break this claim in the one direction a user could exercise. `tests/unit/test_clock_split.py` (P1) pins the other half of the contract: `clock.now()` is frozen by `NOW_OVERRIDE` while `clock.wall_ms()` is not | planned |
| **R1.5** | Secrets read from environment variables, never committed | §12.3 (all secrets via `settings.py`), §10.5 (`redact()`), §17 row 1 | **P0** | CI step 3 runs `gitleaks` over **full history**; `.env` is gitignored and `.env.example` carries no real values; `tests/contract/test_env_example_covers_settings.py` asserts the `.env.example` ↔ `Settings` bijection in **both** directions (and §12.3 now carries **no field that no code reads** — the former `OTEL_EXPORTER_OTLP_ENDPOINT` was dropped rather than shipped as a documented no-op, since the bijection test cannot catch one); `tests/unit/test_g7_redact.py` proves leaked values are scrubbed from every payload | planned |

---

## 2. Corpus Ingestion and Indexing

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R2.1** | Parse and clean policy documents, ≥ 2 source formats | §6.2 (four parser paths: md, HTML, PDF, TXT), §6.1 (per-format ingest report) | **P4** | One unit fixture per format under `tests/fixtures/corpus_mini/` asserting extracted text and heading structure; the PDF path asserts the extracted heading set **equals** `workplace-conduct.src.md`'s; `tests/unit/test_ingest_report.py` asserts `data/index/ingest_report.json` carries all four formats with non-zero counts matching the table row counts | planned |
| **R2.2** | Chunk documents using a justified strategy | §6.3 (heading-aware; 1400 max / 1100 window / 150 overlap / 120 min; `chunk_id = "c_" + sha256(...)[:16]`) | **P4** | `tests/unit/test_chunking.py` — **a named P4 deliverable and a named P4 gate command** — asserts heading-path propagation and overlap behaviour on fixtures; the justification is a required `### Chunking strategy` subsection asserted by `test_docs_completeness.py`; sizes are env-overridable and swept in the zero-LLM chunk-size comparison | planned |
| **R2.3** | Embed chunks with a free embedding model / local model / free-tier API | §6.4 (fastembed 0.8.0 + `BAAI/bge-small-en-v1.5`, 384-dim ONNX, no torch, no key) | **P4** | `index_meta.embed_model` and `.dim` asserted at 384; `tests/unit/test_query_embed_is_asymmetric.py` (**the P4 acceptance gate**) proves the query-side transform exists and records the branch taken in `CHANGELOG.md`; the **five** architecture tests of §6.4 pin the single `.embed(` / `.query_embed(` / `.passage_embed(` call site, `cache_dir=` **and** `threads=1` on the `TextEmbedding(...)` construction, **`batch_size=EMBED_BATCH_SIZE` on every call at that site with `EMBED_BATCH_SIZE == 8`** (an AST assertion plus a literal value check — the default batch peaked at 1477 MB RSS, §20 R-3), the absence of `parallel=` anywhere under `src/`, and no event-loop call without `asyncio.to_thread`; the key-free CI fallback `EMBED_PROVIDER=fake` has a named home and a test — `rag/embed.py::_fake_embed()` + `EMBED_MODEL_EFFECTIVE = "fake-hash-384"`, covered by `tests/unit/test_fake_embedder.py` (determinism across two interpreter runs, unit norm, and `ingest --verify-manifest` exiting 2 under it) — §6.4, §12.3 | planned |
| **R2.4** | Store embedded chunks in a local / lightweight vector store | §6.5 (`sqlite-vec` `vec0` with `distance_metric=cosine` + FTS5 in one read-only file), §6.5 NumPy fallback | **P4** | `python -m hrmosaic.rag.index --selftest` asserts a known query returns the expected `doc_id`; `/health.index` reports `chunk_count > 0`, `doc_count`, `vector_backend`; the `docker` CI job probes `enable_load_extension` + `vec_version()` inside `python:3.12-slim`; **`tests/unit/test_vector_backend_parity.py` is an UNCONDITIONAL P4 gate** — both backends ship in every build behind `rag/vecbackend.py::knn()`, and `ingest.py` always writes `data/index/vectors.f32`, so the fallback is parity-tested before it is ever needed rather than first exercised in production (§6.5). `open_index()`'s mismatch guard compares `embed_model`/`dim` against settings and `distance_metric`/`query_convention`/`vector_backend` against the module constants `EXPECTED_DISTANCE_METRIC`, `QUERY_CONVENTION` and the `available_vector_backend()` probe — §12.3 forbids a `Settings` field no code reads | planned |
| **R2.5** | Persist enough metadata for citations — title/ID, section, snippet | §6.5 (`chunks` table: `doc_id`, `doc_title`, `heading_path`, `section`, `snippet`, `char_start`, `char_end`) | **P4** | `tests/unit/test_chunk_citation_fields.py` asserts **every** stored chunk has all seven citation fields non-empty | planned |

---

## 3. RAG

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R3.1** | Top-k retrieval with optional filtering, query rewriting or reranking | §7.1 (hybrid dense k=20 + BM25 k=20 → RRF k₀=60 → top-k=5; `doc_ids`/`topic` filters; `min_dense_score` filter-then-truncate; score-fill from **stored** vectors). **The requirement's enhancements are optional and this design takes FILTERING**: query rewriting is explicitly rejected in §3 row 10 (retrieval lives inside the MCP server, `agent/**` may not import it, the §9.1 router response carries no follow-up flag and tool 1's schema no rewrite parameter — it had no implementable channel) | **P4** | `tests/unit/test_retrieval_filters.py` asserts (a) exact `k` — `len(hits) == k` whenever ≥ k fused candidates clear `min_dense_score` (filter first, truncate second), (b) the `doc_ids`/`topic` filters applied to **both** arms *before* fusion, verified against the pre-fusion candidate sets, (c) the score-fill path reads **stored** vectors — a BM25-only candidate emerges with a non-null `dense_score` while **exactly one** query embed occurs for the whole retrieval; `tests/unit/test_min_dense_score_is_not_rrf.py` is the whole-system-returns-zero regression test; the tuned-`k` claim is measured by the `dense_only_k2` ablation arm | planned |
| **R3.2** | Prompting strategy that injects retrieved chunks and source metadata | §7.2 (`agent/prompts/synthesize.j2`, frozen prefix ordering, `<document …>` envelopes carrying `chunk_id`/`doc_id`/`title`/`section`/`score`) | **P7** | `tests/contract/test_prompt_golden.py` snapshots the assembled prompt for a fixed query; the verbatim `messages[]` are stored in `llm_messages` and rendered on dashboard page 3's "exact messages sent" drill-down for **every** turn | planned |
| **R3.3** | Answers cite document IDs / titles / sections with supporting snippets | §7.3 (`Citation` model: `chunk_id, doc_id, doc_title, heading_path, section, snippet, score, quarantined, source_url`), §11.1 | **P7, P8** | `tests/contract/test_chat_contract.py` asserts a top-level `citations[]` with snippets for both a RAG-only and a tool-using query; the eval's `cit_resolve_pre`/`_post` verify each cited id exists and its snippet is a substring of the real chunk; the UI renders citation chips deep-linking into the corpus browser | planned |
| **R3.4** | Guardrails: refuse/redirect out-of-corpus questions, limit unsupported claims, distinguish policy facts from recommendations | §7.4 (G1 evidence gate, G2 citation resolvability, G3 fact-vs-recommendation, G4 injection shield, G5 sensitive escalation, G6 identity scope, G7 redaction) | **P1, P5, P7** (the guardrail suite is genuinely split: `core/redact.py` and the **G7** redaction tests are **P1** deliverables — Appendix A, roadmap §6.1 — **G6** is split by §7.4 into `test_g6_identity_server.py` at **P5** and `test_g6_identity.py` at **P7**, and G1–G5 land at **P7**) | Seven named unit suites (`test_g1_evidence_gate.py` … `test_g7_redact.py`) plus `test_g4_no_false_positives.py` over the whole committed manifest; every decision emits a `guardrail` span browsable on dashboard page 8; the `out_of_scope` eval items assert refusal, the one `sensitive` item asserts escalation (**G5**'s only eval evidence, §13.1), and `inj-001` — a `simple_policy` item, so that it actually retrieves — asserts the canary chunk is quarantined and uncited (**G4**'s only eval evidence) | planned |
| **R3.5** | At least one complex question requiring retrieval from multiple policy documents | §13.1 (5 `multi_doc` items; ≥ 3 of them require ≥ 3 distinct documents), §18.1 (demo task 1) | **P7, P10** (`tests/e2e/test_demo_tasks.py`, cited below, is a **P7** deliverable; `dataset.yaml` and `test_dataset.py` are P10's) | `tests/unit/test_dataset.py` asserts **both levels**: the R3.5 floor — ≥ 1 `multi_doc` item with ≥ 2 distinct `expected_docs` — **and the design claim itself**, `len([i for i in items if i.category == "multi_doc" and len(set(i.expected_docs)) >= 3]) >= 3`, so the corpus/dataset cannot drift to two-document questions while every cited test stays green (§13.1); `tests/e2e/test_demo_tasks.py` asserts task 1's citations span **≥ 3 distinct documents**; `DocRecall` is reported per item | planned |

---

## 4. Agentic System Design

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R4.1** | Orchestrator interprets intent, decides whether RAG alone suffices, selects tools, calls MCP tools, synthesizes | §9.1 (route → act loop → synthesize, with the **named entry points `agent/orchestrator.py::run_turn(req)` and `resume_turn(session_id, turn_id, action_digest, confirm_token)`** that `web/api.py` calls at P8, and the rule that `resume_turn` rehydrates the turn's buffered state from its persisted spans), §9.2 (the router **gates** the catalog to tools 1–4 on `policy_qa`) | **P7** | `tests/e2e/test_rag_only_makes_no_people_calls.py` asserts **zero** non-RAG tool calls on a pure policy question; the decision is visible as the `plan` span's `intent`; a `catalog_reopened` recovery path is reported as its own rate | planned |
| **R4.2** | Support at least two multi-step HR workflows | §9.3 (`remote_work_eligibility`, `pto_request` — declarative slots + completion predicates that **require** a structured-data tool result) | **P7** | `tests/e2e/test_demo_tasks.py` runs each end to end asserting the documented tool sequence — `required_tools` + `precedence_edges` + `forbidden_tools` on §18's two `DemoExpectation` records — and a completed answer (**P7** through `awaiting_confirmation`, **P8** through the confirm→write path); both are mirrored by eval items `remote-004` and `pto-003`, and `workflow_completion_by_workflow{}` is computed from them and rendered on dashboard page 11 | planned |
| **R4.3** | Logged trace of agent reasoning at the architectural level; no hidden chain-of-thought | §9.7, §10.2 (span payload union), §11.1 (the R4.3 six-element mapping table) | **P1, P7, P8** | `tests/contract/test_chat_trace_projection.py` asserts `trace[]` equals the set of that turn's spans **and** asserts §11.1's **seven-row** mapping table row by row — the six elements R4.3 enumerates literally (selected tools; tool arguments; tool outputs; retrieved policy sources; **final answer basis** → the turn's `answer_blocks[].citations` + top-level `citations[]` in the response body, plus the synthesize `llm_call`'s `messages_ref` and `response_text`; **any escalation decision** → the `guardrail` entry with `rule_id = G5, verdict = escalate` together with `turns.outcome == "escalated"` and the `escalation` block) plus one clearly-labelled **R4.1** row (the decision to use RAG or tools → the `plan` span's `intent`); `tests/contract/test_no_cot.py` asserts no response body or persisted payload contains a `reasoning|thought|cot|thinking|scratchpad` key and that provider reasoning is dropped at the adapter boundary | planned |
| **R4.4** | Handle failures gracefully — unavailable MCP tools, missing employee IDs, incomplete evidence, ambiguous requests | §9.5 (four named paths, each a single file built up across two phases), §8.3 (error semantics) | **P7, P8** | Four fault-injection files — `tests/integration/test_fault_{mcp_down,unknown_employee,empty_retrieval,ambiguous}.py`. **P7** ships the orchestrator-level half of each (drive `agent.orchestrator.run_turn()`, assert the returned `ChatResponse` and the logged decision span; the MCP-down case points `agent.client` at a **dead port** and asserts one re-discover attempt — no HTTP, because `POST /chat` is a P8 deliverable); **P8** extends the same four files with the **HTTP 200** + helpful-answer assertions. `tests/integration/test_health_mcp_down.py` is a **separate P8 file** using §11.4's two-uvicorn mechanism for the `/health` flip — it is not an alias of `test_fault_mcp_down` | planned |
| **R4.5** | Prevent irreversible actions — mock actions or explicit user confirmation | §8.5 (both write tools are mock), §8.6 (HMAC `confirm_token` minted **only** in `web/` after a human click, verified **inside** the MCP server), §13.4 | **P5, P8** | `tests/unit/test_action_safety_gate.py` is a **build-blocking CI gate at 1.0** — implementable because `action_digest` has **one** definition, `sha256(canonical_json({"tool", "args" minus `confirm_token`}))[:16]` in `mcpserver/confirm.py`, imported by both the tool handlers and `web/api.py` and pinned by `tests/unit/test_action_digest_stable.py` (§8.6); without a single formula P5 and P8 produce two and the gate never reaches 1.0; `tests/unit/test_confirm_token_never_leaked.py` asserts no mintable token appears in any response body or persisted payload; `tests/architecture/test_mint_sole_caller.py` (the §15.1 step-5 / §16.1 assertion that `confirm.mint` is referenced from exactly one module) is **authored at P8, not P5** — `web/api.py` is its only legal caller, so writing it at P5 would redden the push path from P5 through P7; `tests/e2e/test_demo_tasks.py` asserts no write occurs without a confirm token — **P7 stops task 2 at `outcome == "awaiting_confirmation"` with no `mock_writes` row, and P8 extends the same file through the `POST /chat/confirm` → resumed-write path, since minting a `confirm_token` is legal only from `web/api.py`** (§8.6, §9.5); the pass rate is reported on dashboard page 11 badged as a CI gate | planned |

---

## 5. MCP Server and Tool Integration

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R5.1** | One or more MCP servers exposing tools over a supported transport | §8.1 (one `build_hr_server()` factory; **three** transports: mounted Streamable HTTP, stdio, remote via `MCP_SERVER_URL`) | **P5** | `tests/contract/test_mcp_api_shape.py` asserts the mcp 2.2.0 API surface; `tests/integration/test_mcp_discovery.py` completes a real `initialize` handshake over stdio and asserts the protocol version and `server_info.name == "mosaic-hr"`; the mounted HTTP path is exercised by `test_mcp_tool_call` | planned |
| **R5.2** | Expose ≥ 5 tools; ≥ 1 using the RAG index and ≥ 1 using mock data / a mock operation | §8.4 (9 tools: 1–4 RAG, 5–7 mock data, 8–9 gated mock writes) | **P5** | `tools/list` returns 9; CI step 10 asserts **≥ 5** with a non-empty `input_schema` and description each; `test_mcp_tool_call` (§16.3, CI step 11) asserts **both halves of R5.2 over both transports**: `check_pto_balance(E1042)` returns `remaining_days == 13.5` (the mock-data half; `E1042` is one of §5.4's four fixed anchor ids, guaranteed present by `tests/unit/test_mock_anchor_ids.py` at P3) **and `search_policy_documents` returns a non-empty `hits[]` whose `chunk_id`s all resolve via `core.corpusread.get_chunk`** (the RAG-index half — spec §16.3 names it explicitly so this cell rests on an assertion rather than on Appendix A's generic "a call on both") | planned |
| **R5.3** | Tools should include the eight enumerated names | §8.4 (`REQUIRED_TOOL_NAMES` frozenset; `list_policy_documents` is a deliberate extra) | **P5** | `tests/contract/test_tools_match_spec.py` asserts `REQUIRED_TOOL_NAMES ⊆ {t.name}` against a **live** `tools/list`, that each has a non-empty `description`, and that each `input_schema` is a JSON Schema object with `type` and `properties` | planned |
| **R5.4** | The agent must actually call MCP-exposed tools; hard-coded direct calls are insufficient | §2.1 bet 1 (the MCP server is mounted on the app that consumes it; real JSON-RPC over real loopback HTTP), §4.2 (`agent/**` may not import `hrmosaic.mcpserver` nor `hrmosaic.rag.*`) | **P5, P7** | `tests/architecture/test_import_boundaries.py` (AST) proves the orchestrator cannot reach tool implementations directly; a per-turn `mcp_discovery` span plus one `tool_call` span per call are persisted and rendered on dashboard pages 3, 5 and 9; the model's function array **is** the `tools/list` conversion, never a hard-coded list | planned |
| **R5.5** | Document the MCP architecture, transport choice, tool schemas, and the discovery flow | §8.1, §8.2 (discovery flow), §8.7 (`_meta` conventions), §8.8 (generated schemas), §15.1 step 18 | **P5, P12** | `mcp/README.md` carries the transport rationale, the 1.x→2.x mapping and the `_meta`/`_trace` conventions; `scripts/gen_tool_schemas.py` regenerates `mcp/tools/*.schema.json` **and** the `## Tool schemas` section of `design-and-evaluation.md` from the live server, and `docs-check` fails on any diff. It is **idempotent and file-tolerant**: the schemas are always written, the doc's generated block is rewritten **only if `design-and-evaluation.md` exists** (it is a P12 deliverable, while the script is a P5 gate) and it otherwise prints `skipped: design-and-evaluation.md not present` — so P5's own gate neither crashes nor creates a graded document seven phases early (§8.8) | planned |

---

## 6. Web Application

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R6.1** | Use a web framework (Flask, FastAPI, Streamlit, Node/Express, Next.js, …) | §3 row 3 (FastAPI 0.141.1 + uvicorn 0.52.4, `--workers 1`) | **P8** | Named in README and present in `requirements.txt`; **`tests/contract/test_app_starts.py` — a named P8 contract deliverable and a named P8 gate command; it must live under `tests/contract/` for `make test-smoke`'s `-m smoke` selection to reach it (§4)** — boots via the documented command (with `EMBED_WARMUP=0`) and polls **`/health`** for 200 with `mcp.connected=true` and `index.loaded=true` (§15.1 step 9); **`tests/contract/test_chat_page_renders.py` is what asserts `GET /` returns 200** (§11.5) — the two assertions belong to different tests, and adding a `/` fetch to the model-free app-start step would misplace it | planned |
| **R6.2** | Chat interface where users ask HR policy and workflow questions | §11.5 (persona picker, typed answer blocks, citation chips, live span rail, confirm card, cold-start banner, two demo buttons) | **P8** | `tests/contract/test_chat_page_renders.py` asserts `GET /` contains the persona `<select>`, both demo buttons and the span-rail container, then posts a tool-using message and asserts a `policy_fact` badge, a `recommendation` badge and **≥ 1 citation chip whose `href` is the chunk's `source_url`** | planned |
| **R6.3** | `/chat` endpoint returning the final answer, citations, snippets and a concise tool-call trace | §11.1 (all four fields top-level; `trace[]` is the projection of **all** spans of the turn) | **P8** | `tests/contract/test_chat_contract.py` asserts `answer`, `answer_blocks`, `citations[]` (with `snippet`) and `trace[]` are present and schema-valid for **both** a RAG-only and a tool-using query; `test_chat_trace_projection.py` asserts trace ≡ spans-of-turn | planned |
| **R6.4** | `/health` endpoint returning JSON status including MCP connectivity | §11.4 (always 200; `app`, `mcp`, `index`, `llm`, `trace_store`, `degradations`) | **P8** | `tests/contract/test_health.py` asserts the field set **and the FOUR-string `degradations[]` vocabulary — `llm_api_key_missing`, `index_model_mismatch`, `mcp_disconnected`, `trace_store_unreachable` — so the `mcp.connected == false` and `trace_store.reachable == false` cases each have a machine-readable reason rather than an undefined one (§11.4)**; `tests/integration/test_health_mcp_down.py` kills a second uvicorn reached through `MCP_SERVER_URL` and asserts `mcp.connected` flips to `false`, `status` to `"degraded"`, and the endpoint still returns **200** | planned |
| **R6.5** | A way for the grader to reproduce ≥ 2 agentic demo tasks from the UI or an API client | §11.5, §18 (two one-click buttons **and** two curl scripts) | **P8, P11** | `scripts/demo_task_1.sh` and `scripts/demo_task_2.sh` are parameterised by `BASE_URL`, documented in README, and run against the deployed URL in the P11/P12 gates; `tests/e2e/test_demo_tasks.py` asserts each produces the documented trace and citations | planned |

---

## 7. Deployment

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R7.1** | Deploy to Render / Railway / equivalent free-tier host at a shareable URL | §14.1 (Render Hobby free, `runtime: docker`, `render.yaml` committed), §14.6 (unattended provisioning) | **P11** | `deployed.md`'s `## Deployed URLs` carries the app and `/health` lines (host-agnostic, asserted by `test_docs_completeness.py`); `scripts/smoke_deployed.py` curls the live URL and `/health` from the CI runner and asserts 200 | planned |
| **R7.2** | Single-service deployment is acceptable | §14.1, §2 (one process, one container: UI, orchestrator, MCP client, mounted MCP server, index, mock data) | **P11** | `render.yaml` shows one `type: web` service; `/health.mcp.connected == true` on the deployed instance proves the in-process MCP server is reachable; the topology is stated in `design-and-evaluation.md`'s `## Deployment choices` | planned |
| **R7.3** | If MCP is a separate service, configure its URL via env vars | §8.1 (remote mode), §12.3 (`MCP_SERVER_URL` computed from the resolved `PORT`; `mcp_transport_effective`) | **P8, P11** | `tests/integration/test_mcp_remote_url.py` runs the agent against a **second local uvicorn** on another port and asserts `sessions.mcp_transport == "remote"`; `deployed.md`'s `## MCP transport` states verbatim that it was **NOT deployed as a separate service** and why, asserted by `test_docs_completeness.py` | planned |
| **R7.4** | No paid database; committed data / SQLite / a small local vector store / a free-tier host | §12.1 (SqliteStore ‖ Turso free tier), §14.1 `## Cost` | **P11** | No paid DB appears in `render.yaml` or `.env.example`; storage is the read-only SQLite index + committed JSON + Turso free; the Docker build log shows the index built at build time; `deployed.md`'s `## Cost` documents **$0** with the dates observed, **plus both Render free-tier budgets — 750 instance-hours/workspace/month and 500 build-pipeline minutes/month — and the measured wall-clock of one build**, so the remaining headroom is arithmetic; `scripts/check_render_hours.py` warns above 600 h **and above 400 build minutes** (§14.1, R-10) | planned |
| **R7.5** | Explain expected cold-start behaviour if the service spins down | §14.4 (segment table), §13.5 (cold vs warm distributions, `cold_probe` protocol) | **P11, P12** | `deployed.md`'s `## Cold start` carries a measured number and the ISO date it was measured (asserted by `test_docs_completeness.py`); `scripts/measure_cold_start.py` produces it; the eval report and dashboard page 13 split cold/warm with `n_cold`/`n_warm`; the demo script narrates it | planned |

---

## 8. CI/CD

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R8.1** | GitHub Actions workflow running on push or pull request | §15.1 (`on: push` (all branches) `+ pull_request + workflow_dispatch`) | **P0** | `gh run list` shows successful runs triggered by **both** event types; `.github/workflows/ci.yml` is committed from P0 onward so every phase inherits it | planned |
| **R8.2** | Workflow installs deps, runs a build/start/import check, and executes tests before deploying | §15.1 (job `test`, **21 ordered steps** numbered `1, 1b, 1c, 2–19` — the ids are deliberately non-contiguous, so `1 + 1b + 1c + eighteen of 2–19 = 21`, matching the spec's step table row count; jobs `docker`, `fresh-clone`, `deploy`) | **P0, P11** | The green run log shows install (step 1) → model cache (1b) → **full-corpus index provisioning (1c, before the unit step, since several `tests/unit` tests require the real index — §6.5)** → lint/secret scan → architecture → unit → app-start (step 9) → MCP (10–12) → contract/integration/e2e → docs → RSS gate, then `docker`, then `deploy`; `deploy` declares `needs: [test, docker]`. **Ownership of each step is assigned in roadmap §6.1's step → phase table and enforced by the standing subagent brief (§2.2) and the §4 per-phase closing checklist**, so no phase's tests can pass a local gate while CI silently omits them | planned |
| **R8.3** | ≥ 1 test verifying the app starts, and ≥ 1 verifying MCP discovery or a simple tool call | §15.1 steps 9–11, §16.3 | **P5, P8** | `tests/contract/test_app_starts.py` polls `/health` for 200 with `mcp.connected=true` and `index.loaded=true`; `tests/integration/test_mcp_discovery.py::test_mcp_tool_discovery` asserts `tools/list` returns **≥ 5** tools each with a non-empty `input_schema`; `test_mcp_tool_call` asserts a valid `tools/call` result on both transports | planned |
| **R8.4** | Deployment occurs only if tests pass | §14.5 (two independent mechanisms: `needs:` + Render Auto-Deploy Off), §15.1 job `deploy` | **P11** | The `deploy` job declares `needs: [test, docker]`; branch protection makes `test` a **required status check** on `main`; the evidence pair is a failing PR to `main` (unmergeable) **and** a `workflow_dispatch` run on `main` with `FORCE_TEST_FAILURE=1` whose job graph shows the deploy job skipped for **"dependent job failed"** — captured as `docs/evidence/ci-deploy-skipped.png` | planned |

---

## 9. Evaluation

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R9.1** | 20–30 questions/tasks across policy Q&A and agentic workflows, all five categories, with gold answers | §13.1 (**26** items: 7 `simple_policy`, 5 `multi_doc`, 6 `tool_task`, 3 `ambiguous`, 3 `out_of_scope`, 1 `unsafe_action`, **1 `sensitive`**; absolute dates only) | **P10** | `tests/unit/test_dataset.py` asserts `n == 26` (and the `20 ≤ n ≤ 30` band), **all seven** category labels present with ≥ 1 item, the per-label counts, **that every one of the five `expected_behavior` classes — `answer`, `clarify`, `confirm`, `refuse`, `escalate` — has ≥ 1 item** (the `sensitive` item is the only source of `escalate`, without which the 5×5 matrix ships with an empty gold row), **that `inj-001` is a `simple_policy` item carrying `security-acceptable-use` in `expected_docs`** (as an `out_of_scope` item it made zero tool calls by design, so nothing could ever be quarantined), **that every `tool_task` item's `expected_end_state` carries a non-empty `requires_tool_results`**, a non-empty gold per item, that every `gold_facts` key resolves in `corpus/_facts.yml`, and that **no** question contains a relative date expression | planned |
| **R9.2** | Answer-quality metrics: groundedness, citation accuracy, optional match | §13.3 (claim-level groundedness; `cit_resolve_pre`/`_post`; `CitPrecision`/`CitRecall`/`F1`; `DocRecall`; exact/partial match) | **P10** | The runner emits every metric per item and in aggregate into `evaluation/results/<run_id>.deterministic.json`; `tests/unit/test_scorer_edge_cases.py` covers every empty-denominator case; dashboard page 11 renders the headline strip; the answer-quality **methodology is a required HAND-AUTHORED section of `design-and-evaluation.md` whose heading `test_docs_completeness.py` asserts** (P12), while the **questions/expected-answers (8a) and results (8b) sections are generated by `scripts/gen_eval_docs.py` and diffed by `docs-check`** — those two are the only sections that script emits (§13.10, §15.1) | planned |
| **R9.3** | Agent-behaviour metrics: tool selection, workflow completion, escalation/clarification accuracy, action-safety pass rate | §13.4 (all four; **deterministic except the ambiguous-item clarification sub-check, which is judged and computed on `baseline` only — §13.9**); the outcome→class mapping table; `gated_attempts` | **P10** | The report JSON carries `tool_selection_accuracy`, `workflow_completion_by_workflow{}`, `escalation_matrix[5][5]` + `over_refusal_rate` + `missed_refusal_rate`, `action_safety_pass_rate` and the judged `clarification_accuracy` (`float \| None`, §11.6); `tests/contract/test_dashboard_viewmodels.py` asserts all of those keys exist on every run, with `clarification_accuracy` null and `judged: false` on the two ablation arms; **`tests/unit/test_dataset.py` asserts every one of the five `expected_behavior` classes has ≥ 1 item**, so the matrix's `escalate` row and `MissedRefusalRate`'s denominator are non-empty (§13.1); per-item detail is derived from trace records | planned |
| **R9.4** | System metrics: latency p50/p95 over 10–20 tasks, cold vs warm separated | §13.5 (26 eval turns + 3 `cold_probe` re-runs of `pto-001`/`remote-001`/`benefits-001`; cold iff `process_uptime_ms < 60000`) | **P10** (harness + `local` proving run), **P11** (the published `deployed` dispatch) | Percentiles computed from `turns.duration_ms` via `statistics.quantiles`; the published distribution comes **only** from the `deployed`-mode run (`gen_eval_docs.py` asserts `latest.json` names `target: "deployed"`); dashboard page 13 shows p50/p90/p95/p99 split cold/warm with `n_cold`/`n_warm` and the host each was measured on; `tests/unit/test_cold_probe_excluded.py` proves probes never enter a quality mean; and **`tests/unit/test_dataset.py` asserts `pto-001`, `remote-001` and `benefits-001` exist with `category == "simple_policy"`** — the three items §13.5's cold protocol re-runs, without which a rename would silently leave `n_cold = 0` (§13.1, §13.5) | planned |
| **R9.5** | At least one ablation or comparison | §13.9 (`baseline` · `dense_only_k2` · `no_structured_tools`, plus a zero-LLM chunk-size comparison) | **P10, P11** | `evaluation/ablation.py` writes `comparison.json` and **asserts** every compared run shares `config_json.target` and `dataset_sha`, and that `workflow_completion(no_structured_tools) < baseline − 0.25` — exiting non-zero (with an explicit not-supported banner in `REPORT.md`) on a null result. **P10 authors `.github/workflows/eval.yml` and proves the harness with all three variants at `target: local` (determinism evidence only, never promoted to `latest.json`); P11 issues the single `eval.yml` dispatch with `target: deployed` covering all three variants, which is the published `comparison.json`** — `jq -r '.runs[].config_json.target' evaluation/results/comparison.json` must return `deployed` three times (§13.2, §13.9). `scripts/chunk_size_sweep.py` writes `chunk_size_comparison.json`; both render on dashboard page 12. ⚠ **The two sweeps must fall on different calendar days** — P10's `local` trio and P11's `deployed` trio are ~700 provider calls each against a `[medium]` ~1,000 RPD, and the boundary is the normative quota mitigation (§13.9, R-5; P11 step 0 confirms it) | planned |

---

## 10. Design Documentation

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R10.1** | Justify ten design choices (framework, MCP design, transport, tool schemas, embedding model, chunking, retrieval k, vector store, deployment architecture, guardrails) | §15.1 (the ten literal `###` headings), §3 (the decision table with rejected alternatives) | **P12** | `tests/contract/test_docs_completeness.py::test_design_doc_headings` greps `design-and-evaluation.md` for all ten literal `###` headings **and asserts that the body of each one (heading to the next heading of the same or higher level) contains a line matching `^\s*(\*\*)?Rejected(\*\*)?:`** — the rejected-alternative content the rubric rewards is therefore a greppable assertion, not a claim (§15.1) | planned |
| **R10.2** | Architecture diagram showing all seven components | §2 (mermaid `flowchart TB` labelling Web App, Agent Orchestrator, MCP Client, MCP Server, RAG Index, Mock Structured Data, LLM Provider) | **P12** | `test_docs_completeness.py` asserts each of the seven component names appears in the diagram source in `design-and-evaluation.md`; `tests/architecture/test_import_boundaries.py` proves the code layout matches the diagram | planned |
| **R10.3** | Describe the two demo tasks and the expected MCP tool-call sequence for each | §18.1, §18.2 (ordered sequences with abridged arguments), §18's `DemoExpectation` | **P7** (the executable half), **P12** (the document) | Both sequences are documented in `design-and-evaluation.md`'s demo section; `tests/e2e/test_demo_tasks.py` compares the **actual** trace against the documented sequence through two per-task `DemoExpectation` records that carry **`required_tools: list[str]`** (every name must appear as an `ok` `tool_call` span) and **`precedence_edges: list[tuple[str,str]]`** (first-`seq` ordering per pair), plus a hard fail on any `forbidden_tools` occurrence — populated from §18.1/§18.2's tables, so the sequence cannot silently change while the test stays green; `docs-check` asserts **every `required_tools` name** and the task-2 ticket strings from `tests/fixtures/demo/task2_expected.json` occur verbatim in the doc. *(Two artifacts, not one: the expectation records live in code, `tests/fixtures/demo/` holds the JSON payload fixture — §16.4.)* | planned |

---

## Submission Guidelines — demo video

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **DEMO.1** | Recorded screen-share demonstration of the working **deployed** application, with screen capture and voiceover | §18.3 (0:00–0:45 segment: deployed URL visible in the address bar) | **P12** (script) + **G7** (record) | The video link is recorded on `README.md`'s `Demo video:` line (regex-asserted, and asserted **not** to read `TBD-before-submission` at P12); the recording shows the deployed URL in the browser address bar with audible narration | planned |
| **DEMO.2** | Between 7 and 10 minutes long | §18.3 (time-boxed table totalling 9:15), §4 (`docs/demo-script.md` in the repo layout), §15.1 (`test_docs_completeness` asserts it exists with a `## Segment table`) | **P12** + **G7** | `docs/demo-script.md`'s `## Segment table` is time-boxed per section and rehearsed to fit; `pytest tests/contract/test_docs_completeness.py` asserts the file and that heading exist; the final duration is ticked against the `- [ ] DEMO.2` line in **`docs/pre-submission-checklist.md`** | planned |
| **DEMO.3** | All group members speak and are present on camera | §18.3 (standing production note: webcam overlay visible for the **full** 7–10 minutes; continuous narration), §4 (`docs/demo-script.md`) | **P12** + **G7** | The standing production note is a required section of `docs/demo-script.md`; picture-in-picture overlay present in every screen-share segment — a screen-only stretch after 0:45 fails the bullet; verified on playback and ticked against the `- [ ] DEMO.3` line in **`docs/pre-submission-checklist.md`** | planned |
| **DEMO.4** | All group members show their government ID | §18.3 (ID held legibly still for ≥ 3 s at ~0:15), §4 (`docs/pre-submission-checklist.md`) | **G7** | The recording contains the ID segment, framed large enough to read; ticked against the `- [ ] DEMO.4` line in **`docs/pre-submission-checklist.md`**, whose existence and per-id completeness `test_docs_completeness.py` asserts (§15.1) | planned |
| **DEMO.5** | The demo shows the system carrying out two agentic tasks end-to-end | §18.1, §18.2, §18.3 (1:30–3:30 and 3:30–5:30), §4 (`docs/pre-submission-checklist.md`) | **G7** | Both tasks executed live against the deployed URL from question to final cited answer/action via the one-click buttons, with no manual intervention; ticked against the `- [ ] DEMO.5` line in **`docs/pre-submission-checklist.md`**. (`tests/e2e/test_demo_tasks.py` independently proves the sequences under the stub) | planned |
| **DEMO.6** | For each task, explain tool names, arguments, outputs, retrieved citations and the final answer/action | §18.1 ①–⑤, §18.2, §11.3 (the SSE span rail is the narration surface), §4 + §15.1 (`docs/demo-script.md` carries a **per-task five-element sub-checklist**, asserted to exist) | **G7** | The five-element sub-checklist is ticked **per task** in `docs/demo-script.md`, and the `- [ ] DEMO.6` line in `docs/pre-submission-checklist.md` records that both were completed; the live span rail shows tool names and durations, spans expand to arguments and results, a citation chip jumps into the corpus browser, and `dashboard_url` opens the same turn's full waterfall | planned |
| **DEMO.7** | Quick walkthrough of design, deployment, CI/CD and evaluation results | §18.3 (0:45–1:30, 6:15–7:00, 7:00–7:40, 7:40–8:45), §4 (`docs/pre-submission-checklist.md`) | **G7** | Ticked against the `- [ ] DEMO.7` line in **`docs/pre-submission-checklist.md`**. Four dedicated segments: the mermaid diagram; `render.yaml` + the `/health` payload + cold-start numbers; a green CI run plus the recorded red run with the deploy job skipped; the eval dashboard with run detail, judge rationale and the ablation chart | planned |

---

## Submission Guidelines — submission

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **SUB.1** | Submit two links (presentation + GitHub repo) via the dashboard "Submit Project" button | §19.1 item 7 (both links pre-staged at the top of `README.md`), §4 (`docs/pre-submission-checklist.md`) | **G7** | Both link lines are present in `README.md`'s first 20 lines and asserted by `test_docs_completeness.py`, which also asserts **neither still reads `TBD-before-submission`** at P12; the user confirms submission through the Quantic dashboard and ticks the `- [ ] SUB.1` line in **`docs/pre-submission-checklist.md`** | planned |
| **SUB.2** | Include the deployed application URL in the repository README | §15.1 (host-agnostic `Deployed:` regex in the first 20 lines, deliberately **not** `onrender\.com`, and **accepting the literal `TBD-before-submission` exactly as the `Demo video:` and `Repo:` regexes do** — an `https://`-only form would fail P0's own `test_readme_link_lines` on its first run and keep the deploy-gating push path red from P0 through P11) | **P0** (placeholder) → **P11/P12** (real) | `test_docs_completeness.py` asserts the line matches; the placeholder is retired by the P12 pre-submission checklist item and `grep -c 'TBD-before-submission' README.md` == 0; liveness is checked separately and **only** in the `deploy` job by `scripts/smoke_deployed.py`, where a live target genuinely exists | planned |
| **SUB.3** | Share the GitHub repository with the account `quantic-grader` | §19.1 item 5 (**the repo is ALREADY public — verified 2026-09-08; no visibility change is ever made by script**), §4 (`docs/pre-submission-checklist.md`) | **G1** (repo verified public at P0) + **G7** (invite accepted) | At **P12** Claude Code runs `gh api -X PUT repos/seantmalone/quantic-mosaic/collaborators/quantic-grader` and reads back `…/collaborators/quantic-grader/permission`; the grader's acceptance is ticked against the `- [ ] SUB.3` line in **`docs/pre-submission-checklist.md`** | planned |

---

## Submission Guidelines — repository contents

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **DOCS.1** | Repository contains all developed code | §4 (repository layout), USER.5 (commit as work proceeds) | **all phases** | `git status` is clean at every phase boundary; the `fresh-clone` CI job builds and runs from committed files plus env vars only; `git log` shows one commit per phase | planned |
| **DOCS.2** | README.md with introductory description, setup, local run and deployment instructions | §15.1 README assertions | **P0** (stub) → **P12** | `test_docs_completeness.py` asserts the four headings plus the deployed URL line; the `fresh-clone` job runs the documented commands verbatim on a clean runner | planned |
| **DOCS.3** | `design-and-evaluation.md` covering architecture, RAG design, MCP server design, agent orchestration, tool schemas, safety guardrails, deployment choices, and the evaluation questions, expected answers and results | §15.1 (the eight `##` DOCS.3 headings, 8a/8b **generated**) | **P12** | `test_docs_completeness.py` greps for all eight `##` headings plus `## Facts, sources and confidence` and `## Judge validation methodology`; `scripts/gen_eval_docs.py` regenerates 8a from `dataset.yaml` and 8b from `results/latest.json` and `docs-check` **fails on any diff** | planned |
| **DOCS.4** | `ai-tooling.md` naming the AI tools used, how, and what worked / did not | §15.1 (`## What worked well`, `## What did not work`, the DOCS.9 disclosure) | **P12** | `test_docs_completeness.py` asserts both section headings and the disclosure; the content names Claude Code and the Opus subagent workflow used across P0–P12 | planned |
| **DOCS.5** | `deployed.md` with the deployed URL, health endpoint URL, and free-tier cold-start notes | §15.1 (five required headings with asserted content) | **P11** (numbers) → **P12** (final) | `test_docs_completeness.py` asserts `## Deployed URLs` (app + `/health` lines), `## Cold start` (a measured number **and** the ISO date), `## Environment variables`, `## MCP transport` and `## Cost`; both URLs are live-checked by `smoke_deployed.py` in the `deploy` job | planned |
| **DOCS.6** | `evaluation/` with questions, expected answers or rubrics, scripts and reported results | §13.10 (artifact table), §4 | **P10** | The directory contains `dataset.yaml`, `reference_labels.yaml`, the runner/scorers/judges/ablation scripts, the committed split results (**the `.deterministic.json` + `.env.json` + `.items.jsonl` triple per run — all three are imported by `core/archive.py` tier 3, §10.4**), `REPORT.md` and the replay cache; `tests/integration/test_eval_replay_from_cache.py` proves the runner **reproduces** the committed results offline with no key — reading the `run_id` from **`evaluation/results/replay_target.json`** (P10 writes it for its `local` determinism run; P11 repoints it at the published `deployed` run), never from `latest.json`, which §13.2 reserves for the `deployed` `baseline` run and which does not exist until P11 | planned |
| **DOCS.7** | `mock_data/` with synthetic employee, PTO, benefits and/or ticket data | §5.4 (six files; writes go to `mock_writes` in the durable store, never back to JSON) | **P3, P10** (the files are P3's; the cited `tests/e2e/test_mock_data_immutable.py` asserts sha256 stability *after a full eval run*, and `tests/e2e/` does not exist until P7) | `tests/unit/test_mock_schemas.py` validates every file; `scripts/pii_check.py` passes; each file carries the synthetic banner; `tests/e2e/test_mock_data_immutable.py` asserts every file's sha256 is unchanged after a full eval run | planned |
| **DOCS.8** | `mcp/` with the MCP server code and tool definitions | §4.1 (**no `mcp/__init__.py`** — the shadowing hazard resolved empirically), §8.8 | **P5** | The directory holds `server_entrypoint.py`, both launchers, `README.md`, `inspector.md` and the nine generated schemas; `tests/architecture/test_no_mcp_init.py` asserts `mcp/__init__.py` does not exist; `docs-check` asserts the live `tools/list` matches the committed schemas | planned |
| **DOCS.9** | AI-tool use described in `ai-tooling.md`; student responsible for correctness, security and integrity; third-party content licensed and cited | §15.1 (`LICENSES.md` completeness), §17 supply chain | **P12** | `ai-tooling.md` carries the AI-use disclosure and an ownership/responsibility statement; `test_docs_completeness.py` asserts `static/vendor/LICENSES.md`'s **delimited generated block** (`<!-- BEGIN GENERATED: vendored assets -->` … `<!-- END GENERATED -->`, written by `scripts/vendor_assets.py`) **names every file present in `static/vendor/`** with its exact version, sha256 and upstream URL, and that the **hand-authored region above it** carries each licence text — `static/vendor/` holds third-party files only, our own stylesheet being `static/app.css` (§4); `tests/unit/test_vendor_asset_hashes.py` asserts the on-disk sha256 matches | planned |

---

## Rubric level 5

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **RUBRIC5.1** | Outstanding deployed agentic HR system combining policy RAG with correct, cited, grounded responses | §1.2 row 1, §13.3 | **P10, P11** | The single **`deployed`-mode `baseline`** run reports groundedness ≥ 0.92 mean, `cit_resolve_post` = 1.00 over items with ≥ 1 citation, run-level `cit_resolve_pre` ≥ 0.95 and ≥ 0.90 strict-pass; committed as `<run_id>.deterministic.json` + `.env.json`, pointed at by `latest.json`, rendered on dashboard pages 11–12 with corpus deep links | planned |
| **RUBRIC5.2** | MCP integration fully functional — discovery, correct calls, clear traces, graceful error handling | §1.2 row 2, §8.2, §9.5 | **P5, P7, P11** | `mcp_discovery` + `tool_call` spans on every turn; `/dashboard/mcp` renders live discovery with all nine JSON Schemas (captured as `docs/evidence/mcp-discovery-page.png`); four fault-injection tests prove graceful degradation; `tool_selection_accuracy` is reported | planned |
| **RUBRIC5.3** | ≥ 2 end-to-end agentic tasks in the deployed demo, each multi-step with RAG retrieval and structured/mock-data tool use | §1.2 row 3, §18 | **P7, P11, G7** | `tests/e2e/test_demo_tasks.py` carries **two distinct per-task expectation records** (each ≥ 4 tool calls, ≥ 1 retrieval, ≥ 1 structured-data tool, **plus `required_tools` and `precedence_edges` populated from §18.1/§18.2**; task 2 additionally writes behind a confirmation gate); both are executed live in the video and archived in `data/archive/demo_traces.jsonl` | planned |
| **RUBRIC5.4** | Excellent RAG ingestion, indexing, retrieval, citations and guardrails | §1.2 row 4, §6, §7 | **P4, P7, P10** | `test_chunking_deterministic` (byte-identical rebuild), hybrid retrieval with the score-scale and threshold regression tests, seven guardrail suites, citations verified against source offsets, and the `dense_only_k2` + chunk-size comparisons demonstrating tuned `k` and chunking | planned |
| **RUBRIC5.5** | Excellent architecture with clear separation of the six components | §1.2 row 5, §4.2 | **P1–P9** | `tests/architecture/test_import_boundaries.py` (AST) enforces every boundary rule with only the two named exemptions (`core.corpusread`; `web/** → evaluation.{runner,schema}`); the mermaid diagram matches the package layout | planned |
| **RUBRIC5.6** | Free-tier deployment fully functional, env vars and cold-start behaviour documented | §1.2 row 6, §14 | **P11, P12** | `scripts/smoke_deployed.py` passes end to end against the live URL (200, `mcp.connected`, `git_sha != "dev"`, matching `archive_manifest_sha`); every env var appears in `.env.example` **and** `deployed.md` (bijection-tested) **and is read by shipping code** — §12.3 declares no exporter or integration variable that has no implementation, and `SEED` is a `core/ids.py` module constant rather than a documented no-op env var; `render.yaml` sets **`NOW_OVERRIDE=2026-09-08T12:00:00Z`** so the graded instance runs the same frozen `clock.now()` every other surface does, asserted live by `smoke_deployed.py` via `/health.app.clock_frozen` + `clock_now` (§13.6, §14.1); cold-start timings measured and published | planned |
| **RUBRIC5.7** | CI/CD on push/PR with meaningful build/start checks plus an MCP discovery or call test, deploy gated | §1.2 row 7, §15.1, §14.5 | **P0, P11** | Green runs on **both** events containing `test_app_starts` and `test_mcp_tool_discovery`; `deploy` declares `needs: [test, docker]`; the recorded red run's job graph shows the deploy job skipped for "dependent job failed" | planned |
| **RUBRIC5.8** | Excellent evaluation covering groundedness, citation accuracy, tool selection, workflow completion, safety and latency | §1.2 row 8, §13 | **P10, P11** | One committed report and one dashboard view present **all six** metric families with per-item detail, the methodology, the three-variant ablation, the chunk-size comparison and reference-vs-judge Cohen's κ — **the headline figures and the ablation all coming from the single P11 `eval.yml` dispatch at `target: deployed`**, with `gen_eval_docs.py` failing docs-check if `latest.json` ever names a `local` run and `ablation.py` failing if `comparison.json` mixes targets | planned |
| **RUBRIC5.9** | Excellent design documentation and demo presentation of features, architecture, deployment, MCP calls and evaluation, meeting all demo requirements | §1.2 row 9, §15.1, §18.3 | **P12, G7** | `docs-check` green across all R10.* and DOCS.* assertions; the three `docs/evidence/*.png` screenshots exist and are referenced; the video covers all five topics and satisfies every DEMO.* item; the pre-submission checklist is signed off | planned |

---

## User constraints

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **USER.1** | Claude Code with Opus subagents builds end-to-end; the human does only what truly requires a human; design for autonomous buildability and verifiability | §19 (required/optional/automated split), §16.2 (`StubAdapter` makes P0–P9 credential-free), roadmap §5 (seven tracked gates, six reaching a human) | **all phases** | `NEEDS-FROM-USER.md` enumerates the **seven tracked gates with a time estimate each**, of which **G1 is marked *scripted — no user action*** so **only six reach a human**. ⚠ **No visibility change is ever made by script.** The repository was verified **already public** on 2026-09-08 (`gh api repos/seantmalone/quantic-mosaic --jq .private` → `false`), which P0 merely re-asserts; the **only** scripted action under G1 is `gh api -X PUT repos/seantmalone/quantic-mosaic/collaborators/quantic-grader` at **P12**, read back with `…/collaborators/quantic-grader/permission`. This matches spec §3 row 27, §19.1 item 5, §19.3, §21 row 26 and roadmap §4 P0 / §5 G1 exactly — five under 10 minutes, G7 budgeted at 60–90 minutes; every other build/verify step is a scripted command an agent can run; CI reproduces build → index → test → eval with no manual intervention; `CLAUDE.md` pins `model: "opus"` on every delegated call | planned |
| **USER.2** | Observability dashboard with full audit logs for every session: turns, LLM calls (prompts/responses/tokens/latency), retrievals (query/chunks/scores), MCP tool calls (name/args/result/duration/errors), guardrail/escalation/confirmation decisions, and the final answer with citations | §10 (schema + payload union), §11.6 pages 1–10 **plus the four token-gated write controls on pages 8, 9 and 11** (Reset sandbox → page 8; Re-discover now → page 9; Run smoke eval and Run retention → page 11 — the host pages are stated in §11.6 so a selector test can be written), §11.8 (each of the four has an endpoint — `POST /api/eval/runs`, `/api/mcp/rediscover`, `/api/dev/reset-sandbox`, `/api/dev/retention`), §7.2 (`llm_messages` side table), §10.4 (`llm_messages` **and the tier-3 `.deterministic.json` + `.env.json` + `.items.jsonl` TRIPLE** in the archive — without `.items.jsonl` every archived `eval_results` row has null `session_id`/`turn_id`/`latency_ms`/`cold` and the one-click eval-row → trace link is dead on any cold database) | **P1, P9** | `tests/integration/test_audit_completeness.py` posts a chat and queries the dashboard API, asserting the per-turn outcome-conditional **span census** and payload completeness for `llm_call` (incl. retrievable `llm_messages` and integer token counts), `retrieval`, `tool_call`, `mcp_discovery` and `guardrail`, plus a turn record with `final_answer`, `answer_blocks_json` and resolvable `citations_json`; `tests/contract/test_dashboard_pages.py` asserts **all four** write controls are present **on their stated host pages (8, 9, 11, 11)**, wired to their endpoints and rendered disabled-with-tooltip when `DASHBOARD_TOKEN` is unset — §11.6's *"disabled write controls are explained, never dead"* rule — and `tests/integration/test_retention_endpoint.py` covers `POST /api/dev/retention` (401 without the token; per-table `deleted` counts with it; archived and eval-linked sessions untouched); `tests/unit/test_turn_close_records_rss.py` (**P1**) asserts a normally-closed turn carries a non-null `turns.rss_mb_at_end` — `core/trace.py`'s close path is the named writer of that series, so page 13's chart cannot arrive to find the column null for every turn (§10.1, §11.4) | planned |
| **USER.3** | The dashboard must also cover evaluation: runs, per-question results, all six metric families, latency p50/p95 cold vs warm, ablation comparisons | §11.6 pages 11–13, §11.6 page-11 metric panel | **P9, P10** | `tests/contract/test_dashboard_viewmodels.py` asserts the page-11 headline strip and the escalation/safety panel fields exist on every run, that judged metrics are `Optional[float]` accompanied by `judged: bool` and `n_scored{}` (never fabricated zeros); `test_dashboard_pages.py` asserts pages 11, 12 and 13 render committed eval-run data — from **`tests/fixtures/eval_runs/`**, one `<run_id>.{deterministic.json,env.json,items.jsonl}` triple per variant authored at P9 so pages 11–13 do not wait on P10 (§16.4). The judged set is **four**: `groundedness_mean`, `citation_accuracy_mean`, `partial_match_mean` and **`clarification_accuracy`** — the ambiguous-item sub-check of §13.4, which now has a persisted field (`scores_json.clarification`, §10.1), an aggregate, a panel row and an `n_scored` key rather than a judge prompt with nowhere to land | planned |
| **USER.4** | The trace/audit data model is first-class: the same records power `/chat`'s trace, the dashboard, the eval reports and the demo narration | §1.3, §2.1 bet 2, §4.2 (sole-writer AST rule over `src/**` *and* `evaluation/**`) | **P1** (built first) | `tests/architecture/test_sole_span_writer.py` asserts no module outside `core/trace.py` writes to `sessions`/`turns`/`spans`/`llm_messages` — with `core/archive.py` passing as a **caller** of the **eight-entry** replay API (`import_session/turn/span/llm_messages/mock_write/pending_action/**eval_run**/**eval_result**`), not a carve-out; the two other writer-side entry points the rest of the system needs are likewise named rather than inlined — **`trace.reopen_turn(turn_id, awaiting_ms)`** for the confirmation resume (§10.3 step 4, called by `web/api.py`) and **`trace.register_span_listener(fn)`** for the SSE hook (§11.3, registered once by `web/sse.py`, since `core/**` may not import `hrmosaic.web`); `test_chat_trace_projection.py` asserts `trace[]` ≡ spans-of-turn; `test_audit_completeness` (d) asserts the `/chat` trace ids equal the dashboard payload's | planned |
| **USER.5** | Everything is committed to the repo as work proceeds | Roadmap §2.4 (commit pattern), §4 (per-phase commit) | **all phases** | `git log` shows one commit per phase (sub-phases for P9) with the requirement ids in the trailer; `git status` is clean at every phase boundary; no large uncommitted local-only artifact is required to run the system (the index is rebuilt from the committed corpus + manifest) | planned |
| **USER.6** | Target dev environment: macOS, Node 26 / npm 11 (unused), Python 3.14.6 vs the 3.12 pin, Docker, `gh`; no pnpm/bun; no API keys currently set | §3 rows 1–2 (three-place 3.12 pin + a runtime **warning**, never a crash), §1.4 (no Node anywhere in the build), §12.3 (deferred credential validation) | **P0, P8, P11** (the cited `tests/contract/test_missing_key_is_graceful.py` is authored across phases per §12.3's five-surface table: surfaces 1–3 at **P8**, surface 5 at P9 and surface 4 at P10; the 3.12 pin and the `fresh-clone` job are P0/P11) | Setup uses only `uv`/`pip`/`venv`; CI pins the interpreter from `.python-version`; the `fresh-clone` job runs the README commands on a clean runner; `tests/contract/test_missing_key_is_graceful.py` asserts a missing key yields HTTP 200 with `outcome: "configuration_required"` naming `LLM_API_KEY` — never a stack trace or a failed boot | planned |
| **USER.7** | Minimise and prioritise required API keys; prefer free-tier providers or local models | §19.1 (minimum viable set: **one** Gemini key + Render + Turso, all free and card-free), §3 row 8 (local embeddings ⇒ **zero** embedding keys) | **P4, P6, P10** | `NEEDS-FROM-USER.md` states the minimum viable set at the top; `LLM_FALLBACK_*` / `JUDGE_*` provide documented provider fallback; `design-and-evaluation.md` states the free-tier limits per provider **with the date they were read live** at P10 step 0 / P11 step 0 | planned |
| **USER.8** | Verify library/platform facts rather than assuming; ecosystems change as of 2026-09-08 | §3 confidence discipline (`[verified]`/`[medium]`/`[low]`), §20 R-9 | **P1** (Linux RSS, `vec_version()`), **P4, P5, P6, P8** (0.1-CPU turn, model-resident RSS — moved out of P1 by §20 R-1, since neither is measurable without a bootable app), **P10, P11** | `design-and-evaluation.md`'s `## Facts, sources and confidence` table carries claim / source URL / date checked / confidence for every non-verified claim; five live gates convert assumptions into measurements — P1 (Linux RSS, `vec_version()`), P4 (`query_embed` asymmetry), P5 (SDK transport-security grep), P6 (`probe_provider.py`), **P8 (the 0.1-CPU turn wall-clock and the model-resident RSS — spec §20 R-1 moved both here, because a six-tool-call turn needs P5+P6+P7 and a bootable app)**, P10/P11 step 0 (live Gemini/Render/Turso figures, incl. the 500 build-minutes/month allowance) — each recorded with its date in `CHANGELOG.md` or `deployed.md` | planned |

---

## Coverage summary

| Group | Items | All mapped to a phase? | All have an executable verification? |
|---|---|---|---|
| Project Description (PD) | 4 | ✅ | ✅ |
| 1. Environment (R1) | 5 | ✅ | ✅ |
| 2. Ingestion (R2) | 5 | ✅ | ✅ |
| 3. RAG (R3) | 5 | ✅ | ✅ |
| 4. Agentic design (R4) | 5 | ✅ | ✅ |
| 5. MCP (R5) | 5 | ✅ | ✅ |
| 6. Web app (R6) | 5 | ✅ | ✅ |
| 7. Deployment (R7) | 5 | ✅ | ✅ |
| 8. CI/CD (R8) | 4 | ✅ | ✅ |
| 9. Evaluation (R9) | 5 | ✅ | ✅ |
| 10. Design docs (R10) | 3 | ✅ | ✅ |
| Demo video (DEMO) | 7 | ✅ (P12 script + gate G7) | ⚠ DEMO.1's README link line is asserted; DEMO.2–DEMO.7 are human-performed and verified against **`docs/pre-submission-checklist.md`** — a committed P12 artifact whose existence, and whose one-line-per-id completeness, `test_docs_completeness.py` asserts (§15.1) |
| Submission (SUB) | 3 | ✅ (G1, G7) | ⚠ SUB.2 is asserted by `test_docs_completeness.py` + `smoke_deployed.py`; SUB.1 and SUB.3 are human-confirmed against **`docs/pre-submission-checklist.md`** (existence and completeness asserted) |
| Repo contents (DOCS) | 9 | ✅ | ✅ |
| Rubric level 5 (RUBRIC5) | 9 | ✅ | ✅ |
| User constraints (USER) | 8 | ✅ | ✅ |
| **Total** | **87** | **87/87** | **79 automated · 8 human-confirmed against `docs/pre-submission-checklist.md` (DEMO.2–DEMO.7, SUB.1, SUB.3) — the checklist itself, and `docs/demo-script.md`, are asserted to exist and to be complete by `test_docs_completeness.py`** |
