# Requirements Traceability Matrix — Mosaic HR Copilot

**Project:** `quantic-mosaic` · Quantic "AI Engineering Techniques and Architectures"
**Date:** 2026-09-09 · **Statuses last read against the tree at the publish step, 2026-09-11.**

**Where the 87 rows stand.** **72 are `built`** — every clause of their *Verification* column that
does not need a live service passes in the CI suite at this commit. **4 are now `verified` against
the deployed instance**: **R7.1** (live URL + green smoke), **SUB.2** (the tokenized `Deployed:`
link), **RUBRIC5.6** (free-tier deployment, env vars, measured cold start) and **RUBRIC5.1**, which
is verified on two of its three thresholds and **published short on the third** — strict pass 0.808
against ≥ 0.85, with each failing item's clause named rather than the bar moved. **11 stay
`planned — verified at publish`**, every one of them waiting on a human: the recording at gate 6
(**DEMO.1–DEMO.7**, **RUBRIC5.3**, **RUBRIC5.9**) and the submission at gate 7 (**SUB.1**,
**SUB.3**). Two **project-added** rows sit outside the 87 and say so in their own cells — **R6.6**
(streaming and step narration) and **DOCS.10** (the optimization log) — because they are things
this build shipped that the checklist never asked for, and folding them into a neighbouring row
would misreport both. No row is promoted on the strength of prose. Gate numbers throughout this
file are `NEEDS-FROM-USER.md`'s, which are design spec §19.1's.

**Sources**
- Requirement text: [`docs/project-requirements.md`](project-requirements.md) (extracted from the authoritative PDF)
- Design that addresses it: [`docs/superpowers/specs/2026-09-08-hr-agentic-rag-design.md`](superpowers/specs/2026-09-08-hr-agentic-rag-design.md) (**v2**) — cited as **§n**
- Phase that builds it: [`docs/superpowers/plans/2026-09-08-implementation-roadmap.md`](superpowers/plans/2026-09-08-implementation-roadmap.md) (**v2**) — cited as **P0…P12**

**How to use this file.** Every row's *Verification* column names a command, a test file or a committed artifact — never a claim. As each phase lands,
flip that row's *Status* to `built` when the verification passes in CI, and to `verified` when it has additionally passed against the **deployed**
instance. A row may never be marked complete on the strength of prose.

**Status vocabulary:** `planned` → `built` (green in CI) → `verified` (green against the deployed URL or, for human-only items, confirmed by the user).
A row reading `planned — verified at publish` is one whose verification cannot run at all until the live URL, the recording or the submission exists;
it goes straight to `verified` at that point, never through `built`.

**Counts:** 87 checklist items — PD ×4, R1–R10 ×47, DEMO ×7, SUB ×3, DOCS ×9, RUBRIC5 ×9, USER ×8
— plus 2 project-added rows (**R6.6**, **DOCS.10**) that are counted separately everywhere.

**v2 note.** The verification methods below name only mechanisms v2 keeps. The frozen clock, HMAC confirm tokens, AST sole-writer/sole-caller tests, the
corpus generator and its numeric-claim gate, the `eval.yml` workflow, the committed replay cache and trace archive, the docs-diff check, Cohen's κ,
branch protection, the `fresh-clone` job and the 21-step CI job are gone (spec §22); every row that cited one now cites its v2 replacement.

---

## Project Description — corpus, mock data, cost

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **PD.1** | Coherent policy corpus of ~5–20 short md/HTML/PDF/TXT files totalling 30–120 pages | §5.3 (14 files, ~63 pages, 4 formats), §5.2 (`corpus/facts.yml` is an index into authored prose, not a generator) | **P2** | `python scripts/corpus_stats.py` prints files, pages, words and the per-format table; `tests/unit/test_corpus_stats.py` asserts the **bands** `5 ≤ files ≤ 20` and `30 ≤ pages ≤ 120` — never an exact figure, so ordinary wording edits cannot fail the build (§22 row 13) | built |
| **PD.2** | Corpus supports realistic questions on PTO, holidays, remote work, expenses, data security, benefits, onboarding, equipment, leave, workplace conduct | §5.3 (topic → document map: PTO ①, holidays ①, remote ②③, expenses ④⑤, security ⑭, benefits ⑦, onboarding ⑨, equipment ⑥, leave ⑧, conduct ⑩⑬) | **P2** | `tests/unit/test_corpus_topics.py` asserts each of the ten topics maps to ≥ 1 document; the map is published in the hand-authored `corpus/README.md`, and no generator writes into any document | built |
| **PD.3** | Small mock structured datasets, clearly synthetic, no real private data | §5.4 (6 committed JSON files, the `_synthetic` banner, the **`as_of: 2026-09-01` snapshot**, 24 non-contiguous ids matching `^E1[0-9]{3}$` with the anchors `E1002`, `E1007`, `E1042`, `E1108`, `.example` emails, 555 phones, **no SSN field exists**) | **P3** | `tests/unit/test_mock_schemas.py` validates every file against `mock_data/schemas/*.schema.json`; `python scripts/pii_check.py` fails the build on SSN shapes, non-`.example` addresses or non-555 E.164 numbers; `tests/unit/test_mock_anchor_ids.py` asserts the four anchors are present, that all 24 ids are unique and well-formed, and that `E1108` is ineligible with `waiting_period_ends == "2026-11-13"` — **after the 2026-09-01 snapshot, not after "today"**; `tests/unit/test_pto_balance_arithmetic.py` asserts `E1042` resolves to `13.5` at the snapshot; `mock_data/README.md` carries the SYNTHETIC DATA banner and the snapshot convention | built |
| **PD.4** | Use free / zero-cost model and embedding options; keep the corpus and datasets small enough for modest free-tier resources | §3 rows 6, 7, 8, 15; **§9.8 (the model-allocation table, `MODEL_PRICES` and `LLM_DAILY_CALL_CAP`)**; §14.1 `## Cost`; §14.3 memory budget | **P4, P11, P12** | `deployed.md`'s `## Cost` names each provider, model id and tier with the date observed, and states the agent's **estimated** spend (under $10 all in) beside the judge — free-tier until 2026-09-10, billed at $0.30 / $2.50 per MTok since, ≈ $0.16–$0.18 a pass (249–296 calls) — and keyless local embeddings; `.env.example` documents the zero-cost `LLM_PROVIDER=openai_compat` path a grader can run instead; `make docker-run-512` records the measured `rss_mb < 420` under `docker run -m 512m` at P11; `test_docs_completeness.py` (P12) asserts the `## Cost` heading | built |

---

## 1. Environment and Reproducibility

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R1.1** | Create a virtual environment | §3 rows 1–2 (`uv` + `python3.12 -m venv`), §4 (the `Makefile`) | **P0** | README's `## Setup` names `python3.12 -m venv .venv` literally and `make setup` runs it; `tests/contract/test_readme_headings.py` asserts the heading and that command string at P0, and `tests/contract/test_docs_completeness.py` re-asserts it at P12 | built |
| **R1.2** | List dependencies in a manifest with pinned versions | §3 row 2, §4 (`pyproject.toml` authoritative, `requirements.txt` compiled and committed, `requirements-dev.txt` never in the image) | **P0** | The CI `test` job installs **only** from the committed manifests (`pip install -r requirements.txt -r requirements-dev.txt`); no ad-hoc installs anywhere; the Dockerfile installs from `requirements.txt` alone | built |
| **R1.3** | README.md with setup, local run, deployment and evaluation instructions | Appendix A P0 / P12 | **P0** (stub) → **P12** (final) | `tests/contract/test_readme_headings.py` (P0) asserts `## Setup`, `## Local Run`, `## Deployment`, `## Evaluation`, `## Third-party components` and the three first-20-line link lines with the `TBD-before-submission` placeholder accepted; `test_docs_completeness.py` (P12) re-asserts them with the placeholder gone | built |
| **R1.4** | Fixed seeds where applicable — deterministic chunking, evaluation sampling | §6.3 (the chunker is a pure function of the corpus bytes plus four constants, so no seed is needed), §13.6 (the determinism table) | **P4** (chunking), **P10** (sampling) | `tests/unit/test_chunking_deterministic.py` re-runs the chunker and asserts the manifest is **byte-identical** to the committed file (`python -m hrmosaic.rag.ingest --verify-manifest` runs the same check in CI, **on chunking only, never vectors**); eval item order is the order of `evaluation/dataset.yaml` — no sort, no shuffle; `SEED = 1729` is a **module constant in `core/ids.py`** used only where sampling exists (the 8 reference-labelled items), and `tests/unit/test_reference_subset_deterministic.py` builds that subset twice under different `PYTHONHASHSEED` values and asserts element-for-element identity. **There is no clock to freeze and no `SEED` environment variable** (§13.6, §22 row 1) | built |
| **R1.5** | Secrets read from environment variables, never committed | §12.3 (all secrets via `settings.py`, `APP_ACCESS_TOKEN` included), §10.4 (`redact()`), §17 rows 1 and 3 | **P0** (+ **P1** for `redact`) | The CI `lint` job runs `gitleaks` over **full history**; `.env` is gitignored and `.env.example` carries no real values; `tests/contract/test_env_example_covers_settings.py` asserts the `.env.example` ↔ `Settings` bijection in **both** directions (§12.3 declares no field that no shipping code reads); `tests/unit/test_g6_redact.py` proves leaked values are scrubbed from every payload while the three integer token counts survive | built |

---

## 2. Corpus Ingestion and Indexing

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R2.1** | Parse and clean policy documents, ≥ 2 source formats | §6.2 (four parser paths: md, HTML, PDF, TXT), §6.1 (the per-format ingest report) | **P4** | One unit fixture per format under `tests/fixtures/corpus_mini/` asserting extracted text and heading structure; the PDF path asserts the extracted heading set **equals** `workplace-conduct.src.md`'s; `tests/unit/test_ingest_report.py` asserts `data/index/ingest_report.json` carries all four formats with non-zero counts, that the rows sum to `totals`, and that `totals` equals the `documents` / `chunks` row counts — never literals | built |
| **R2.2** | Chunk documents using a justified strategy | §6.3 (heading-aware; 1,400 max / 1,100 window / 150 overlap / 120 min; `heading_path` joined with `" > "` before hashing; `chunk_id = "c_" + sha256(...)[:16]`) | **P4** | `tests/unit/test_chunking.py` — a named P4 deliverable and gate — asserts heading-path propagation and overlap behaviour on fixtures; the justification is a required `###` subsection of `design-and-evaluation.md` asserted by `test_docs_completeness.py` (P12); the sizes are env-overridable and swept by the zero-LLM `scripts/chunk_size_sweep.py` comparison | built |
| **R2.3** | Embed chunks with a free embedding model / local model / free-tier API | §6.4 (fastembed 0.8.0 + `BAAI/bge-small-en-v1.5`, 384-dim ONNX, no torch, no key) | **P4** | `index_meta.embed_model` and `.dim` are asserted at 384; `tests/unit/test_query_embed_is_asymmetric.py` proves the query-side transform exists and the branch taken is recorded in `CHANGELOG.md`; **two greps in `tests/architecture/test_conventions.py`** pin the single `.embed(` / `.query_embed(` call site and the absence of `parallel=` anywhere under `src/` (the default batch peaked at 1,477 MB RSS and `parallel=1` hung — §20 R-3); the key-free path `EMBED_PROVIDER=fake` is `rag/embed.py::_fake_embed()`, covered by `tests/unit/test_fake_embedder.py` (384-dim, unit norm, identical across two interpreter runs) | built |
| **R2.4** | Store embedded chunks in a local / lightweight vector store | §6.5 (`sqlite-vec` `vec0` declared `distance_metric=cosine` + FTS5 in one read-only file) | **P4** | `python -m hrmosaic.rag.index --selftest` asserts a known query returns the expected `doc_id` with `dense_score ≥ SELFTEST_MIN_DENSE_SCORE` and that `index_meta.chunk_count` equals the committed manifest's line count; `/health.index` reports `chunk_count`, `doc_count` and `embed_model`; the CI `docker` job runs `scripts/probe_sqlite_vec.py` inside `python:3.12-slim` (`enable_load_extension` → `sqlite_vec.load` → `vec_version()`) on every run. The NumPy fallback is **described, not built** (§6.5, §22 row 18) | built |
| **R2.5** | Persist enough metadata for citations — title/ID, section, snippet | §6.5 (the `chunks` table: `doc_id`, `doc_title`, `heading_path`, `section`, `snippet`, `char_start`, `char_end`) | **P4** | `tests/unit/test_chunk_citation_fields.py` asserts **every** stored chunk has all seven citation fields non-empty | built |

---

## 3. RAG

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R3.1** | Top-k retrieval with optional filtering, query rewriting or reranking | §7.1 (hybrid dense k=20 + BM25 k=20 → RRF k₀=60 → top-k=5; `doc_ids`/`topic` filters; `min_dense_score` filter-then-truncate; score-fill from **stored** vectors). **The requirement's enhancements are optional and this design takes FILTERING**: query rewriting is rejected in §3 row 10 — retrieval lives inside the MCP server and `agent/**` may not import it | **P4** | `tests/unit/test_retrieval_filters.py` asserts (a) `len(hits) == k` whenever ≥ k fused candidates clear `min_dense_score` (filter first, truncate second), (b) the `doc_ids`/`topic` filters applied to **both** arms *before* fusion, verified against the pre-fusion candidate sets, (c) a BM25-only candidate emerging with a non-null `dense_score` while **exactly one** query embed occurs for the whole retrieval; `tests/unit/test_min_dense_score_is_not_rrf.py` is the whole-system-returns-zero regression test; the tuned-`k` claim is measured by the `dense_only_k2` ablation arm | built |
| **R3.2** | Prompting strategy that injects retrieved chunks and source metadata | §7.2 (`agent/prompts/synthesize.j2`, the frozen prefix ordering, `<document …>` envelopes carrying `chunk_id`/`doc_id`/`title`/`section`/`rrf`) | **P7** | `tests/contract/test_prompt_golden.py` snapshots the assembled prompt for a fixed query, so a shape change requires a deliberate re-review; the verbatim `messages[]` are stored in `llm_messages` (outside the payload cap) and rendered on dashboard page 5's `llm_call` drill-down for **every** turn | built |
| **R3.3** | Answers cite document IDs / titles / sections with supporting snippets | §7.3 (the `Citation` model: `chunk_id, doc_id, doc_title, heading_path, section, snippet, score, quarantined, source_url`), §11.1 | **P7, P8** | `tests/contract/test_chat_contract.py` asserts a top-level `citations[]` with snippets for both a RAG-only and a tool-using query; the eval's deterministic `cit_resolve` check verifies each cited id exists, its displayed metadata matches the real chunk and its snippet is a whitespace-normalised substring of the chunk text; the UI renders citation chips deep-linking into the corpus browser | built |
| **R3.4** | Guardrails: refuse/redirect out-of-corpus questions, limit unsupported claims, distinguish policy facts from recommendations | §7.4 (**six** rules: G1 evidence gate, G2 citation resolvability, G3 fact-vs-recommendation, G4 injection shield, G5 sensitive escalation, G6 redaction). Identity scoping was dropped — synthetic data, no rubric authorization requirement (§22 row 6) | **P1** (`redact.py` + `test_g6_redact.py`), **P7** (G1–G5) | Six named unit suites (`test_g1_evidence_gate.py` … `test_g6_redact.py`) plus `test_g4_no_false_positives.py` over **every chunk in the committed manifest** (only `security-acceptable-use` chunks may be quarantined, and at least one must be); every decision emits a `guardrail` span browsable on dashboard page 8; the `out_of_scope` eval items assert refusal with **zero** tool calls, the one `sensitive` item asserts escalation (G5's only eval evidence), and `inj-001` — a `simple_policy` item, so that it actually retrieves — asserts the canary chunk is quarantined and uncited (G4's only eval evidence) | built |
| **R3.5** | At least one complex question requiring retrieval from multiple policy documents | §13.1 (5 `multi_doc` items, ≥ 3 of them requiring ≥ 3 distinct documents), §18.1 (demo task 1) | **P8** (`tests/e2e/test_demo_tasks.py`), **P10** (`dataset.yaml`, `test_dataset.py`) | `tests/unit/test_dataset.py` asserts **both levels**: the R3.5 floor — ≥ 1 `multi_doc` item with ≥ 2 distinct `expected_docs` — **and** the design claim, `len([i for i in items if i.category == "multi_doc" and len(set(i.expected_docs)) >= 3]) >= 3`, so the dataset cannot drift to two-document questions while every cited test stays green; `tests/e2e/test_demo_tasks.py` asserts task 1's citations span **≥ 3 distinct documents**; `DocRecall` is reported per item | built |

---

## 4. Agentic System Design

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R4.1** | Orchestrator interprets intent, decides whether RAG alone suffices, selects tools, calls MCP tools, synthesizes | §9.1 (route → act loop → synthesize, with the named entry points `agent/orchestrator.py::run_turn(req)` and `resume_turn(session_id, turn_id, confirmation_token)` that `web/api.py` calls at P8, and the rule that `resume_turn` rehydrates the turn's state from its persisted spans), §9.2 (the router **gates** the catalog to tools 1–4 on `policy_qa`) | **P7** | `tests/e2e/test_rag_only_makes_no_people_calls.py` asserts **zero** non-RAG tool calls on a pure policy question; the decision is visible as the `plan` span's `intent`; the one-step `catalog_reopened` recovery path is reported as its own rate | built |
| **R4.2** | Support at least two multi-step HR workflows | §9.3 (`remote_work_eligibility`, `pto_request` — declarative slots plus completion predicates that **require** a structured-data tool result) | **P8** (authored once, where `/chat/confirm` exists) | `tests/e2e/test_demo_tasks.py` runs each end to end against the two `DemoExpectation` records' `required_tools` + `precedence_edges` + `forbidden_tools`; both are mirrored by eval items `remote-004` and `pto-003`, from which `workflow_completion_by_workflow{}` is computed and rendered on dashboard page 11 | built |
| **R4.3** | Logged trace of agent reasoning at the architectural level; no hidden chain-of-thought | §9.7, §10.2 (the span payload union), §11.1 (the seven-row mapping table) | **P1, P7, P8** | `tests/contract/test_chat_trace_projection.py` asserts `trace[]` equals the set of that turn's spans **and** asserts §11.1's mapping table row by row — the six elements R4.3 enumerates (selected tools; tool arguments; tool outputs; retrieved policy sources; **final answer basis** → `answer_blocks[].citations` + the top-level `citations[]`, plus the synthesize `llm_call`'s `messages_ref` resolving to the verbatim `llm_messages` rows; **any escalation decision** → the `guardrail` entry with `rule_id = G5, verdict = escalate` together with `turns.outcome == "escalated"` and the `escalation` block) plus the clearly-labelled **R4.1** row (the `plan` span's `intent`); `tests/contract/test_no_chain_of_thought.py` asserts no span payload field is named `reasoning` / `thoughts` / `chain_of_thought` and that `rationale_summary` is ≤ 200 characters | built |
| **R4.4** | Handle failures gracefully — unavailable MCP tools, missing employee IDs, incomplete evidence, ambiguous requests | §9.5 (four named paths, all HTTP 200), §8.3 (error semantics) | **P8** | Four fault-injection files — `tests/integration/test_fault_{mcp_down,unknown_employee,empty_retrieval,ambiguous}.py` — **each authored once, in full, at P8**, when `POST /chat` exists: each asserts the orchestrator behaviour (one re-discover attempt then an `error` span `tool_unavailable`; the structured `not_found` converted to a clarification; G1's `guardrail` span carrying the observed scores; the router's `needs_clarification` with zero `tool_call` spans) **and** HTTP 200. `tests/integration/test_health_mcp_down.py` is a separate P8 file covering the `/health` degradation flip. No phase owns half a file (§22 row 15) | built |
| **R4.5** | Prevent irreversible actions — mock actions or explicit user confirmation | §8.5 (both write tools are mock), §8.6 (a random one-time token in the `confirmations` table, minted only in `web/`, validated **inside** the MCP server against the exact tool name and arguments), §13.4 | **P5, P7, P8, P10** | `tests/unit/test_confirmation_gate.py` is the whole gate in three cases — **missing**, **mismatched arguments**, **reused** — each returning `CONFIRMATION_REQUIRED` and writing **no** `mock_writes` row (P5); `tests/unit/test_confirmation_token_stripped.py` proves the orchestrator strips any model-supplied token (P7); `tests/integration/test_confirm_resume_lifecycle.py` covers decline → re-ask → confirm, ending with exactly one `mock_writes` row on one reopened turn (P8); `tests/unit/test_action_safety.py` asserts the four §13.4 clauses (an earlier `confirmed` `confirmation` span in the same turn; every `mock_writes.confirmation_token` resolving to a consumed confirmed row with matching `arguments_json`; no write on a turn with no confirmation span; no token in any response body or persisted payload) over the fixture traces at P8 and over the real eval traces at P10. **There is no HMAC, no `action_digest`, no `CONFIRM_SECRET` and no separate 1.0 gate** — it is an ordinary test that must pass, and the suite gates the deploy (§21 row 23, §22 row 4) | built |

---

## 5. MCP Server and Tool Integration

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R5.1** | One or more MCP servers exposing tools over a supported transport | §8.1 (one `build_hr_server()` factory; **three** transports: mounted Streamable HTTP, stdio, remote via `MCP_SERVER_URL`) | **P5** | `tests/contract/test_mcp_api_shape.py` asserts the mcp 2.2.0 API surface (all four 1.x→2.x differences), so a 1.x-era paste fails in seconds; `tests/integration/test_mcp_discovery.py` completes a real `initialize` handshake over **stdio and mounted HTTP** and asserts the protocol version and `server_info.name == "mosaic-hr"` | built |
| **R5.2** | Expose ≥ 5 tools; ≥ 1 using the RAG index and ≥ 1 using mock data / a mock operation | §8.4 (9 tools: 1–4 RAG, 5–7 mock data, 8–9 gated mock writes) | **P5** | `tools/list` returns 9, and `test_mcp_discovery.py` asserts **≥ 5** each with a non-empty description and an `input_schema` carrying `type` and `properties`; `tests/integration/test_mcp_tool_call.py` asserts **both halves of R5.2 over both transports** — `check_pto_balance("E1042")` returns `remaining_days == 13.5` with `as_of == "2026-09-01"` (the mock-data half, on an anchor id guaranteed present by `test_mock_anchor_ids.py` at P3) **and** `search_policy_documents` returns a non-empty `hits[]` whose `chunk_id`s all resolve via `core.corpusread` (the RAG-index half) | built |
| **R5.3** | Tools should include the eight enumerated names | §8.4 (the `REQUIRED_TOOL_NAMES` frozenset; `list_policy_documents` is a deliberate extra) | **P5** | `tests/contract/test_tools_match_spec.py` asserts `REQUIRED_TOOL_NAMES ⊆ {t.name}` against a **live** `tools/list`, that each has a non-empty `description`, and that each `input_schema` is a JSON Schema object carrying `type` and `properties` | built |
| **R5.4** | The agent must actually call MCP-exposed tools; hard-coded direct calls are insufficient | §2.1 bet 1 (the MCP server is mounted on the app that consumes it; real JSON-RPC over real loopback HTTP), §4.2 (`agent/**` never imports `hrmosaic.mcpserver`) | **P5, P7** | `tests/architecture/test_conventions.py::test_agent_does_not_import_mcpserver` — a grep, not an AST walker (§22 row 5) — proves the orchestrator cannot reach tool implementations directly; a per-turn `mcp_discovery` span plus one `tool_call` span per call are persisted and rendered on dashboard pages 3, 7 and 9; the model's function array **is** the `tools/list` conversion, never a hard-coded list | built |
| **R5.5** | Document the MCP architecture, transport choice, tool schemas, and the discovery flow | §8.1, §8.2 (the discovery flow), §8.4 (generated schemas), §8.7 (`_meta` conventions) | **P5, P12** | `mcp/README.md` carries the transport rationale, the discovery flow, the full 1.x→2.x mapping table and the `_meta` / `_trace` conventions; `scripts/gen_tool_schemas.py` regenerates `mcp/tools/*.schema.json` from the live server and `tests/contract/test_tool_schemas_committed.py` asserts the committed files equal a live `tools/list` — one of the project's only two generated artifacts (§21 row 28). `design-and-evaluation.md`'s MCP section is hand-authored at P12 and checked for **headings**, never diffed against a generator (§22 row 13) | built |

---

## 6. Web Application

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R6.1** | Use a web framework (Flask, FastAPI, Streamlit, Node/Express, Next.js, …) | §3 row 3 (FastAPI 0.141.1 + uvicorn 0.52.4, `--workers 1`) | **P8** | Named in README and present in `requirements.txt`; `tests/contract/test_app_starts.py` boots the app via the documented command (with `EMBED_WARMUP=0`) and polls **`/health`** for 200 with `mcp.connected=true` and `index.loaded=true`; `tests/contract/test_chat_page_renders.py` is what asserts `GET /` returns 200 — two assertions, two tests | built |
| **R6.2** | Chat interface where users ask HR policy and workflow questions | §11.5 (act-as selector, typed answer blocks, citation chips, live span rail, confirm card, snapshot note, cold-start banner, two demo buttons) | **P8** | `tests/contract/test_chat_page_renders.py` asserts `GET /` contains the act-as `<select>`, both demo buttons and the span-rail container, then posts a tool-using message and asserts a `policy_fact` badge, a `recommendation` badge carrying the literal *"Recommendation — not company policy"*, and **≥ 1 citation chip whose `href` is the chunk's `source_url`** | built |
| **R6.3** | `/chat` endpoint returning the final answer, citations, snippets and a concise tool-call trace | §11.1 (all four fields top-level; `trace[]` is the projection of **all** the turn's spans) | **P8** | `tests/contract/test_chat_contract.py` asserts `answer`, `answer_blocks`, `citations[]` (with `snippet`) and `trace[]` are present and schema-valid for **both** a RAG-only and a tool-using query; `test_chat_trace_projection.py` asserts `trace ≡ spans-of-turn` on the **final** response of a turn (the pre-confirmation response is a documented strict prefix) | built |
| **R6.4** | `/health` endpoint returning JSON status including MCP connectivity | §11.4 (always 200; `app`, `mcp`, `index`, `data`, `llm`, `trace_store`, `degradations`) | **P8** | `tests/contract/test_health.py` asserts the field set **and the five-string `degradations[]` vocabulary** — `llm_api_key_missing`, `index_model_mismatch`, `mcp_disconnected`, `trace_store_unreachable`, `access_token_missing` — so every `"degraded"` status carries a machine-readable reason; `tests/integration/test_health_mcp_down.py` points the client at a dead port and asserts `mcp.connected` flips to `false`, `status` to `"degraded"`, and the endpoint still returns **200** | built |
| **R6.5** | A way for the grader to reproduce ≥ 2 agentic demo tasks from the UI or an API client | §11.5, §18 (two one-click buttons **and** two curl scripts) | **P8, P11** | `scripts/demo_task_1.sh` and `scripts/demo_task_2.sh` are parameterised by `BASE_URL`, documented in README, run locally under the stub at P8 and against the deployed URL in the P11/P12 gates; `tests/e2e/test_demo_tasks.py` asserts each produces the documented tool sequence and citations | built |
| **R6.6** *(project-added, not one of the 87 checklist items)* | The answer streams to the browser as it is written, and the rail narrates each step in plain language while it runs | §11.3 (the SSE seam), §1.4 (the streaming non-goal, reversed with Sean's approval 2026-09-10), performance plan W2-E | **P16** | `src/hrmosaic/core/llm/anthropic.py` streams through `client.messages.stream` with a `DeltaSink`, and `Completion.streamed` / `LlmCallPayload.streamed` record that it did; `web/sse.py` and `web/narration.py` emit `step_started` frames at span open with plain-language labels from one mapping, and `agent/answer_stream.py` carries the answer blocks; **the streamed text passes G6 before it leaves the process** (commit `991a6fc`). Unit and integration tests cover the frames, the label mapping and the G6 ordering under `StubAdapter`, which replays as deltas; the one thing tests cannot prove — a real provider streaming — was checked live against the Anthropic API on 2026-09-10: 11 progress lines, 4 answer-delta frames, 24 span frames, a fully cited answer, no errors | **verified** 2026-09-10 (live check) |

---

## 7. Deployment

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R7.1** | Deploy to Render / Railway / equivalent free-tier host at a shareable URL | §14.1 (Render Hobby free, `runtime: docker`, `render.yaml` committed), §14.6 (unattended provisioning) | **P11** | `deployed.md`'s `## Deployed URLs` carries the app and `/health` lines (host-agnostic, asserted by `test_docs_completeness.py` at P12); `scripts/smoke_deployed.py` curls the live URL and `/health` and asserts 200, `mcp.connected` and `git_sha != "dev"` | **verified** 2026-09-10 — live at `https://mosaic-hr-copilot.onrender.com`; smoke green (`status: ok`, `mcp.connected`, `tool_count: 9`, 14 docs / 204 chunks, `trace_store_backend: turso`, `degradations: []`); evidence `deployed.md` §*Deployed URLs* and `data/runtime/post_gate_results.json` |
| **R7.2** | Single-service deployment is acceptable | §14.1, §2 (one process, one container: UI, orchestrator, MCP client, mounted MCP server, index, mock data) | **P11** | `render.yaml` shows one `type: web` service; `/health.mcp.connected == true` on the deployed instance proves the in-process MCP server is reachable; the topology is stated in `design-and-evaluation.md`'s `## Deployment choices` | built |
| **R7.3** | If MCP is a separate service, configure its URL via env vars | §8.1 (remote mode), §12.3 (`MCP_SERVER_URL` computed from the resolved `PORT`; `mcp_transport_effective`) | **P8, P11** | `tests/integration/test_mcp_remote_url.py` runs the agent against a **second local uvicorn** on another port and asserts discovery and a call succeed with `sessions.mcp_transport == "remote"`; `deployed.md`'s `## MCP transport` states verbatim that the server was **not** deployed as a separate service and why, asserted by `test_docs_completeness.py` | built |
| **R7.4** | No paid database; committed data / SQLite / a small local vector store / a free-tier host | §12.1 (`SqliteStore` ‖ Turso free tier), §14.1 `## Cost` | **P11** | No paid DB appears in `render.yaml` or `.env.example`; storage is the read-only SQLite index + committed JSON + Turso free; the Docker build log shows the index built at build time; `deployed.md`'s `## Cost` documents **$0 infrastructure** — no paid database, $0 Render Hobby, $0 Turso, local embeddings — beside the model spend: the agent's **estimated** Anthropic bill (under $10 all in, §9.8) and the billed Gemini judge (≈ $0.16–$0.18 a pass (249–296 calls) since paid billing on 2026-09-10), each with the date observed, **plus both Render free-tier budgets — 750 instance-hours/workspace/month and 500 build minutes/month — and the measured wall-clock of one build**, so the remaining headroom is arithmetic; `scripts/check_render_hours.py` **warns** (never fails) above 600 h or 400 build minutes | built |
| **R7.5** | Explain expected cold-start behaviour if the service spins down | §14.4 (the segment table and the README instruction), §13.5 (cold vs warm distributions, the `cold_probe` protocol) | **P11, P12** | README's `## Deployment` section carries the cold-start note — open `/health` first and wait for 200 — and `deployed.md`'s `## Cold start` carries a measured number and the ISO date it was measured (both reached by `test_docs_completeness.py` at P12); `scripts/measure_cold_start.py` produces it; the eval report and dashboard page 11's metrics tab split cold from warm with `n_cold` / `n_warm`; the demo script narrates it | built |

---

## 8. CI/CD

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R8.1** | GitHub Actions workflow running on push or pull request | §15.1 (`on: push [main]` + `pull_request` + `workflow_dispatch`) | **P0** | `gh run list` shows successful runs triggered by **both** event types — the `pull_request` half is P0's recorded evidence run (throwaway branch → PR → green `test` → the run URL in `CHANGELOG.md` → PR closed), since every phase otherwise commits directly to `main`; `.github/workflows/ci.yml` is committed from P0 so every phase inherits it | built |
| **R8.2** | Workflow installs deps, runs a build/start/import check, and executes tests before deploying | §15.1 (**one file, four jobs**: `lint`, `test`, `docker`, `deploy`; the `test` job runs the whole pytest suite in one command) | **P0** (lint + test), **P2 / P3 / P4** (the three data steps), **P11** (docker + deploy) | The green run log shows install from the committed manifests → the cached fastembed model → `ingest --verify-manifest` → `pytest -q` over the **whole** suite (unit, contract, integration, e2e-with-stub) → `pii_check.py` + `check_facts.py`; then `docker` builds the image, probes sqlite-vec on Debian and health-checks the running container; then `deploy` declares `needs: [test, docker]`. **Each phase adds test files to the suite and nothing to the workflow** — no step numbering, no ownership table, and no step a later phase must retrofit (§22 row 14) | built |
| **R8.3** | ≥ 1 test verifying the app starts, and ≥ 1 verifying MCP discovery or a simple tool call | §15.1, §16.4 | **P5, P8** | `tests/contract/test_app_starts.py` polls `/health` for 200 with `mcp.connected=true` and `index.loaded=true`; `tests/integration/test_mcp_discovery.py` asserts `tools/list` returns **≥ 5** tools each with a non-empty `input_schema`, on stdio and mounted HTTP; `tests/integration/test_mcp_tool_call.py` asserts a valid `tools/call` result on both transports | built |
| **R8.4** | Deployment occurs only if tests pass | §14.5 (two independent mechanisms: `needs:` + Render Auto-Deploy Off), §15.1 job `deploy` | **P11** | The `deploy` job declares `needs: [test, docker]` and `if: (github.event_name == 'push' && github.ref == 'refs/heads/main') \|\| (github.event_name == 'workflow_dispatch' && inputs.deploy_only == 'true')`; `render.yaml` sets `autoDeploy: false`, so the only path from a commit to the running service is the hook this job curls. **There is no branch protection** (§22 row 7) — the evidence is a recorded red run: a temporary branch carrying one deliberately failing test, dispatched with `deploy_only: true`, whose job graph shows `deploy` **skipped with reason "dependent job failed"**, captured as `docs/evidence/ci-deploy-skipped.png` | built |

---

## 9. Evaluation

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R9.1** | 20–30 questions/tasks across policy Q&A and agentic workflows, all five categories, with gold answers | §13.1 (**26** items: 7 `simple_policy`, 5 `multi_doc`, 6 `tool_task`, 3 `ambiguous`, 3 `out_of_scope`, 1 `unsafe_action`, **1 `sensitive`**; absolute dates only; the file's order *is* the run order) | **P10** | `tests/unit/test_dataset.py` asserts `n == 26` (and the `20 ≤ n ≤ 30` band), **all seven** category labels with the counts above, **that every one of the five `expected_behavior` classes — `answer`, `clarify`, `confirm`, `refuse`, `escalate` — carries ≥ 1 item** (the `sensitive` item is the only source of `escalate`, without which the matrix ships with an empty gold row), **that `inj-001` is a `simple_policy` item carrying `security-acceptable-use` in `expected_docs`**, that every `tool_task` item's `expected_end_state` carries a non-empty `requires_tool_results`, that every `out_of_scope` item has `expected_tools: []`, that `pto-001` / `remote-001` / `benefits-001` exist as `simple_policy` (the §13.5 cold probes), that every item has a non-empty gold, that every `gold_facts` key resolves in `corpus/facts.yml`, and that **no** question contains a relative date expression | built |
| **R9.2** | Answer-quality metrics: groundedness, citation accuracy, optional match | §13.3 (claim-level groundedness; **one** `cit_resolve_mean` measured on the served answer beside `blocks_dropped_by_g2`; `CitPrecision` / `CitRecall` / `F1`; `DocRecall`; exact / partial match) | **P10** | The runner emits every metric per item and in aggregate into `evaluation/results/<run_id>.json`; `tests/unit/test_scorer_edge_cases.py` covers every empty-denominator case against the committed fixture traces, so a `ZeroDivisionError` or silent `NaN` can never reach production; dashboard page 11 renders the headline strip. **There is no `cit_resolve_pre`/`_post` pair and no 1.00 build gate** (§21 row 17, §22 row 17). The methodology is a hand-authored section of `design-and-evaluation.md` whose heading `test_docs_completeness.py` asserts (P12), and `scripts/paste_eval_numbers.py` fills the results table from `latest.json` — no CI diff-check of documentation | built |
| **R9.3** | Agent-behaviour metrics: tool selection, workflow completion, escalation/clarification accuracy, action-safety pass rate | §13.4 (all four; deterministic except the ambiguous-item clarification sub-check, which is judged and computed on `baseline` only); the outcome→class mapping table; `gated_attempts` | **P10** | The report JSON carries `tool_selection_accuracy`, `arg_correctness_rate`, `workflow_completion_by_workflow{}`, the 5×5 `escalation_matrix` with `escalation_n_excluded`, `over_refusal_rate`, `missed_refusal_rate`, `action_safety_pass_rate` and the judged `clarification_accuracy` (`float \| None`); `tests/contract/test_dashboard_viewmodels.py` asserts all of those keys exist on every run, with the four judged aggregates null and `judged: false` on the two ablation arms and the deterministic ones non-null everywhere; `tests/unit/test_dataset.py` keeps the matrix's `escalate` row and `MissedRefusalRate`'s denominator non-empty; per-item detail is derived from trace records | built |
| **R9.4** | System metrics: latency p50/p95 over 10–20 tasks, cold vs warm separated | §13.5 (26 eval turns + 3 `cold_probe` re-runs of `pto-001` / `remote-001` / `benefits-001`; cold iff `process_uptime_ms < 60000`, **asserted before tagging, never by construction**) | **P10** (harness + the `local` proving run), **P11** (the published `deployed` run) | Percentiles computed from `turns.duration_ms` via `statistics.quantiles`; the published distribution comes **only** from the `deployed` `baseline` run, and `tests/unit/test_latest_points_at_deployed.py` asserts `latest.json` can never name a `local` run; dashboard page 11's metrics tab shows p50/p90/p95/p99 split cold/warm with `n_cold` / `n_warm` and the host each was measured on; `tests/unit/test_cold_probe_excluded.py` proves probe rows never enter a quality mean; the runner asserts every scored turn's `llm_call` spans have `cache_hit == false` (`LLM_CACHE_TTL_S=0` is the default and is what every latency run uses) | built |
| **R9.5** | At least one ablation or comparison | §13.9 (`baseline` · `dense_only_k2` · `no_structured_tools`, all configured **per request** via `options` against one running instance, plus a zero-LLM chunk-size comparison) | **P10, P11** | `evaluation/ablation.py` writes `comparison.json`, **asserts** every compared run shares `target` and `dataset_sha`, and checks `workflow_completion(no_structured_tools) < baseline − 0.25` — exiting non-zero with an explicit not-supported banner in `REPORT.md` on a null result. **P10 proves the harness with all three variants at `target: local`** (determinism evidence only, never promoted to `latest.json`); **P11 runs all three at `target: deployed`**, and `jq -r '.target, (.variants[].variant)' evaluation/results/comparison.json` must print `deployed` and then the three variant names (the one shared top-level `target` is what `ablation.py` asserts; corrected 2026-09-10, P11 fix round 2 — the earlier `jq -r '.runs[].config_json.target'` named keys the artifact has never carried). `scripts/chunk_size_sweep.py` writes `chunk_size_comparison.json` from temporary indexes (never touching the committed manifest) and `scripts/gen_ablation_evidence.py` captures a 4-tool `tools/list` from a separate stdio server; both render on dashboard page 11's compare tab. Each sweep spends ~560–710 provider calls, of which ~260 are Gemini judge calls on a dedicated key — free until the judge project moved to paid billing on 2026-09-10, ≈ $0.16 a pass since; the paid agent is bounded by `LLM_DAILY_CALL_CAP` and the prompt cache instead (§13.9, §20 R-5) | built |

---

## 10. Design Documentation

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **R10.1** | Justify ten design choices (framework, MCP design, transport, tool schemas, embedding model, chunking, retrieval k, vector store, deployment architecture, guardrails) | §3 (the decision table with its rejected alternatives), Appendix A P12 | **P12** | `tests/contract/test_docs_completeness.py` greps `design-and-evaluation.md` for all ten literal `###` headings; the rejected alternative under each is written from spec §3's own "Alternative rejected" column and checked by the phase reviewer — **headings are asserted, prose is not diffed** (§1.2 row 9, §22 row 13) | built |
| **R10.2** | Architecture diagram showing all seven components | §2 (a mermaid `flowchart TB` labelling Web App, Agent Orchestrator, MCP Client, MCP Server, RAG Index, Mock Structured Data, LLM Provider) | **P12** | `test_docs_completeness.py` asserts each of the seven component names appears in the diagram source in `design-and-evaluation.md`; `tests/architecture/test_conventions.py` proves the code layout respects the boundaries the diagram draws | built |
| **R10.3** | Describe the two demo tasks and the expected MCP tool-call sequence for each | §18.1, §18.2 (ordered sequences with abridged arguments), §18's `DEMO_EXPECTATIONS` | **P8** (the executable half), **P12** (the document) | Both sequences are documented in `design-and-evaluation.md`'s demo section; `tests/e2e/test_demo_tasks.py` compares the **actual** trace against them through two `DemoExpectation` records carrying `required_tools` (every name appearing as an `ok` `tool_call` span), `precedence_edges` (first-`seq` ordering per pair) and `forbidden_tools` (a hard fail on any occurrence), so the sequence cannot silently change while the test stays green; the P12 reviewer checks the documented tables against those records | built |

---

## Submission Guidelines — demo video

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **DEMO.1** | Recorded screen-share demonstration of the working **deployed** application, with screen capture and voiceover | §18.3 (0:00–0:45: the deployed URL visible in the address bar) | **P12** (script) + **gate 6** (record) | The video link is recorded on `README.md`'s `Demo video:` line (regex-asserted, and asserted **not** to read `TBD-before-submission` at P12); the recording shows the deployed URL in the browser address bar with audible narration | planned — verified at publish |
| **DEMO.2** | Between 7 and 10 minutes long | §18.3 (the time-boxed segment table totalling 9:15), §4 (`docs/demo-script.md` in the repo layout) | **P12** + **gate 6** | `docs/demo-script.md`'s segment table is time-boxed per section and rehearsed to fit; `pytest tests/contract/test_docs_completeness.py` asserts the file and that heading exist; the final duration is ticked against the `- [ ] DEMO.2` line in **`docs/pre-submission-checklist.md`** | planned — verified at publish |
| **DEMO.3** | All group members speak and are present on camera | §18.3 (the standing production note: a webcam overlay visible for the **full** 7–10 minutes; continuous narration) | **P12** + **gate 6** | The standing production note is a required section of `docs/demo-script.md`; the picture-in-picture overlay is present in every screen-share segment — a screen-only stretch after 0:45 fails the bullet; verified on playback and ticked against the `- [ ] DEMO.3` line in `docs/pre-submission-checklist.md` | planned — verified at publish |
| **DEMO.4** | All group members show their government ID | §18.3 (the ID held legibly still for ≥ 3 s at ~0:15) | **gate 6** | The recording contains the ID segment, framed large enough to read; ticked against the `- [ ] DEMO.4` line in `docs/pre-submission-checklist.md`, whose existence and one-line-per-id completeness `test_docs_completeness.py` asserts | planned — verified at publish |
| **DEMO.5** | The demo shows the system carrying out two agentic tasks end-to-end | §18.1, §18.2, §18.3 (1:30–3:30 and 3:30–5:30) | **gate 6** | Both tasks executed live against the deployed URL from question to final cited answer/action via the one-click buttons, with no manual intervention; ticked against the `- [ ] DEMO.5` line in `docs/pre-submission-checklist.md`. (`tests/e2e/test_demo_tasks.py` independently proves the sequences under the stub) | planned — verified at publish |
| **DEMO.6** | For each task, explain tool names, arguments, outputs, retrieved citations and the final answer/action | §18.1 ①–⑤, §18.2, §11.3 (the SSE span rail is the narration surface), §18.3 (`docs/demo-script.md` carries a per-task five-element sub-checklist) | **gate 6** | The five-element sub-checklist is ticked **per task** in `docs/demo-script.md`, and the `- [ ] DEMO.6` line in `docs/pre-submission-checklist.md` records that both were completed; the live span rail shows tool names and durations, spans expand to arguments and results, a citation chip jumps into the corpus browser, and `dashboard_url` opens the same turn's full waterfall | planned — verified at publish |
| **DEMO.7** | Quick walkthrough of design, deployment, CI/CD and evaluation results | §18.3 (0:45–1:30, 6:15–7:00, 7:00–7:40, 7:40–8:45) | **gate 6** | Ticked against the `- [ ] DEMO.7` line in `docs/pre-submission-checklist.md`. Four dedicated segments: the mermaid diagram; `render.yaml` + the `/health` payload + the cold-start numbers; a green CI run plus the recorded red run with the deploy job skipped; the eval dashboard with run detail, judge rationale and the ablation chart | planned — verified at publish |

---

## Submission Guidelines — submission

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **SUB.1** | Submit two links (presentation + GitHub repo) via the dashboard "Submit Project" button | §19.1 item 7 (both links pre-staged at the top of `README.md`) | **gate 7** | Both link lines are present in `README.md`'s first 20 lines and asserted by `test_docs_completeness.py`, which also asserts **neither still reads `TBD-before-submission`** at P12; the user confirms submission through the Quantic dashboard and ticks the `- [ ] SUB.1` line in `docs/pre-submission-checklist.md` | planned — verified at publish |
| **SUB.2** | Include the deployed application URL in the repository README | §15.1 / Appendix A P0 (a host-agnostic `Deployed:` line in the first 20 lines, deliberately **not** `onrender\.com`, and **accepting the literal `TBD-before-submission` exactly as the `Demo video:` and `Repo:` lines do** — an `https://`-only form would fail P0's own `test_readme_headings` on its first run and keep the deploy-gating push path red from P0 through P11) | **P0** (placeholder) → **P11 / P12** (real) | `test_readme_headings.py` (P0) and `test_docs_completeness.py` (P12) assert the line matches — the P0 regex still accepts the placeholder, while at P12 the line must carry the full tokenized link `https://<app>.onrender.com/?access=<token>` (asserted as `Deployed:` carrying `?access=`); the placeholder is retired at P12 with `grep -c 'TBD-before-submission' README.md` == 0; liveness is checked separately and **only** in the `deploy` job by `scripts/smoke_deployed.py`, where a live target genuinely exists | **verified** 2026-09-11 — `README.md`'s `Deployed:` line carries `https://mosaic-hr-copilot.onrender.com/?access=<token>`; `test_docs_completeness.py::test_deployed_link_is_tokenized_or_names_an_open_gate` holds it to the `?access=` form |
| **SUB.3** | Share the GitHub repository with the account `quantic-grader` | §19.1 item 5 (**the repo is ALREADY public — verified 2026-09-08; no visibility change is ever made by script**) | **gate 5** (scripted at P12) + **gate 7** (invite accepted) | At **P12** Claude Code runs `gh api -X PUT repos/seantmalone/quantic-mosaic/collaborators/quantic-grader` and reads back `…/collaborators/quantic-grader/permission`; the grader's acceptance is ticked against the `- [ ] SUB.3` line in `docs/pre-submission-checklist.md` | planned — verified at publish |

---

## Submission Guidelines — repository contents

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **DOCS.1** | Repository contains all developed code | §4 (the repository layout), USER.5 (commit as work proceeds) | **all phases** | `git status` is clean at every phase boundary; the CI `docker` job builds and runs the image from committed files plus env vars only; `git log` shows one commit per phase (three for P9) carrying the roadmap §2.4 trailer | built |
| **DOCS.2** | README.md with introductory description, setup, local run and deployment instructions | Appendix A P0 / P12 | **P0** (stub) → **P12** | `test_readme_headings.py` then `test_docs_completeness.py` assert the five headings plus the deployed-URL line; the documented `## Setup` and `## Local Run` commands are the ones the phase gates actually run | built |
| **DOCS.3** | `design-and-evaluation.md` covering architecture, RAG design, MCP server design, agent orchestration, tool schemas, safety guardrails, deployment choices, and the evaluation questions, expected answers and results | Appendix A P12 (which enumerates the eight `##` DOCS.3 subjects), §13.10 | **P12** | `test_docs_completeness.py` greps for all eight `##` headings, the judge-methodology heading and the `### Security posture` subsection (no user accounts by design, the three controls — access token, persona roles, confirmation gate — and what production would add: SSO, employee-scoped data access, a retention policy); the document is **hand-authored**, and `scripts/paste_eval_numbers.py` writes only the results table's figures from `evaluation/results/latest.json`. There is no generator for the questions section and **no CI diff-check of documentation** (§21 row 28, §22 row 13) | built |
| **DOCS.4** | `ai-tooling.md` naming the AI tools used, how, and what worked / did not | Appendix A P12 (`## What worked well`, `## What did not work`, the DOCS.9 disclosure) | **P12** | `test_docs_completeness.py` asserts both section headings and the disclosure; the content names Claude Code and the Opus subagent workflow used across P0–P12 | built |
| **DOCS.5** | `deployed.md` with the deployed URL, health endpoint URL, and free-tier cold-start notes | §14.1 (the six required headings), §3.1 (the dated facts pasted at P10 / P11 step 0) | **P10 / P11** (numbers) → **P12** (asserted) | `test_docs_completeness.py` asserts `## Deployed URLs` (app + `/health` lines), `## Access` (the tokenized `?access=` link, the `mosaic_access` cookie, the `Authorization: Bearer` header for API clients and MCP Inspector, the two personas, and the post-grading rotation step), `## Cold start` (a measured number **and** the ISO date), `## Environment variables`, `## MCP transport` and `## Cost`; both URLs are live-checked by `smoke_deployed.py` in the `deploy` job | built |
| **DOCS.6** | `evaluation/` with questions, expected answers or rubrics, scripts and reported results | §13.10 (the artifact table), §4 | **P10, P11** | The directory contains `dataset.yaml`, `reference_labels.yaml`, `schema.py`, `deterministic.py`, `judges.py`, `runner.py`, `ablation.py`, `REPORT.md` and `results/` — **one `<run_id>.json` per run** carrying metrics and per-item detail, plus `latest.json`, `comparison.json` and `chunk_size_comparison.json` (§22 row 23 removed the three-file split, and §22 row 2 the replay cache); `tests/integration/test_results_import.py` (P1) proves `core/archive.py` imports them idempotently at boot, so a completely cold database still renders full evaluation pages | built |
| **DOCS.7** | `mock_data/` with synthetic employee, PTO, benefits and/or ticket data | §5.4 (six files, immutable; writes go to the `mock_writes` table in the durable store, never back to JSON) | **P3** | `tests/unit/test_mock_schemas.py` validates every file; `scripts/pii_check.py` passes; each file carries the synthetic banner and its `as_of`; the read/write split holds by construction — both write tools append to `mock_writes`, which `tests/unit/test_action_safety.py` asserts can hold no row without a confirmed, consumed `confirmations` token | built |
| **DOCS.8** | `mcp/` with the MCP server code and tool definitions | §4.1 (**no `mcp/__init__.py`** — the shadowing hazard resolved empirically), §8.4 | **P5** | The directory holds `server_entrypoint.py`, both launchers, `README.md` and the nine generated schemas; `tests/architecture/test_conventions.py::test_mcp_dir_is_not_a_package` asserts `mcp/__init__.py` does not exist; `tests/contract/test_tool_schemas_committed.py` asserts the committed schemas equal a live `tools/list` | built |
| **DOCS.9** | AI-tool use described in `ai-tooling.md`; student responsible for correctness, security and integrity; third-party content licensed and cited | Appendix A P12, §17 (supply chain) | **P0** (vendoring) → **P12** (disclosure) | `ai-tooling.md` carries the AI-use disclosure and an ownership/responsibility statement, asserted by `test_docs_completeness.py`; `static/vendor/LICENSES.md` carries the hand-authored MIT/BSD texts plus, per vendored file, its version and upstream URL written by `scripts/vendor_assets.py` at P0 (with the resolved versions dated in `CHANGELOG.md`); `static/vendor/` holds third-party files only — our own stylesheet is `static/app.css`. **There is no sha256 assertion test** (§22 row 21) | built |
| **DOCS.10** *(project-added, not one of the 87 checklist items)* | `docs/optimization-log.md` — a dated engineering log of every optimization question asked, the evidence gathered, the decision taken and what it changed | §13.10 (results are published with their provenance), §22 row 13 (no docs generator, no CI diff-check) | **P11c → P16, publish** | The log is hand-authored and every entry names its evidence — a run id, a trace query, a Render log line or a script — so a reader can check it: the seven-mitigation failure analysis and its measured column (`r_1789069158_baseline`), the readiness defect found while measuring cold start, the CPU question and its adversarial refutation, the three judged columns (`r_1789055103_baseline` → `r_1789069158_baseline` → `r_1789086979_baseline`) and the performance plan's wave table. `README.md` and `deployed.md` link it; `docs/demo-script.md` §*Optimization story* is drawn from its talking points | **verified** 2026-09-11 |

---

## Rubric level 5

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **RUBRIC5.1** | Outstanding deployed agentic HR system combining policy RAG with correct, cited, grounded responses | §1.2 row 1, §13.3, §13.8 | **P10, P11** | The single **`deployed` `baseline`** run reports groundedness ≥ 0.90 mean, `cit_resolve_mean` ≥ 0.95 and `strict_pass_rate` ≥ 0.85; committed as `evaluation/results/<run_id>.json`, pointed at by `latest.json`, rendered on dashboard page 11 with corpus deep links | **verified on two of the three thresholds; the third is short and is published short.** `r_1789086979_baseline` (deployed, judged, served by `da0dca2`, pointed at by `latest.json`): groundedness **0.982** ≥ 0.90 ✅, `cit_resolve_mean` **1.000** ≥ 0.95 ✅, `strict_pass_rate` **0.808** < 0.85 ❌ — five items, each with its failing clause tabled in `evaluation/REPORT.md` and `design-and-evaluation.md`. Not relaxed and not re-scored |
| **RUBRIC5.2** | MCP integration fully functional — discovery, correct calls, clear traces, graceful error handling | §1.2 row 2, §8.2, §9.5 | **P5, P7, P8, P11** | One `mcp_discovery` span per turn plus a `tool_call` span per call; `/dashboard/mcp` renders live discovery with all nine JSON Schemas (captured as `docs/evidence/mcp-discovery-page.png`); the four §9.5 fault-injection tests prove graceful degradation at HTTP 200; `tool_selection_accuracy` is reported | built |
| **RUBRIC5.3** | ≥ 2 end-to-end agentic tasks in the deployed demo, each multi-step with RAG retrieval and structured/mock-data tool use | §1.2 row 3, §18 | **P8, P11, gate 6** | `tests/e2e/test_demo_tasks.py` carries **two distinct expectation records** (each ≥ 4 tool calls, ≥ 1 retrieval, ≥ 1 structured-data tool, with `required_tools` and `precedence_edges` populated from §18.1 / §18.2; task 2 additionally writes behind the confirmation gate); both are executed live in the video against the deployed URL via the one-click buttons | planned — verified at publish |
| **RUBRIC5.4** | Excellent RAG ingestion, indexing, retrieval, citations and guardrails | §1.2 row 4, §6, §7 | **P4, P7, P10** | `test_chunking_deterministic` (byte-identical rebuild), hybrid retrieval with the `min_dense_score`-is-not-`rrf_score` regression test, six guardrail suites, citations verified against real chunk metadata and offsets, and the `dense_only_k2` + chunk-size comparisons demonstrating tuned `k` and chunking | built |
| **RUBRIC5.5** | Excellent architecture with clear separation of the six components | §1.2 row 5, §4.2, §16.3 | **P1–P9** | `tests/architecture/test_conventions.py` — the **only** structural test file, five greps: spans written only by `core/trace.py`; fastembed called only in `rag/embed.py`; no `parallel=` under `src/`; `agent/` importing no `hrmosaic.mcpserver`; no `mcp/__init__.py`. No AST walker, no carve-out list (§22 row 5). The mermaid diagram matches the package layout | built |
| **RUBRIC5.6** | Free-tier deployment fully functional, env vars and cold-start behaviour documented | §1.2 row 6, §14 | **P11, P12** | `scripts/smoke_deployed.py` passes end to end against the live URL (200, `mcp.connected`, `git_sha != "dev"`); every env var appears in `.env.example` (bijection-tested against `Settings` in both directions) **and is read by shipping code** — §12.3 declares no exporter variable, no `SEED` and no clock override; `render.yaml` carries `autoDeploy: false`, `healthCheckPath: /health` and every `sync: false` secret; cold-start and warm-turn timings are measured and published with their dates | **verified** 2026-09-10 — smoke green end to end against the live URL; `deployed.md` §*Environment variables* lists what the service sets (the ten `render.yaml` keys read back from the API, plus `LLM_RPM`/`LLM_BURST`) and §*Cold start* publishes the measured segments with their dates and their `n` (segment medians 44.8 s → `/health`, 0.1 s → `/ready`, 23.9 s first turn, **71.0 s** total median over 67.5–77.6, **22.5 s** warm; n=3 on 2026-09-10 and 2026-09-11, no keep-alive, raw segments in `docs/evidence/cold-start-probes.json`) |
| **RUBRIC5.7** | CI/CD on push/PR with meaningful build/start checks plus an MCP discovery or call test, deploy gated | §1.2 row 7, §15.1, §14.5 | **P0, P5, P8, P11** | Green runs on **both** events containing `test_app_starts` and `test_mcp_discovery`; `deploy` declares `needs: [test, docker]` with Render auto-deploy off; the recorded red run's job graph shows the deploy job skipped for "dependent job failed" | built |
| **RUBRIC5.8** | Excellent evaluation covering groundedness, citation accuracy, tool selection, workflow completion, safety and latency | §1.2 row 8, §13 | **P10, P11** | One committed report and one dashboard view present **all six** metric families with per-item detail, the methodology, the three-variant ablation, the chunk-size comparison and the **judge agreement rate with its `n`** (8 reference items labelled by an independent, blind Opus subagent — no Cohen's κ, §13.7, §22 row 11) — the headline figures and the ablation all coming from the single P11 `deployed` sweep, with `test_latest_points_at_deployed` and `ablation.py`'s same-target assertion enforcing it | built |
| **RUBRIC5.9** | Excellent design documentation and demo presentation of features, architecture, deployment, MCP calls and evaluation, meeting all demo requirements | §1.2 row 9, §15.1, §18.3 | **P12, gate 6** | `tests/contract/test_docs_completeness.py` green across all R10.* and DOCS.* heading assertions; the three `docs/evidence/*.png` screenshots exist and are referenced; the video covers all five topics and satisfies every DEMO.* item; `docs/pre-submission-checklist.md` is signed off | planned — verified at publish |

---

## User constraints

| ID | Requirement | Spec section | Phase | Verification method | Status |
|---|---|---|---|---|---|
| **USER.1** | Claude Code with Opus subagents builds end-to-end; the human does only what truly requires a human; design for autonomous buildability and verifiability | §19 (the required / optional / automated split), §16.2 (`StubAdapter` makes P0–P9 credential-free), roadmap §5 (seven tracked gates, two discharged, five reaching a human) | **all phases** | `NEEDS-FROM-USER.md` enumerates the **seven tracked gates with a time estimate each**, of which **two are fully discharged** — **gate 5** is marked *scripted — no user action* and **gate 1** *provided 2026-09-09* — so **five reach a human**: three single-paste gates under 10 minutes (**gates 2, 3 and 4**), **gate 6 budgeted at 60–90 minutes** (the recording) and **gate 7 at ~2 minutes** (the submission). ⚠ **No visibility change is ever made by script:** the repository was verified **already public** on 2026-09-08 (`gh api repos/seantmalone/quantic-mosaic --jq .private` → `false`), which P0 merely re-asserts, and the only scripted action under gate 5 is `gh api -X PUT repos/seantmalone/quantic-mosaic/collaborators/quantic-grader` at **P12**, read back with `…/collaborators/quantic-grader/permission`. Every other build/verify step is a scripted command an agent runs; CI reproduces build → index → test with no manual intervention; `CLAUDE.md` pins `model: "opus"` on every delegated call | built |
| **USER.2** | Observability dashboard with full audit logs for every session: turns, LLM calls (prompts/responses/tokens/latency), retrievals (query/chunks/scores), MCP tool calls (name/args/result/duration/errors), guardrail/escalation/confirmation decisions, and the final answer with citations | §10 (the schema + payload union), §11.6 (pages 1–11 **plus the three admin-only write controls**: Reset sandbox → page 8, Re-discover now → page 9, Run smoke eval → page 11; §11 for the access gate and the admin persona that guard every dashboard route, and §10.1's `sessions.auth_mode` / `actor_role`), §11.8 (each has an endpoint: `POST /api/dev/reset-sandbox`, `/api/mcp/rediscover`, `/api/eval/runs`), §7.2 (the `llm_messages` side table), §10.3 (the idempotent boot import of committed eval results) | **P1, P8, P9** | `tests/integration/test_audit_completeness.py` posts one full tool-using turn through `POST /chat` and asserts against the store: exactly one `mcp_discovery` span; ≥ 1 `plan`, `llm_call`, `retrieval`, `tool_call` and `guardrail` span; every `llm_call`'s `llm_messages` row count equalling its `messages_ref.n_messages` with non-empty contents; `arguments` and `result_json` on every `tool_call`; a ranked, scored `chunks[]` on every `retrieval`; non-null turn rollups with `final_answer`, `answer_blocks_json` and resolvable `citations_json`; and `{s.seq for s in response.trace} == {seq of the turn's spans}`. `tests/contract/test_dashboard_pages.py` asserts **all three** write controls are present on their stated host pages (8, 9, 11) and wired to their endpoints in the admin persona, and that every dashboard page and `/api/traces\|eval\|corpus\|mcp/*` read returns **403** `{"code":"ADMIN_REQUIRED"}` without it — §11.6's *"write controls are never rendered dead"* rule, since reaching the host page already proves the persona; `tests/contract/test_personas.py` and `tests/contract/test_access_gate.py` (both **P8**) cover the gate itself; `tests/unit/test_turn_close_records_rss.py` (**P1**) asserts a normally-closed turn carries a non-null `turns.rss_mb_at_end`, and `tests/unit/test_retention.py` asserts the sweep leaves no orphaned `llm_messages` and never prunes an eval-linked session | built |
| **USER.3** | The dashboard must also cover evaluation: runs, per-question results, all six metric families, latency p50/p95 cold vs warm, ablation comparisons | §11.6 page 11 (list · detail · **compare tab** · **metrics tab**) and its metric block — ablation and latency are tabs of the eval view, not separate pages (§22 row 16) | **P9, P10** | `tests/contract/test_dashboard_viewmodels.py` asserts page 11's headline strip and the escalation/safety panel fields exist on every run, that the **four** judged metrics (`groundedness_mean`, `citation_accuracy_mean`, `partial_match_mean`, `clarification_accuracy`) are `Optional[float]` accompanied by `judged: bool` and `n_scored{}` — never fabricated zeros — and that the deterministic metrics are non-null on all three variants; `test_dashboard_pages.py` asserts the list, detail, compare and metrics views render committed eval-run data from **`tests/fixtures/eval_runs/`** — **one `<run_id>.json` per variant** authored at P9 so the pages do not wait on P10, refreshed from the real runs at P10 / P11 | built |
| **USER.4** | The trace/audit data model is first-class: the same records power `/chat`'s trace, the dashboard, the eval reports and the demo narration | §1.3 (one writer, five readers), §2.1 bet 2, §16.3 | **P1** (built first) | `tests/architecture/test_conventions.py::test_only_trace_module_writes_spans` asserts `INSERT INTO spans|turns|sessions` appears only in `core/trace.py` (and in the migrations that create them) — a grep with no carve-out list (§22 row 5); the two writer-side entry points the rest of the system needs are named rather than inlined — **`trace.reopen_turn(turn_id, awaiting_ms)`** for the confirmation resume (called by `web/api.py`) and **`trace.register_span_listener(fn)`** for the SSE hook (registered once by `web/sse.py`, since `core/**` may not import `hrmosaic.web`); `test_chat_trace_projection.py` asserts `trace[] ≡ spans-of-turn`, and `test_audit_completeness` asserts the `/chat` trace ids equal the dashboard payload's | built |
| **USER.5** | Everything is committed to the repo as work proceeds | Roadmap §2.4 (the commit pattern), §4 (per-phase commit), §13.2 (results committed by the main session, never by CI) | **all phases** | `git log` shows one commit per phase (three for P9) with the requirement ids in the trailer; `git status` is clean at every phase boundary; **no CI job writes to the repository** (§22 row 3); no large uncommitted local-only artifact is required to run the system — the index is rebuilt from the committed corpus and manifest | built |
| **USER.6** | Target dev environment: macOS, Node 26 / npm 11 (unused), Python 3.14.6 vs the 3.12 pin, Docker, `gh`; no pnpm/bun; no API keys currently set (no API keys were set at capture; three model keys were supplied 2026-09-09 and live only in the git-ignored `.env` — §19.1) | §3 rows 1–2 (the 3.12 pin in three places plus a runtime **warning**, never a crash), §1.4 (no Node anywhere in the build), §12.3 (deferred credential validation) | **P0, P8** | Setup uses only `uv` / `pip` / `venv`; CI pins the interpreter from `.python-version`; `tests/contract/test_missing_key_is_graceful.py` — **authored once, at P8** (§22 row 15) — asserts all three surfaces: import succeeds; `/health` is 200 `degraded` with `llm_api_key_missing`; and `POST /chat` returns 200 with `outcome: "configuration_required"` naming the missing credential for the configured provider (`ANTHROPIC_API_KEY`, or `LLM_API_KEY` on the free `openai_compat` path — §9.8, §12.3) and its signup URL — never a stack trace, never a failed boot | built |
| **USER.7** | Minimise and prioritise required API keys; prefer free-tier providers or local models | §19.1 (the minimum viable set: the model keys — **provided 2026-09-09** — plus Render + Turso, both free and card-free), **§9.8 (one paid agent key, two Google keys — the judge's project on paid billing since 2026-09-10 at ≈ $0.16 a judge pass, the failover's still free — and a documented zero-cost path)**, §3 row 8 (local embeddings ⇒ **zero** embedding keys) | **P4, P6, P10** | `NEEDS-FROM-USER.md` states the minimum viable set at the top and marks the model keys provided; `LLM_FALLBACK_*` and `JUDGE_*` provide documented provider fallback (free tier until the judge project moved to paid billing on 2026-09-10, ≈ $0.16 a judge pass since — §9.8); `scripts/probe_provider.py` confirms the configured model exists before the judged run; the live free-tier limits per provider are read and **dated** at P10 step 0 / P11 step 0 and pasted into `deployed.md` | built |
| **USER.8** | Verify library/platform facts rather than assuming; ecosystems change as of 2026-09-08 | §3.1 (**the only list of unsettled facts in the document**; no confidence tags anywhere else — §22 row 20) | **P4, P5, P6, P10, P11** | Each §3.1 row names where to read it and when; **P10 step 0 and P11 step 0** read them live and paste the observed value with its date into `deployed.md`. Live gates convert assumptions into measurements: **P4** (`test_query_embed_is_asymmetric`, branch and fastembed version in `CHANGELOG.md`), **P5** (the SDK transport-security grep, recorded in `mcp/README.md` with what was actually found), **P6** (`scripts/probe_provider.py`), **P11** (`make docker-run-512`'s Linux RSS under a real cgroup, the 0.1-CPU turn wall-clock, the measured cold start, the per-build minutes) — each recorded with its date in `CHANGELOG.md` or `deployed.md` | built |

---

## Coverage summary

| Group | Items | All mapped to a phase? | All have an executable verification? |
|---|---|---|---|
| Project Description (PD) | 4 | ✅ | ✅ |
| 1. Environment (R1) | 5 | ✅ | ✅ |
| 2. Ingestion (R2) | 5 | ✅ | ✅ |
| 3. RAG (R3) | 5 | ✅ | ✅ |
| 4. Agentic design (R4) | 5 | ✅ | ✅ |
| 5. MCP (R5) | 5 | ✅ | ✅ |
| 6. Web app (R6) | 5 (+1 project-added) | ✅ | ✅ |
| 7. Deployment (R7) | 5 | ✅ | ✅ |
| 8. CI/CD (R8) | 4 | ✅ | ✅ |
| 9. Evaluation (R9) | 5 | ✅ | ✅ |
| 10. Design docs (R10) | 3 | ✅ | ✅ |
| Demo video (DEMO) | 7 | ✅ (P12 script + gate 6, then gate 7) | ⚠ DEMO.1's README link line is asserted; DEMO.2–DEMO.7 are human-performed and verified against **`docs/pre-submission-checklist.md`** — a committed P12 artifact whose existence, and whose one-line-per-id completeness, `test_docs_completeness.py` asserts |
| Submission (SUB) | 3 | ✅ (gate 5, gate 7) | ⚠ SUB.2 is asserted by `test_readme_headings.py` (P0) / `test_docs_completeness.py` (P12) plus `smoke_deployed.py`; SUB.1 and SUB.3 are human-confirmed against `docs/pre-submission-checklist.md` (existence and completeness asserted) |
| Repo contents (DOCS) | 9 (+1 project-added) | ✅ | ✅ |
| Rubric level 5 (RUBRIC5) | 9 | ✅ | ✅ |
| User constraints (USER) | 8 | ✅ | ✅ |
| Project-added (R6.6, DOCS.10) | 2 | ✅ | ✅ |
| **Total** | **87 + 2** | **89/89** | **81 automated · 8 human-confirmed against `docs/pre-submission-checklist.md` (DEMO.2–DEMO.7, SUB.1, SUB.3) — the checklist itself, and `docs/demo-script.md`, are asserted to exist and to be complete by `test_docs_completeness.py`** |
