# Plan: close every gap in the 2026-09-21 grade report and reach band 5

**Spec:** `docs/project-requirements.md` (the Quantic rubric) is the binding authority.
**Findings:** `.superpowers/sdd/2026-09-21-grade-5/grade-report.md` (the full grade report) and
`.superpowers/sdd/2026-09-21-grade-5/gaps.json` (the 27 ranked gaps, each with `why_it_costs_marks`,
`evidence`, `fix`, `effort`). Every task below names the gap ranks it closes; the implementer reads
those entries in `gaps.json` for the exact evidence and the proposed fix.

## Global constraints

- Do not over-engineer. Change application code only where a task says so, and take the cheapest
  fix the gap entry offers unless the task says otherwise.
- Every number a document states must come from a committed artifact, and the contract tests under
  `tests/contract/` hold the documents to their sources. Run `.venv/bin/pytest -q tests/contract`
  after any documentation change and `make lint` after any code change.
- Repository conventions: Python 3.12 in `.venv`, `ruff` formatting, tests under `tests/`, docs in
  the prose voice already used (plain sentences, measured claims, no marketing). Do not add a
  `Claude-Session` trailer; do end commit messages with
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Never modify `evaluation/results/*.json` by hand; run files are produced only by
  `evaluation/runner.py`.
- The deployed service is https://mosaic-hr-copilot.onrender.com with the bearer token on the
  `Deployed:` line of `README.md`. Do not POST to it except where a task says so.
- Work on `main` (the project's norm; CI deploys `main`). Commit per task; the controller pushes.

## Task 1 — Agent code: clarification names every missing slot; profile debt keyed on arguments; refusal copy; router catalog

Closes gaps **4 (part a, the repair)**, **11**, **18**, **19**, **17**.

1. Gap 4a — `_clarification_text` (in `src/hrmosaic/agent/`) must join every unfilled slot, not
   the first, and `CLARIFY_QUESTIONS` gains an `employee_data` key so a balance/identity ambiguity
   gets its own question that names what is missing. Add a unit test asserting amb-002's and
   amb-003's questions name the missing information (use the dataset questions from
   `evaluation/dataset.yaml`). The judge's `named_missing_information` verdict must be able to
   pass on the served text.
2. Gap 11 — `_profile_outstanding` in `src/hrmosaic/agent/orchestrator.py` must key on the
   recorded call **arguments** of `PROFILE_FIRST_TOOLS` rather than an `employee_id` in the result
   body, so `check_policy_compliance` counts. Add a unit test whose scripted turn calls only
   `check_policy_compliance` for the actor and asserts the profile debt fires.
3. Gap 18 — covered by item 2; add no extra reopen trigger.
4. Gap 19 — `_resume` must not report a refused confirmation token as a failed policy search. Add a
   `CONFIRMATION_REFUSAL` reason with its own user copy (two entries: token invalid, token
   already used or expired), and a test that the user sees confirmation copy, not USER_REFUSAL copy.
5. Gap 17 — render the discovered catalog names (`turn.catalog.names`) into `route.j2`'s user
   block so `selected_tools` is chosen from something the router can see. Keep the cacheable
   system prefix stable. Update `tests/contract/test_prompt_golden.py` fixtures if they pin the
   prompt. If the prompt golden makes this disproportionate, drop `selected_tools` from
   `RouteDecision` instead and say so in the report.

Run `make lint` and the unit + contract suites before committing. Commit as `G5(agent): ...`.

## Task 2 — Evaluation compare: comparability by build, the hypothesis metric on the compare tab, cold cells

Closes gaps **8 (code half)** and **25**.

1. Gap 8 — `evaluation/ablation.py::assert_comparable` must also require a shared
   `target_git_sha` across the arms it compares. The dashboard compare view
   (`src/hrmosaic/web/dashboard.py::build_eval_compare` and its template) must show each arm's
   `target_git_sha` (short form) and must not silently pair arms from different builds: render a
   visible "arms measured on different builds" notice when they differ.
2. Gap 25 — add `workflow_completion` and `doc_recall_mean` to `EvalMetrics`
   (`evaluation/schema.py`) and to `ablation_series`, so the compare tab can chart the ablation's
   own hypothesis metric; surface `workflow_completion_check` (delta and the pre-registered bar) on
   the compare tab; and where the cold-latency cells read n=0, render "not measured (n=0)" rather
   than a bare 0.

Add or extend tests under `tests/contract/test_dashboard_viewmodels.py` and a unit test for
`assert_comparable`. Run `make lint`, the unit and contract suites. Commit as `G5(eval-compare): ...`.

## Task 3 — Configuration and scripts: the dead knob, self-dating demo scripts

Closes gaps **20** and **7**.

1. Gap 20 — `MCP_TOOLS_DISABLED` must be read: use it as the process default for
   `options.tools_disabled` where `ChatOptions` is built, with a unit test. Keep the
   `.env.example` line and correct its comment if needed.
2. Gap 7 — `scripts/demo_task_1.sh` and `scripts/demo_task_2.sh` must produce the documented
   verdicts against **any** server, including the deployed one, without `MOCK_TODAY`. The chat UI
   already self-dates through `demo_prompts()` in `src/hrmosaic/web/api.py`; give the scripts the
   same behaviour (fetch the prompt from the endpoint that serves the UI buttons if one exists, or
   compute the dates in POSIX `sh` the same way). The stub replays under `make demo1`/`make demo2`
   must still pass (`tests/e2e/test_demo_tasks.py` and `tests/contract/test_deploy_scripts_are_runnable.py`).

Run `make lint`, `make demo1`, `make demo2` and the affected suites. Commit as `G5(config-scripts): ...`.

## Task 4 — Measurement on the shipped build (controller pushes and deploys first)

Closes gaps **21**, **2**, **8 (data half)**, **9 (data half)**, **24**, and supplies the numbers
for gaps **1**, **3**, **4b**, **10**.

Precondition (controller): `main` pushed, CI green, `GET /health` `app.git_sha` equals `HEAD`.

1. Gap 21 — run one confirmed `draft_hr_email` turn against the deployed service (the
   manager-message variant of demo task 2, through `/chat` then `/chat/confirm` with the bearer
   token) so `/dashboard/tools` lists calls for all nine tools. Record the session id in the report.
2. Drive the baseline: `EVAL_TARGET_BASE_URL=https://mosaic-hr-copilot.onrender.com
   .venv/bin/python -m evaluation.runner --variant baseline` (with the `.env` credentials), then
   judge it: `--judge <run_id>`. Confirm `evaluation/results/latest.json` names the new run.
3. Drive the two arms on the same build: `--variant dense_only_k2` and
   `--variant no_structured_tools`, then `.venv/bin/python -m evaluation.ablation`. Confirm
   `comparison.json` names three runs with one shared `target_git_sha`.
4. Build both blind label packets for the new baseline with `scripts/gen_label_packet.py`
   (`--subset seed` and `--subset judge_lowest`) into `.superpowers/sdd/2026-09-21-grade-5/`.
   Do **not** author labels; the controller dispatches two independent blind labellers.
5. Gap 24 — the live `/dashboard/evals` lists judged runs with no committed file
   (`r_1789547562_baseline`, `r_1789534779_baseline`). If the dashboard or API exposes the run
   file, download and commit both; otherwise write one sentence for the report that names them and
   their builds, for the documentation task to place.
6. Run `--report <new baseline run_id>` so `evaluation/REPORT.md` describes the published run, then
   `.venv/bin/python scripts/paste_eval_numbers.py` (no `--check`) and confirm `--check` exits 0.

Commit results as `G5(eval): ...`. The report must list: run ids, `target_git_sha`, every headline
metric with its n, the strict-pass causes (`deterministic.strict_pass_causes()`), and the
clarification-accuracy figure. Do not edit prose documents in this task.

## Task 5 — Blind reference labels and agreement (controller-orchestrated)

Closes gap **9**. Two fresh Opus sessions each read only one packet and author
`evaluation/reference_labels.yaml` (seed subset) and `evaluation/reference_labels_hard.yaml` (hard
subset) in the existing schema, with `protocol.blinding` naming exactly one run and stating plainly
which run's served answers the packet carried. Then `--recompute-agreement` for both metrics and
`--report` again. Commit as `G5(labels): ...`.

## Task 6a — README, deployed, traceability, checklist, NEEDS-FROM-USER, corpus/mock/mcp READMEs

Closes gaps **1 (README part)**, **6**, **7 (README caveat)**, **15**, **24 (sentence)**, **26 (doc
note)**, **27 (the README/corpus figures)**, and the traceability/checklist rows that name the
published run. Files owned: `README.md`, `deployed.md`, `docs/requirements-traceability.md`,
`docs/pre-submission-checklist.md`, `NEEDS-FROM-USER.md`, `corpus/README.md`, `mock_data/README.md`,
`mcp/README.md`, `CHANGELOG.md`. Touch no other file.

- README `## Evaluation` publishes the new run id and its figures from `latest.json`; the
  before/after table keeps its "Before" column and the published column is the new run.
- README `## Setup` names `make ingest` immediately after `make setup` and `## Local Run` names it
  before `make run`, with the one-line reason (the index is built, not committed).
- README `## Evaluation` carries the full recipe: drive, judge, the two variants, ablation, with the
  env var each step needs and the note that `make eval` rewrites `REPORT.md`.
- README's demo-script paragraph states what the scripts reproduce against which server (Task 3
  decides whether the caveat is still needed; read its report).
- README line 11 keeps a video placeholder but phrased for a reader, not an internal gate: `Demo
  video: link added at submission` — unless a URL is provided in the dispatch.
- `deployed.md`: the run history table gains the new run; a short note that the hosted trace store
  is a hard dependency of the chat path and what a reader sees if it is unreachable; the live sha
  after the final deploy.
- Every stale figure gap 27 names in these files.
- A `CHANGELOG.md` entry for this wave (G5) in the file's existing style.

Run `.venv/bin/pytest -q tests/contract` before committing. Commit as `G5(docs-readme): ...`.

## Task 6b — design-and-evaluation.md and optimization-log.md

Closes gaps **1 (design-doc part)**, **2 (disclosure)**, **3**, **4b (disclosure)**, **10**, **16**,
**27 (design-doc figures)**. Files owned: `design-and-evaluation.md`, `docs/optimization-log.md`.
Touch no other file.

- The EVAL-NUMBERS block is already regenerated by Task 4; hand-update the surrounding prose: the
  published run id everywhere it appears, the history table gains a column for the new run, the
  "Known limitations" list is rewritten from `deterministic.strict_pass_causes()` on the new run
  and says which earlier limitations were closed and when.
- Clarification accuracy is stated with its n and, if below 1.0, the item that fails and why.
- Action safety and escalation state their real n from `n_scored`; no "28" for a metric scored on
  fewer items.
- The MCP section states the measured error behaviour (a schema violation returns `isError: true`
  with the validator's text; no `-32602` on the wire; the orchestrator keys on `is_error`) and
  drops the retracted claims gap 16 names.
- `docs/optimization-log.md` gains a dated section for this wave with a published-measurements
  table for the new run beside the previous column, in the file's existing style.

Run `.venv/bin/pytest -q tests/contract` before committing. Commit as `G5(docs-design): ...`.

## Task 6c — docs/demo-script.md

Closes gaps **5**, **13**, **14**, and the script's superseded evaluation causes and admin-profile
instruction. File owned: `docs/demo-script.md` only.

- Both DEMO.6 sub-checklists run each live turn twice-framed: the chat page for the answer, the
  `Sources (n)` strip and the confirmation card; then `/dashboard/sessions/{id}` (the
  `dashboard_url` every `ChatResponse` returns, surfaced as "Open this conversation's record") for
  tool names, arguments and outputs. Remove every reference to the deleted span rail. Budget the
  switch in the segment timings and keep the total between 7:00 and 10:00.
- CI/CD beat: five jobs (lint, test, ux, docker, deploy), why `ux` sits outside `needs:`, and the
  test count read off the screen (about 3,040 in the test job; 3,339 including the 299 browser
  tests) — verify both numbers with `pytest --collect-only -q` before writing them.
- Observability beat: describe what the waterfall shows (every model call with its purpose, the
  tools it was offered, its token counts and the text it returned); no claim of verbatim messages.
- Evaluation beat: the new published run's figures and its failing items (read
  `evaluation/REPORT.md` after Task 4).
- Remove the instruction to switch to an admin profile for reads that no longer require it;
  verify against `src/hrmosaic/web/` which routes need HR admin.

Run `.venv/bin/pytest -q tests/contract tests/unit/test_demo_prompts_are_dated.py`. Commit as
`G5(demo-script): ...`.

## Task 6d — ai-tooling.md

Closes gap **12**. File owned: `ai-tooling.md` only.

- Period header runs to the final build date; a new section covers 2026-09-14 to 2026-09-16 and
  this wave: the UX audit/fix loop (UX-W1 to W7), the demo-path logic waves (W8 to W10), the
  independent-auditor pattern, the two score trajectories (UX principles 6/15 to 9/15; persona
  scenarios 5/16 to 13/16), and this grade-and-fix pass (an independent 82-agent grading workflow,
  adversarial verification of each finding, Opus implementers per task with a reviewer per task).
  Source material: `docs/process/sdd/`, `docs/evidence/ux-*`, `docs/optimization-log.md`,
  `git log --since=2026-09-14`.
- Keep the file's existing honesty: what did not work is stated with the same specificity as what
  did.

Run `.venv/bin/pytest -q tests/contract`. Commit as `G5(ai-tooling): ...`.

## Task 7 — Final verification (controller)

Closes gap **22**. `make lint`, `make test`, `make coverage` gate, `paste_eval_numbers.py --check`
exit 0, `check_facts.py`, `pii_check.py`; push; dispatch `ci.yml` with `deploy_only=true` so the
tip commit carries a green run and the live sha equals HEAD; re-run the grading workflow.
